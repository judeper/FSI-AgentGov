---
phase: 04-feature-enhancement-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/controls/pillar-1-security/1.5-data-loss-prevention-dlp-and-sensitivity-labels.md
  - docs/playbooks/control-implementations/1.5/portal-walkthrough.md
  - docs/playbooks/control-implementations/1.5/verification-testing.md
autonomous: true

must_haves:
  truths:
    - "Control 1.5 documents all 11 virtual governance connectors with standardized Feature | Status | Description | Configuration table"
    - "Virtual connectors are integrated as natural extension of existing DLP section, not a separate subsection"
    - "Playbook portal-walkthrough includes steps for configuring virtual connector DLP policies"
    - "Playbook verification-testing includes test cases for virtual connector enforcement"
  artifacts:
    - path: "docs/controls/pillar-1-security/1.5-data-loss-prevention-dlp-and-sensitivity-labels.md"
      provides: "Enhanced DLP control with complete virtual connector governance table"
      contains: "Feature | Status | Description | Configuration"
    - path: "docs/playbooks/control-implementations/1.5/portal-walkthrough.md"
      provides: "Updated portal walkthrough with virtual connector configuration steps"
    - path: "docs/playbooks/control-implementations/1.5/verification-testing.md"
      provides: "Updated verification testing with virtual connector test cases"
  key_links:
    - from: "docs/controls/pillar-1-security/1.5-data-loss-prevention-dlp-and-sensitivity-labels.md"
      to: "docs/playbooks/control-implementations/1.5/portal-walkthrough.md"
      via: "Implementation Guides section link"
---

<objective>
Enhance Control 1.5 (DLP and Sensitivity Labels) with complete virtual governance connector documentation using standardized table format, and update associated playbooks with configuration and verification steps.

Purpose: FSI organizations need clear guidance on all 11 Copilot Studio virtual governance connectors to configure DLP policies that protect against data exposure through AI agents. Control 1.5 already has partial virtual connector content (connector categories table at lines 63-72, 11 connectors mentioned in enforcement admonition at lines 45-52) — this plan enhances with the standardized Feature | Status | Description | Configuration table and ensures playbooks cover virtual connector configuration.

Output: Enhanced Control 1.5 with standardized virtual connector table, updated portal walkthrough playbook, updated verification testing playbook.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-feature-enhancement-updates/04-CONTEXT.md
@.planning/phases/04-feature-enhancement-updates/04-RESEARCH.md

Reference files (read before modifying):
@docs/controls/pillar-1-security/1.5-data-loss-prevention-dlp-and-sensitivity-labels.md
@docs/playbooks/control-implementations/1.5/portal-walkthrough.md
@docs/playbooks/control-implementations/1.5/verification-testing.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Control 1.5 with standardized virtual connector table</name>
  <files>docs/controls/pillar-1-security/1.5-data-loss-prevention-dlp-and-sensitivity-labels.md</files>
  <action>
Read the existing Control 1.5 file. It already has:
- Lines 45-52: Mention of 11 virtual governance connectors in the enforcement admonition
- Lines 63-72: "Copilot Studio DLP Connector Categories" table (Knowledge Sources, Channels, Actions, AI Services)

Enhance the existing "Copilot Studio DLP Connector Categories" section (do NOT create a new subsection — integrate naturally per CONTEXT.md decision). Add a standardized Feature | Status | Description | Configuration table AFTER the existing connector categories table that lists all 11 individual virtual governance connectors. The 11 connectors are:

1. AI Builder (GPT) — GA
2. AI Builder (Document Processing) — GA
3. Copilot Studio Topics — GA
4. Copilot Studio Skills — GA
5. Copilot Studio Knowledge — GA
6. HTTP with Microsoft Entra ID — GA
7. HTTP Webhook — GA
8. Direct Line — GA
9. Microsoft Teams Channel — GA
10. SharePoint Channel — GA
11. Custom Website Channel — GA

Format the table as:

| Connector | Status | Description | Configuration |
|-----------|--------|-------------|---------------|
| AI Builder (GPT) | GA | Controls access to GPT models via AI Builder | PPAC > DLP Policies > Connector classification |
| ... | ... | ... | ... |

Include FSI governance notes as a separate column or inline where applicable. Ensure the new table integrates seamlessly with existing content — no "New Features" or "2026 Updates" headings. The existing connector categories table (Knowledge Sources, Channels, Actions, AI Services) provides the organizational view; the new table provides the individual connector inventory.

Also verify and update the enforcement admonition (lines 45-52) to ensure the 11 connectors listed there are accurate and consistent with the new table.

Do NOT change the control's existing structure, section ordering, or metadata header.
  </action>
  <verify>
Run: `python3 -m mkdocs build --strict` from project root — must pass with zero errors.
Grep for "Feature | Status | Description | Configuration" or "Connector | Status | Description | Configuration" in the control file to confirm table exists.
  </verify>
  <done>
Control 1.5 contains a standardized table listing all 11 virtual governance connectors with Status, Description, and Configuration columns. Table is integrated naturally into existing DLP Connector Categories section without creating a new subsection. Enforcement admonition connector list is consistent with table.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update playbooks with virtual connector configuration and verification</name>
  <files>
docs/playbooks/control-implementations/1.5/portal-walkthrough.md
docs/playbooks/control-implementations/1.5/verification-testing.md
  </files>
  <action>
Read both playbook files before modifying.

**Portal Walkthrough (portal-walkthrough.md):**
Add a section covering virtual connector DLP configuration steps. Integrate into the existing flow (not as a standalone appendix). Steps should cover:
1. Navigate to Power Platform Admin Center > Data Policies
2. Select or create a DLP policy
3. Locate the virtual governance connectors in the connector list
4. Classify AI-related connectors (AI Builder, Copilot Studio, HTTP) into appropriate categories (Business, Non-Business, Blocked)
5. Configure HTTP endpoint filtering for external API calls
6. Save and verify policy propagation

Include FSI-specific guidance: "For Zone 3 environments, classify all HTTP connectors and AI Builder connectors as Business-only to prevent unauthorized external data access."

**Verification Testing (verification-testing.md):**
Add test cases for virtual connector enforcement:
1. Test: Create agent using blocked connector category → Expected: Agent creation blocked with DLP error
2. Test: Agent attempts HTTP call to non-allowlisted endpoint → Expected: Call blocked by HTTP endpoint filter
3. Test: Verify DLP policy shows all 11 virtual connectors classified → Expected: All connectors appear in policy with correct classification
4. Test: Agent uses Knowledge Source connector to access approved SharePoint site → Expected: Access allowed

Add evidence collection guidance for virtual connector DLP compliance.

Maintain existing playbook formatting, heading levels, and style conventions.
  </action>
  <verify>
Run: `python3 -m mkdocs build --strict` from project root — must pass with zero errors.
Verify new sections exist in both playbooks by grepping for "virtual connector" (case-insensitive).
  </verify>
  <done>
Portal walkthrough includes step-by-step virtual connector DLP configuration. Verification testing includes 4+ test cases for virtual connector enforcement with evidence collection guidance. Build passes with zero errors.
  </done>
</task>

</tasks>

<verification>
1. `python3 -m mkdocs build --strict` passes with zero errors
2. Control 1.5 contains standardized connector table with all 11 virtual governance connectors
3. Portal walkthrough includes virtual connector configuration steps
4. Verification testing includes virtual connector test cases
5. No "New Features" or temporal section headings created (seamless integration)
</verification>

<success_criteria>
- All 11 Copilot Studio virtual governance connectors documented in standardized table format in Control 1.5
- Playbooks updated with configuration steps and test cases for virtual connector DLP
- Build passes `mkdocs build --strict`
- Content integrated seamlessly into existing sections without visible seams
</success_criteria>

<output>
After completion, create `.planning/phases/04-feature-enhancement-updates/04-01-SUMMARY.md`
</output>
