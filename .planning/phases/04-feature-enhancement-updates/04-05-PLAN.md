---
phase: 04-feature-enhancement-updates
plan: 05
type: execute
wave: 2
depends_on: [04-01, 04-02, 04-03, 04-04]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "mkdocs build --strict passes with zero errors after all Phase 4 changes"
    - "verify_controls.py reports all 62 controls valid"
    - "No prohibited regulatory language introduced (ensures compliance, guarantees, will prevent, eliminates risk)"
    - "Researcher package regenerated with Phase 4 content"
    - "All cross-references between updated controls resolve correctly"
  artifacts: []
  key_links: []
---

<objective>
Validate all Phase 4 changes through comprehensive build verification, cross-reference checking, regulatory language compliance, and researcher package regeneration.

Purpose: Phase 4 modifies 4 controls (1.5, 1.6, 1.8, 3.8), 1 reference document (role catalog), and 6 playbooks across 4 plans. This validation plan ensures all changes integrate correctly, no prohibited language was introduced, and cross-references resolve. Follows the validation pattern established in Phase 3 (Plan 03-03).

Output: Verified build, confirmed cross-references, regenerated researcher package.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-feature-enhancement-updates/04-CONTEXT.md

Prior plan summaries (read for context):
@.planning/phases/04-feature-enhancement-updates/04-01-SUMMARY.md
@.planning/phases/04-feature-enhancement-updates/04-02-SUMMARY.md
@.planning/phases/04-feature-enhancement-updates/04-03-SUMMARY.md
@.planning/phases/04-feature-enhancement-updates/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full build and structural validation</name>
  <files></files>
  <action>
Run the full validation suite:

1. `python3 -m mkdocs build --strict` — Must pass with zero errors/warnings
2. `python3 scripts/verify_controls.py` — Must report all 62 controls valid

If either fails, identify the cause and fix it. Common issues:
- Broken internal links from new cross-references
- Markdown syntax errors in new tables
- Missing navigation entries

3. Verify regulatory language compliance — grep across all modified files for prohibited phrases:
- "ensures compliance"
- "guarantees"
- "will prevent"
- "eliminates risk"

If any prohibited phrases are found, replace with approved alternatives per CONTRIBUTING.md:
- "ensures compliance" → "supports compliance with" or "helps meet"
- "guarantees" → "helps support" or "aids in"
- "will prevent" → "helps prevent" or "recommended to prevent"
- "eliminates risk" → "reduces risk" or "mitigates risk"

4. Verify cross-references between updated controls:
- Control 1.5 ↔ Control 1.6 (DLP ↔ DSPM)
- Control 1.6 ↔ Control 1.8 (DSPM ↔ Defender)
- Control 1.8 → Role catalog (Defender XDR Admin reference)
- Control 3.8 → Role catalog (AI Administrator reference)

5. Verify standardized table format consistency — grep for "Feature | Status | Description | Configuration" or "Connector | Status | Description | Configuration" across Controls 1.5, 1.6, 3.8 to confirm consistent column structure.

6. Verify preview admonition pattern consistency — grep for 'warning "Preview' across modified files to confirm consistent admonition format.
  </action>
  <verify>
`python3 -m mkdocs build --strict` exits with code 0.
`python3 scripts/verify_controls.py` reports 62/62 valid.
No prohibited regulatory phrases found in modified files.
  </verify>
  <done>
Full build passes, all 62 controls valid, no prohibited language, all cross-references resolve, standardized tables consistent, preview admonitions consistent.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate researcher package</name>
  <files></files>
  <action>
Run: `python3 scripts/compile_researcher_package.py`

This regenerates the researcher package with all Phase 4 content updates. The researcher package is used for context loading in future research tasks.

Verify the regenerated package includes:
- Updated Control 1.5 content (virtual connectors)
- Updated Control 1.6 content (DSPM weekly assessments)
- Updated Control 1.8 content (Defender verification)
- Updated Control 3.8 content (feature access control)
- Updated role catalog (AI Administrator, Defender XDR Admin)
  </action>
  <verify>
`python3 scripts/compile_researcher_package.py` completes without errors.
Output file exists and has been updated (check modification timestamp).
  </verify>
  <done>
Researcher package regenerated with all Phase 4 content. Package includes updated controls and role catalog.
  </done>
</task>

</tasks>

<verification>
1. `python3 -m mkdocs build --strict` passes
2. `python3 scripts/verify_controls.py` reports 62/62 valid
3. No prohibited regulatory language in modified files
4. Cross-references between Controls 1.5, 1.6, 1.8, 3.8 resolve correctly
5. Researcher package regenerated
6. Standardized table format consistent across updated controls
</verification>

<success_criteria>
- Build and structural validation both pass with zero errors
- No prohibited regulatory language introduced
- All cross-references between updated controls resolve
- Researcher package regenerated with Phase 4 content
</success_criteria>

<output>
After completion, create `.planning/phases/04-feature-enhancement-updates/04-05-SUMMARY.md`
</output>
