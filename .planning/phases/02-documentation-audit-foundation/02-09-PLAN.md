---
phase: 02-documentation-audit-foundation
plan: 09
type: execute
wave: 3
depends_on: ["02-05"]
files_modified:
  - docs/controls/pillar-4-sharepoint/*.md
  - docs/playbooks/control-implementations/4.*/*.md
autonomous: true

must_haves:
  truths:
    - "All Critical findings from AUDIT-PILLAR-4.md have been corrected"
    - "All Moderate findings from AUDIT-PILLAR-4.md have been corrected"
    - "All Minor findings from AUDIT-PILLAR-4.md have been corrected (unless excluded by user)"
    - "Each corrected control has a 'Last Verified' metadata field added"
    - "mkdocs build --strict passes after all corrections"
    - "Final build validation covers all 62 controls across all 4 pillars"
  artifacts:
    - path: "docs/controls/pillar-4-sharepoint/*.md"
      provides: "Corrected Pillar 4 control files"
      contains: "Last Verified"
  key_links:
    - from: "AUDIT-PILLAR-4.md"
      to: "docs/controls/pillar-4-sharepoint/*.md"
      via: "each finding maps to specific file and line range for correction"
      pattern: "Should Say"
---

<objective>
Apply all approved corrections from AUDIT-PILLAR-4.md to Pillar 4 controls and playbooks. Add "Last Verified" metadata field to each corrected control. Run final full-framework validation.

Purpose: Second pass of the two-pass audit methodology for Pillar 4. Also serves as the final validation checkpoint since it's the last fix plan, confirming all 62 controls pass build and structural validation.
Output: Corrected Pillar 4 control files and playbooks with "Last Verified" dates. Clean build across entire framework.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-documentation-audit-foundation/AUDIT-PILLAR-4.md
@.planning/phases/02-documentation-audit-foundation/02-05-SUMMARY.md
@docs/templates/control-setup-template.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply all corrections to Pillar 4 controls and playbooks</name>
  <files>docs/controls/pillar-4-sharepoint/*.md, docs/playbooks/control-implementations/4.*/*.md</files>
  <action>
Read AUDIT-PILLAR-4.md and 02-05-SUMMARY.md.

For each finding (Critical, Moderate, and Minor):
1. Read affected file(s)
2. Apply correction from "Should Say"
3. Add transition notes for renamed/removed capabilities
4. Apply formatting standardization

**Pillar 4 specific considerations:**
- Control 4.1 (SharePoint IAG): Verify Restricted Content Discovery terminology is current
- Control 4.6 (Grounding Scope): Critical for Copilot data security — corrections must be precise
- Control 4.7 (M365 Copilot Data Governance): Broad scope — verify corrections don't contradict other pillars
- SharePoint Advanced Management feature names must match current Microsoft terminology

**Do NOT apply:** User-excluded findings, new features, new controls/playbooks.
  </action>
  <verify>
All findings from AUDIT-PILLAR-4.md addressed. Grep confirms no prohibited language in corrected files.
  </verify>
  <done>All corrections applied to Pillar 4 controls and playbooks.</done>
</task>

<task type="auto">
  <name>Task 2: Add "Last Verified" metadata and run final full-framework validation</name>
  <files>docs/controls/pillar-4-sharepoint/*.md</files>
  <action>
**Step 1 — Add "Last Verified" metadata:**
Add `**Last Verified:** 2026-02-XX` to header metadata block of each Pillar 4 control, after "Governance Levels" line.

**Step 2 — Final full-framework validation:**
This is the last fix plan. Run comprehensive validation across ALL pillars:

1. `mkdocs build --strict` — must pass with zero errors/warnings
2. `python scripts/verify_controls.py` — must report all 62 controls valid
3. Spot-check: grep all 62 controls for "Last Verified" metadata field to confirm all pillars have it
4. Spot-check: grep for any remaining prohibited language across all pillars
5. Spot-check: grep for any remaining non-canonical role names across all pillars

**Step 3 — Generate researcher package:**
Run `python scripts/compile_researcher_package.py` to regenerate the researcher package with all corrections applied.

If any validation failures, fix them before completing.
  </action>
  <verify>
`mkdocs build --strict` passes. `verify_controls.py` reports all 62 controls valid. All 62 controls have "Last Verified" metadata. No prohibited language found. Researcher package regenerated.
  </verify>
  <done>"Last Verified" added to all 7 Pillar 4 controls, full-framework validation passes, researcher package regenerated.</done>
</task>

</tasks>

<verification>
1. All findings from AUDIT-PILLAR-4.md applied (except user-excluded items)
2. "Last Verified" date present in all 7 Pillar 4 control header metadata blocks
3. ALL 62 controls across all 4 pillars have "Last Verified" metadata
4. `mkdocs build --strict` passes for entire framework
5. `python scripts/verify_controls.py` passes for all 62 controls
6. Researcher package regenerated
7. No prohibited language remaining in any control
8. No non-canonical role names remaining in any control
</verification>

<success_criteria>
All 7 Pillar 4 controls corrected. Full framework validation passes: 62 controls with "Last Verified" metadata, clean build, no prohibited language, no non-canonical roles, researcher package current.
</success_criteria>

<output>
After completion, create `.planning/phases/02-documentation-audit-foundation/02-09-SUMMARY.md`
</output>
