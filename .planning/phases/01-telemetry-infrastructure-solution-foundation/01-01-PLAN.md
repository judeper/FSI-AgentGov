---
phase: 01-telemetry-infrastructure-solution-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/dev/FSI-AgentGov-Solutions/agent-observability-foundation/config/config.schema.json
  - C:/dev/FSI-AgentGov-Solutions/agent-observability-foundation/config/config.example.yml
  - C:/dev/FSI-AgentGov-Solutions/agent-observability-foundation/scripts/requirements.txt
  - C:/dev/FSI-AgentGov-Solutions/agent-observability-foundation/scripts/provision.py
autonomous: true

must_haves:
  truths:
    - "User can run provision.py --dry-run and see preview of all Azure resources that would be created"
    - "User can run provision.py with a config file and have Application Insights, Log Analytics, Storage Account, Diagnostic Settings, and RBAC assignments created"
    - "User can override any config value via CLI arguments (--resource-group, --location, --retention-days)"
    - "Provision script creates resources idempotently — rerunning does not fail or duplicate resources"
  artifacts:
    - path: "agent-observability-foundation/config/config.schema.json"
      provides: "JSON Schema for config validation"
      contains: "resource_group"
    - path: "agent-observability-foundation/config/config.example.yml"
      provides: "Example YAML configuration with annotated defaults"
      contains: "retention_days: 730"
    - path: "agent-observability-foundation/scripts/requirements.txt"
      provides: "Python dependency list for pip install"
      contains: "azure-mgmt-applicationinsights"
    - path: "agent-observability-foundation/scripts/provision.py"
      provides: "Main provisioning script for all Azure telemetry resources"
      contains: "create_or_update"
  key_links:
    - from: "scripts/provision.py"
      to: "config/config.example.yml"
      via: "YAML config loading with argparse overrides"
      pattern: "yaml.safe_load"
    - from: "scripts/provision.py"
      to: "Azure SDK clients"
      via: "DefaultAzureCredential authentication"
      pattern: "DefaultAzureCredential"
---

<objective>
Create the project scaffolding (config schema, example config, Python dependencies) and the main provisioning script that deploys all Azure telemetry infrastructure resources.

Purpose: This is the core automation that enables a user to deploy the entire FSI-compliant telemetry pipeline (Application Insights, Log Analytics, ADLS Gen2 storage, Diagnostic Settings, RBAC assignments) with a single command. It covers requirements TELE-01, TELE-02, TELE-03, TELE-04, and TELE-06.

Output: 4 files — config schema, example YAML config, requirements.txt, and provision.py
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-telemetry-infrastructure-solution-foundation/01-CONTEXT.md
@.planning/phases/01-telemetry-infrastructure-solution-foundation/01-RESEARCH.md

Reference existing solution patterns:
@C:/dev/FSI-AgentGov-Solutions/environment-lifecycle-management/scripts/deploy.py (banner, argparse, preflight patterns)
@C:/dev/FSI-AgentGov-Solutions/environment-lifecycle-management/scripts/requirements.txt (dependency format)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create config schema, example YAML, and requirements.txt</name>
  <files>
    C:/dev/FSI-AgentGov-Solutions/agent-observability-foundation/config/config.schema.json
    C:/dev/FSI-AgentGov-Solutions/agent-observability-foundation/config/config.example.yml
    C:/dev/FSI-AgentGov-Solutions/agent-observability-foundation/scripts/requirements.txt
  </files>
  <action>
    Create the directory structure under `C:/dev/FSI-AgentGov-Solutions/agent-observability-foundation/`.

    **config/config.schema.json** — JSON Schema (draft-07) validating config YAML:
    - Required fields: `subscription_id`, `resource_group`, `location`
    - Optional with defaults: `retention_days` (default 730), `naming_prefix` (default "aof"), `tags` (object)
    - Resource-specific sections: `application_insights` (name, kind), `log_analytics` (name, sku), `storage` (name, account_kind, replication), `diagnostic_settings` (name, log_categories array), `rbac` (array of {role_name, principal_id, principal_type})
    - Include `description` for each property explaining its purpose
    - Include `examples` where helpful

    **config/config.example.yml** — Annotated example with:
    - All fields populated with realistic but clearly-example values (e.g., subscription_id: "00000000-0000-0000-0000-000000000000")
    - Comments explaining each field (especially retention_days: 730 with note "# 2-year retention for SEC 17a-4(b)(4)")
    - Comments on storage account noting "# StorageV2 WITHOUT hierarchical namespace — required for diagnostic settings export"
    - RBAC section with two example assignments: one for Monitoring Reader (operational team), one for Storage Blob Data Reader (compliance team)
    - Tags section with `environment`, `solution`, `compliance-framework` keys

    **scripts/requirements.txt** — Pin major versions:
    ```
    azure-identity>=1.18.0
    azure-mgmt-applicationinsights>=4.1.0
    azure-mgmt-loganalytics>=13.0.0
    azure-mgmt-monitor>=6.0.0
    azure-mgmt-storage>=21.0.0
    azure-mgmt-authorization>=4.0.0
    azure-mgmt-resource>=23.0.0
    pyyaml>=6.0
    ```
    Include azure-mgmt-resource for resource group operations. Include a header comment: "# Agent Observability Foundation - Python Dependencies"
  </action>
  <verify>
    - `python -c "import yaml; yaml.safe_load(open('C:/dev/FSI-AgentGov-Solutions/agent-observability-foundation/config/config.example.yml'))"` succeeds
    - `python -c "import json; json.load(open('C:/dev/FSI-AgentGov-Solutions/agent-observability-foundation/config/config.schema.json'))"` succeeds
    - requirements.txt contains all 8 packages listed above
  </verify>
  <done>Config schema validates, example YAML parses, requirements.txt lists all Azure SDK dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Create provision.py with full Azure resource provisioning</name>
  <files>
    C:/dev/FSI-AgentGov-Solutions/agent-observability-foundation/scripts/provision.py
  </files>
  <action>
    Create the main provisioning script following the environment-lifecycle-management deploy.py patterns.

    **Script structure:**
    1. Shebang: `#!/usr/bin/env python3`
    2. Module docstring with usage examples (dry-run, full deploy, CLI overrides)
    3. Imports: azure SDK clients, yaml, argparse, sys, os, uuid
    4. `print_banner()` function with solution name and component list
    5. `load_config(args)` function: load YAML config, merge CLI overrides, validate required fields
    6. `preflight_check(config)` function: verify subscription access, resource group exists (create if not), print plan
    7. Resource creation functions (one per resource, idempotent create_or_update pattern):
       - `create_log_analytics_workspace(config, credential)` — PerGB2018 SKU, set BOTH `retention_in_days=730` AND `total_retention_in_days=730` (PITFALL #2 from research)
       - `create_application_insights(config, credential, workspace_id)` — workspace-based, kind="web", retention_in_days=730, link to Log Analytics workspace
       - `create_storage_account(config, credential)` — StorageV2 general-purpose v2, NO hierarchical namespace (PITFALL #1), Standard_GRS replication
       - `create_diagnostic_settings(config, credential, app_insights_id, storage_id)` — Export AppTraces + AppEvents to storage account
       - `create_rbac_assignments(config, credential)` — Process array from config, assign Monitoring Reader / Storage Blob Data Reader roles
    8. `main()` function orchestrating: banner → load_config → preflight → create resources in order → print summary
    9. Error handling: catch ClientAuthenticationError, HttpResponseError, AzureError with specific messages and exit codes
    10. `--dry-run` flag: print what would be created without making API calls
    11. `--verbose` flag: print detailed API responses

    **Critical implementation details:**
    - Use `DefaultAzureCredential()` for authentication (supports interactive + SP + managed identity)
    - argparse arguments: `--config` (default config/config.yml), `--resource-group`, `--location`, `--retention-days`, `--subscription-id`, `--dry-run`, `--verbose`
    - Print status symbols: ✓ for success, ○ for skipped (already exists), ✗ for failure
    - Log Analytics `begin_create_or_update()` returns a poller — call `.result()` to wait for completion
    - Storage account `begin_create()` also returns a poller
    - After all resources created, print summary table with resource names and IDs
    - DO NOT include any WORM policy creation code (LOCKED DECISION — too risky for accidental lockdown)
    - Set ingestion sampling at workspace level: include comment about configuring via portal (not SDK-configurable for Copilot Studio telemetry)
  </action>
  <verify>
    - `python -c "import ast; ast.parse(open('C:/dev/FSI-AgentGov-Solutions/agent-observability-foundation/scripts/provision.py').read())"` succeeds (valid Python syntax)
    - Script contains `DefaultAzureCredential` import
    - Script contains `--dry-run` argument
    - Script contains `begin_create_or_update` for Log Analytics (poller pattern)
    - Script does NOT contain "worm" or "immutab" (WORM excluded by design)
    - Script contains both `retention_in_days` and `total_retention_in_days` for Log Analytics
    - Script contains comment about hierarchical namespace being disabled for storage
  </verify>
  <done>provision.py is syntactically valid Python, uses Azure SDK with DefaultAzureCredential, supports --dry-run and --config, creates all 5 resource types (Log Analytics, App Insights, Storage, Diagnostic Settings, RBAC), handles errors with specific Azure exceptions, and explicitly excludes WORM policy creation.</done>
</task>

</tasks>

<verification>
1. All 4 files exist under `C:/dev/FSI-AgentGov-Solutions/agent-observability-foundation/`
2. `python -c "import ast; ast.parse(open('.../provision.py').read())"` — valid Python
3. `python -c "import yaml; yaml.safe_load(open('.../config.example.yml'))"` — valid YAML
4. `python -c "import json; json.load(open('.../config.schema.json'))"` — valid JSON
5. provision.py contains all 5 resource creation functions
6. No WORM/immutability automation in provision.py
7. Storage account creation specifies NO hierarchical namespace
8. Log Analytics uses BOTH retention settings (interactive + total = 730)
</verification>

<success_criteria>
- Requirements covered: TELE-01 (App Insights), TELE-02 (Log Analytics 730-day), TELE-03 (ADLS Gen2 export via diagnostic settings), TELE-04 (RBAC separation), TELE-06 (sampling defaults in config)
- Config file allows environment-specific customization
- provision.py follows established deploy.py patterns (banner, argparse, preflight, status symbols)
- Idempotent execution — safe to rerun
</success_criteria>

<output>
After completion, create `.planning/phases/01-telemetry-infrastructure-solution-foundation/01-01-SUMMARY.md`
</output>
