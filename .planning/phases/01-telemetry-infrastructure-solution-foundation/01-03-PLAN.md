---
phase: 01-telemetry-infrastructure-solution-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - /Users/admin/dev/FSI-AgentGov-Solutions/agent-observability-foundation/scripts/teardown.py
  - /Users/admin/dev/FSI-AgentGov-Solutions/agent-observability-foundation/scripts/verify_telemetry.py
  - /Users/admin/dev/FSI-AgentGov-Solutions/agent-observability-foundation/scripts/verify_worm.py
autonomous: true

must_haves:
  truths:
    - "User can run teardown.py to delete all provisioned resources for lab cycling"
    - "User can run verify_telemetry.py to confirm data is flowing from Copilot Studio to customEvents table"
    - "User can run verify_worm.py to confirm WORM policy is correctly applied on ADLS Gen2 container (read-only verification)"
    - "All scripts support --dry-run and --config flags matching provision.py patterns"
  artifacts:
    - path: "agent-observability-foundation/scripts/teardown.py"
      provides: "Resource cleanup script for lab cycling"
      contains: "begin_delete"
    - path: "agent-observability-foundation/scripts/verify_telemetry.py"
      provides: "Post-deployment telemetry flow validation"
      contains: "customEvents"
    - path: "agent-observability-foundation/scripts/verify_worm.py"
      provides: "WORM policy verification (read-only, never creates policies)"
      contains: "immutability_policy"
  key_links:
    - from: "scripts/teardown.py"
      to: "config/config.example.yml"
      via: "Same YAML config loading pattern as provision.py"
      pattern: "yaml.safe_load"
    - from: "scripts/verify_worm.py"
      to: "ADLS Gen2 Storage"
      via: "Read-only immutability policy check"
      pattern: "get_immutability_policy"
---

<objective>
Create the teardown script for lab cycling and two verification scripts for post-deployment validation (telemetry flow and WORM policy).

Purpose: The teardown script enables the user's lab workflow (deploy → test → destroy → repeat). The verification scripts confirm that telemetry is flowing correctly and that WORM policy (configured manually) meets SEC 17a-4 immutability requirements. Covers remaining aspects of TELE-01, TELE-02, TELE-03.

Output: 3 Python scripts in the scripts/ directory
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-telemetry-infrastructure-solution-foundation/01-CONTEXT.md
@.planning/phases/01-telemetry-infrastructure-solution-foundation/01-RESEARCH.md
@.planning/phases/01-telemetry-infrastructure-solution-foundation/01-01-SUMMARY.md

Reference Plan 01 outputs for consistent patterns:
@/Users/admin/dev/FSI-AgentGov-Solutions/agent-observability-foundation/scripts/provision.py (config loading, auth, error handling patterns)
@/Users/admin/dev/FSI-AgentGov-Solutions/agent-observability-foundation/config/config.example.yml (config structure)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create teardown.py for lab resource cleanup</name>
  <files>
    /Users/admin/dev/FSI-AgentGov-Solutions/agent-observability-foundation/scripts/teardown.py
  </files>
  <action>
    Create teardown.py that deletes all resources created by provision.py. This is the matching destroy script for lab cycling per LOCKED DECISION.

    **Script structure (follow provision.py patterns):**
    1. Shebang: `#!/usr/bin/env python3`
    2. Module docstring with usage examples
    3. `print_banner()` — "Agent Observability Foundation - Teardown"
    4. `load_config(args)` — Same config loading pattern as provision.py (reuse YAML + argparse overrides)
    5. argparse: `--config`, `--resource-group`, `--subscription-id`, `--dry-run`, `--verbose`, `--force` (skip confirmation prompt)
    6. **Safety confirmation prompt** — Before deletion, print list of resources that will be deleted and require user to type "yes" (unless --force). This is critical since teardown is destructive.
    7. Delete resources in REVERSE order of creation (dependencies first):
       - Delete diagnostic settings first (depends on App Insights + Storage)
       - Delete RBAC assignments
       - Delete Application Insights component
       - Delete Log Analytics workspace
       - Delete Storage Account (WARNING: if WORM policy is applied, deletion will fail — catch and inform user)
    8. Error handling: ResourceNotFoundError → print "already deleted" with ○ symbol, HttpResponseError → print error and continue to next resource
    9. Use `begin_delete()` for async operations (Log Analytics, Storage) and `.result()` to wait
    10. Print summary: resources deleted, resources already absent, resources failed

    **Critical details:**
    - NEVER delete resource groups (too dangerous — user may have other resources there)
    - Catch storage deletion failure due to WORM policy and print clear message: "Storage account has immutable policy. Delete manually via Azure portal after policy expires."
    - Print prominent warning: "This will permanently delete telemetry data. This action cannot be undone."
  </action>
  <verify>
    - `python -c "import ast; ast.parse(open('.../teardown.py').read())"` succeeds
    - Script contains safety confirmation prompt (unless --force)
    - Script deletes in reverse dependency order (diagnostic settings first)
    - Script catches storage WORM deletion failure with helpful message
    - Script does NOT delete resource groups
    - Script supports --dry-run flag
  </verify>
  <done>teardown.py safely deletes all provisioned resources in reverse dependency order with confirmation prompt, WORM-aware error handling, and --dry-run support.</done>
</task>

<task type="auto">
  <name>Task 2: Create verify_telemetry.py and verify_worm.py</name>
  <files>
    /Users/admin/dev/FSI-AgentGov-Solutions/agent-observability-foundation/scripts/verify_telemetry.py
    /Users/admin/dev/FSI-AgentGov-Solutions/agent-observability-foundation/scripts/verify_worm.py
  </files>
  <action>
    **verify_telemetry.py** — Validates that telemetry is flowing from Copilot Studio to Application Insights.

    Structure:
    1. Shebang + docstring with usage
    2. `print_banner()` — "Agent Observability Foundation - Telemetry Verification"
    3. argparse: `--config`, `--subscription-id`, `--resource-group`, `--app-insights-name`, `--hours` (default 24, lookback window)
    4. Use `azure-mgmt-applicationinsights` to get component details (verify exists, check retention, check workspace binding)
    5. Use `azure.monitor.query` LogsQueryClient to run a simple KQL query against the workspace:
       ```
       customEvents
       | where timestamp > ago({hours}h)
       | summarize EventCount=count(), DistinctSessions=dcount(session_Id) by name
       | order by EventCount desc
       ```
    6. Add `azure-monitor-query>=1.3.0` to the verification message if not in requirements.txt (or note it as optional dependency)
    7. Print results table: Event Name | Count | Distinct Sessions
    8. Verification checks with pass/fail:
       - ✓ Application Insights exists and is workspace-based
       - ✓ Retention is >= 730 days
       - ✓ customEvents table has data in last {hours} hours (or ○ "No data yet — ensure Copilot Studio agent is connected")
       - ✓ CopilotInteraction events detected (or ○ "No Copilot events — check Copilot Studio Application Insights connection")
    9. Exit code 0 if all checks pass, 1 if critical checks fail

    **verify_worm.py** — Validates WORM policy on ADLS Gen2 storage (READ-ONLY, NEVER creates policies).

    LOCKED DECISION: WORM verification in Phase 1, not deferred. Script verifies but NEVER applies WORM.

    Structure:
    1. Shebang + docstring with usage and prominent warning: "This script ONLY VERIFIES WORM policy. It does NOT create or modify immutability policies."
    2. `print_banner()` — "Agent Observability Foundation - WORM Policy Verification"
    3. argparse: `--config`, `--subscription-id`, `--resource-group`, `--storage-account`, `--container-name` (default: from config or "telemetry-export")
    4. Use `azure-mgmt-storage` BlobContainersOperations to read immutability policy:
       - `get_immutability_policy()` to check if policy exists
       - Check policy state: "Locked" (compliant), "Unlocked" (not yet compliant), or absent
    5. Verification checks:
       - ✓ Storage account exists and is StorageV2 (not hierarchical namespace)
       - ✓ Container exists
       - ✓ Immutability policy is present
       - ✓ Policy state is "Locked" (required for SEC 17a-4(f))
       - ✓ Retention period meets minimum (e.g., >= 2555 days for 7-year retention)
       - If policy absent: ○ "No WORM policy found. See docs/worm-configuration.md for manual setup steps."
    6. Print compliance summary: Compliant / Partially Compliant / Not Configured
    7. Exit code 0 if fully compliant, 1 if not configured, 2 if partially compliant (unlocked)

    **Critical: verify_worm.py must NEVER contain any create/set/lock immutability policy calls. Read-only verification ONLY.**

    **Update requirements.txt** to add `azure-monitor-query>=1.3.0` (needed for verify_telemetry.py LogsQueryClient).
  </action>
  <verify>
    - Both scripts pass Python syntax check: `python -c "import ast; ast.parse(...)"`
    - verify_telemetry.py contains KQL query with "customEvents"
    - verify_telemetry.py checks retention >= 730
    - verify_worm.py contains "get_immutability_policy" (read operation)
    - verify_worm.py does NOT contain "set_immutability_policy", "create_or_update_immutability_policy", or "lock_immutability_policy"
    - verify_worm.py docstring contains warning about read-only verification
    - Both scripts support --config and standard argparse patterns
    - requirements.txt updated with azure-monitor-query
  </verify>
  <done>verify_telemetry.py validates Application Insights exists with correct retention and queries customEvents for Copilot Studio data. verify_worm.py performs read-only WORM policy verification with compliance status reporting. Neither script modifies any resources.</done>
</task>

</tasks>

<verification>
1. All 3 scripts exist under `agent-observability-foundation/scripts/`
2. All 3 scripts pass Python syntax validation
3. teardown.py deletes resources in reverse order with confirmation
4. verify_telemetry.py queries customEvents table
5. verify_worm.py is strictly read-only (no create/set/lock operations)
6. All scripts follow provision.py patterns (banner, argparse, config loading)
7. requirements.txt includes azure-monitor-query for LogsQueryClient
</verification>

<success_criteria>
- Requirements reinforced: TELE-01 (verification), TELE-02 (retention check), TELE-03 (WORM verification)
- Lab cycling workflow complete: provision → verify → teardown → repeat
- WORM verification per LOCKED DECISION (Phase 1, not deferred)
- All scripts are strictly read-only verification or safe deletion (no WORM creation)
</success_criteria>

<output>
After completion, create `.planning/phases/01-telemetry-infrastructure-solution-foundation/01-03-SUMMARY.md`
</output>
