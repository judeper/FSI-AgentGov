---
phase: 5
plan: 2
title: "Config, Schema & Documentation"
wave: 1
dependencies: []
must_haves: [SCF-05, SCF-06]
worktree: B
---

# Plan 05-02: Config, Schema & Documentation

**Phase:** 5 — Script & Code Fixes
**Wave:** 1 (no dependencies — parallel with Plan 05-01)
**Worktree:** B
**Scope:** Fix cross-solution integration parameter mismatches and add audit log pagination warnings

## Requirements Covered

| Req | Must-Have | Status |
|-----|----------|--------|
| SCF-05 | Cross-solution integration parameter names aligned (`-SolutionId` not `-Solution`) | Planned |
| SCF-06 | Search-UnifiedAuditLog pagination warnings added | Planned |

## Tasks

### Task 1: Fix parameter names in Sync-SolutionAssessments.ps1 (SCF-05)

**Files:**
- `maintainers-local/solutions-staging/cross-solution-integration/scripts/powershell/Sync-SolutionAssessments.ps1`

**Problem:** Three call sites use `-Solution` but `IntegrationConfig.psm1` defines `-SolutionId`. Also, `Get-SolutionTableConfig` is called with a `-Solution` parameter it doesn't accept (it takes no parameters — returns full hashtable).

**Changes:**

1. **L417:** Change `Get-SolutionTableConfig -Solution $solution` to `(Get-SolutionTableConfig)[$solution]`
2. **L483:** Change `ConvertTo-DashboardStatus -Solution 'DEC'` to `ConvertTo-DashboardStatus -SolutionId 'DEC'`
3. **L578:** Change `ConvertTo-DashboardStatus -Solution $solution` to `ConvertTo-DashboardStatus -SolutionId $solution`

**Acceptance Criteria:**
- No instances of `-Solution ` (with space) remain as a parameter name in `Sync-SolutionAssessments.ps1`
- All calls to `ConvertTo-DashboardStatus` use `-SolutionId`
- `Get-SolutionTableConfig` is called without parameters and result is indexed by solution key

---

### Task 2: Add pagination warnings to purview-audit-query-pack.md (SCF-06)

**File:** `docs/playbooks/monitoring-and-validation/purview-audit-query-pack.md`

**Problem:** Three `Search-UnifiedAuditLog` calls use `-ResultSize 5000` (the maximum) with no pagination handling. In high-volume tenants, results silently truncate.

**Changes:**

Add a pagination warning admonition at the top of the Purview Audit Queries section (before the first `Search-UnifiedAuditLog` code block):

```markdown
!!! warning "Pagination Required for Large Tenants"
    `Search-UnifiedAuditLog` returns a maximum of 5,000 records per call.
    In high-volume FSI environments, queries may silently truncate results.
    Use session-based pagination for production evidence collection:

    ```powershell
    $sessionId = [guid]::NewGuid().ToString()
    $allResults = @()
    do {
        $batch = Search-UnifiedAuditLog -StartDate $startDate -EndDate $endDate `
            -RecordType <RecordType> -SessionId $sessionId `
            -SessionCommand ReturnLargeSet -ResultSize 5000
        $allResults += $batch
    } while ($batch.Count -eq 5000)
    ```

    See [Search-UnifiedAuditLog](https://learn.microsoft.com/en-us/powershell/module/exchange/search-unifiedauditlog) for details.
```

**Acceptance Criteria:**
- Pagination warning admonition present in the file
- Warning appears before the first `Search-UnifiedAuditLog` usage
- `mkdocs build --strict` passes

---

### Task 3: Add pagination warnings to 4.5 powershell-setup.md (SCF-06)

**File:** `docs/playbooks/control-implementations/4.5/powershell-setup.md`

**Problem:** Four `Search-UnifiedAuditLog` calls in the 4.5 SharePoint audit playbook use `-ResultSize 5000` with no pagination.

**Changes:**

Add the same pagination warning admonition near the top of the file, before the first `Search-UnifiedAuditLog` code block.

**Acceptance Criteria:**
- Pagination warning admonition present in the file
- `mkdocs build --strict` passes

---

### Task 4: Add pagination warnings to remaining high-priority playbooks (SCF-06)

**Files:**
- `docs/playbooks/monitoring-and-validation/semantic-index-governance-queries.md` (uses `-ResultSize 1000`)
- `docs/playbooks/control-implementations/3.2/powershell-setup.md` (uses `-ResultSize 5000`)
- `docs/playbooks/control-implementations/1.6/powershell-setup.md` (4 calls, various ResultSize)
- `docs/reference/agent-audit-event-taxonomy.md` (3 calls)

**Changes:**

Add a compact pagination warning admonition to each file, near the first `Search-UnifiedAuditLog` usage:

```markdown
!!! warning "Pagination"
    `Search-UnifiedAuditLog` returns a maximum of 5,000 records per call.
    Use `-SessionId` and `-SessionCommand ReturnLargeSet` for pagination in
    high-volume environments. See [Microsoft documentation](https://learn.microsoft.com/en-us/powershell/module/exchange/search-unifiedauditlog).
```

**Note:** Files with small ResultSize (10, 100) used for quick checks or tests do not need pagination warnings.

**Acceptance Criteria:**
- Pagination warnings added to all files using `Search-UnifiedAuditLog` with `-ResultSize ≥ 1000`
- `mkdocs build --strict` passes

---

## File Manifest

| Action | File | Task |
|--------|------|------|
| Modify | `maintainers-local/solutions-staging/cross-solution-integration/scripts/powershell/Sync-SolutionAssessments.ps1` | 1 |
| Modify | `docs/playbooks/monitoring-and-validation/purview-audit-query-pack.md` | 2 |
| Modify | `docs/playbooks/control-implementations/4.5/powershell-setup.md` | 3 |
| Modify | `docs/playbooks/monitoring-and-validation/semantic-index-governance-queries.md` | 4 |
| Modify | `docs/playbooks/control-implementations/3.2/powershell-setup.md` | 4 |
| Modify | `docs/playbooks/control-implementations/1.6/powershell-setup.md` | 4 |
| Modify | `docs/reference/agent-audit-event-taxonomy.md` | 4 |

## Validation

```bash
# Documentation build
mkdocs build --strict

# Grep checks — parameter alignment
grep -rn '\-Solution ' maintainers-local/solutions-staging/cross-solution-integration/  # expect: context-only, no parameter usage

# Grep checks — pagination warnings present
grep -rn 'Pagination' docs/playbooks/monitoring-and-validation/purview-audit-query-pack.md  # expect: 1+ match
grep -rn 'Pagination' docs/playbooks/control-implementations/4.5/powershell-setup.md  # expect: 1+ match
```
