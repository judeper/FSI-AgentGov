---
phase: 01-powershell-tech-debt
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - segregation-detector/scripts/Invoke-SoDScan.ps1
  - segregation-detector/scripts/Import-ConflictRules.ps1
  - scope-drift-monitor/scripts/New-AgentBaseline.ps1
  - rag-source-validator/scripts/Invoke-SourceValidation.ps1
  - dr-testing-framework/scripts/Invoke-DRTest.ps1
  - finra-supervision-workflow/scripts/requirements.txt
autonomous: true

must_haves:
  truths:
    - "All 5 remaining PowerShell scripts have #Requires -Version 7.0"
    - "FINRA requirements.txt contains only stdlib documentation comments (all 6 third-party dependencies removed)"
    - "ELM requirements.txt is unchanged (all dependencies are used)"
    - "No existing script logic or functionality is altered"
  artifacts:
    - path: "segregation-detector/scripts/Invoke-SoDScan.ps1"
      provides: "Version declaration"
      contains: "#Requires -Version 7.0"
    - path: "segregation-detector/scripts/Import-ConflictRules.ps1"
      provides: "Version declaration"
      contains: "#Requires -Version 7.0"
    - path: "scope-drift-monitor/scripts/New-AgentBaseline.ps1"
      provides: "Version declaration"
      contains: "#Requires -Version 7.0"
    - path: "rag-source-validator/scripts/Invoke-SourceValidation.ps1"
      provides: "Version declaration"
      contains: "#Requires -Version 7.0"
    - path: "dr-testing-framework/scripts/Invoke-DRTest.ps1"
      provides: "Version declaration"
      contains: "#Requires -Version 7.0"
    - path: "finra-supervision-workflow/scripts/requirements.txt"
      provides: "Clean dependency file with no unused packages"
  key_links: []
---

<objective>
Add #Requires statements to the remaining 5 PowerShell scripts and clean up unused Python dependencies.

Purpose: Completes DEBT-03 (#Requires coverage for all 11 scripts) and DEBT-04 (dependency cleanup). These 5 scripts all use direct REST API calls via `Invoke-RestMethod` rather than PowerShell modules, so they only need version declarations. The FINRA requirements.txt has 6 unused dependencies that inflate container images.
Output: 5 scripts with #Requires -Version 7.0, and a cleaned FINRA requirements.txt.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-powershell-tech-debt/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add #Requires to segregation-detector, scope-drift, rag-source, and dr-testing scripts</name>
  <files>
/Users/admin/dev/FSI-AgentGov-Solutions/segregation-detector/scripts/Invoke-SoDScan.ps1
/Users/admin/dev/FSI-AgentGov-Solutions/segregation-detector/scripts/Import-ConflictRules.ps1
/Users/admin/dev/FSI-AgentGov-Solutions/scope-drift-monitor/scripts/New-AgentBaseline.ps1
/Users/admin/dev/FSI-AgentGov-Solutions/rag-source-validator/scripts/Invoke-SourceValidation.ps1
/Users/admin/dev/FSI-AgentGov-Solutions/dr-testing-framework/scripts/Invoke-DRTest.ps1
  </files>
  <action>
Add `#Requires -Version 7.0` to each script immediately after the closing `)` of the param block.

**Critical: These 5 scripts all use direct REST API calls** (`Invoke-RestMethod`) for Microsoft Graph and Dataverse access. They do NOT import or use any PowerShell modules. Therefore, add ONLY `#Requires -Version 7.0` — do NOT add `#Requires -Modules` lines.

**Invoke-SoDScan.ps1** — after line 50 (the `)` closing param), add:
```powershell

#Requires -Version 7.0
```

**Import-ConflictRules.ps1** — after line 45 (the `)` closing param), add:
```powershell

#Requires -Version 7.0
```

**New-AgentBaseline.ps1** — after line 41 (the `)` closing param), add:
```powershell

#Requires -Version 7.0
```

**Invoke-SourceValidation.ps1** — after line 35 (the `)` closing param), add:
```powershell

#Requires -Version 7.0
```

**Invoke-DRTest.ps1** — after line 45 (the `)` closing param), add:
```powershell

#Requires -Version 7.0
```

Do NOT modify any other lines. Only insert the #Requires statement (plus blank line before it) after each param block.
  </action>
  <verify>
```bash
for f in \
  /Users/admin/dev/FSI-AgentGov-Solutions/segregation-detector/scripts/Invoke-SoDScan.ps1 \
  /Users/admin/dev/FSI-AgentGov-Solutions/segregation-detector/scripts/Import-ConflictRules.ps1 \
  /Users/admin/dev/FSI-AgentGov-Solutions/scope-drift-monitor/scripts/New-AgentBaseline.ps1 \
  /Users/admin/dev/FSI-AgentGov-Solutions/rag-source-validator/scripts/Invoke-SourceValidation.ps1 \
  /Users/admin/dev/FSI-AgentGov-Solutions/dr-testing-framework/scripts/Invoke-DRTest.ps1; do
  echo "$(basename $f): $(grep -c '#Requires -Version 7.0' "$f")"
done
```
Expected: Each file shows count of 1.
  </verify>
  <done>All 5 PowerShell scripts have #Requires -Version 7.0 declarations. No #Requires -Modules added because these scripts use Invoke-RestMethod directly.</done>
</task>

<task type="auto">
  <name>Task 2: Remove unused dependencies from FINRA requirements.txt</name>
  <files>/Users/admin/dev/FSI-AgentGov-Solutions/finra-supervision-workflow/scripts/requirements.txt</files>
  <action>
Replace the entire contents of `finra-supervision-workflow/scripts/requirements.txt` with only stdlib documentation comments. The research confirmed that the 2 Python scripts in this solution (`scripts/` directory) use ONLY stdlib modules: argparse, hashlib, json, os, sys, datetime. None of the 6 listed third-party packages (msal, azure-identity, requests, pandas, tabulate, python-dotenv) are imported.

New file contents:
```
# FINRA Supervision Workflow - Python Dependencies
#
# This solution uses only Python standard library modules:
# - argparse (CLI argument parsing)
# - hashlib (hash generation)
# - json (JSON handling)
# - os (file operations)
# - sys (system operations)
# - datetime (date/time handling)
#
# No third-party dependencies required.
```

Do NOT modify `environment-lifecycle-management/scripts/requirements.txt` — all 4 of its dependencies (msal, requests, azure-identity, azure-keyvault-secrets) are actively used.
  </action>
  <verify>
```bash
grep -c "^[a-z]" /Users/admin/dev/FSI-AgentGov-Solutions/finra-supervision-workflow/scripts/requirements.txt
```
Expected: 0 (no non-comment package lines)

Verify ELM requirements.txt is unchanged:
```bash
grep -c "^[a-z]" /Users/admin/dev/FSI-AgentGov-Solutions/environment-lifecycle-management/scripts/requirements.txt
```
Expected: 4
  </verify>
  <done>FINRA requirements.txt contains only stdlib documentation comments. ELM requirements.txt is unchanged with its 4 used dependencies.</done>
</task>

</tasks>

<verification>
1. All 5 PowerShell scripts have `#Requires -Version 7.0`
2. None of the 5 scripts have `#Requires -Modules` (correct — they use Invoke-RestMethod)
3. FINRA requirements.txt has zero third-party package lines
4. ELM requirements.txt still has 4 package lines
5. No functional changes to any script
</verification>

<success_criteria>
- DEBT-03 complete: All 11 scripts (combined with Plan 02) now have #Requires statements
- DEBT-04 complete: FINRA requirements.txt cleaned, ELM preserved
- Zero functional changes to any script
</success_criteria>

<output>
After completion, create `.planning/phases/01-powershell-tech-debt/01-03-SUMMARY.md`
</output>
