---
phase: 01-powershell-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deny-event-correlation-report/scripts/Export-CopilotDenyEvents.ps1
  - deny-event-correlation-report/scripts/Export-DlpCopilotEvents.ps1
  - deny-event-correlation-report/scripts/Invoke-DailyDenyReport.ps1
  - deny-event-correlation-report/scripts/Export-RaiTelemetry.ps1
  - pipeline-governance-cleanup/src/Get-PipelineInventory.ps1
  - pipeline-governance-cleanup/src/Send-OwnerNotifications.ps1
autonomous: true

must_haves:
  truths:
    - "All 4 deny-event-correlation scripts have #Requires -Version 7.0 and appropriate -Modules statements"
    - "Both pipeline-governance-cleanup scripts have #Requires -Version 7.0 and appropriate -Modules statements"
    - "No existing script logic or functionality is altered"
  artifacts:
    - path: "deny-event-correlation-report/scripts/Export-CopilotDenyEvents.ps1"
      provides: "Module dependency declaration for ExchangeOnlineManagement"
      contains: "#Requires -Modules ExchangeOnlineManagement"
    - path: "deny-event-correlation-report/scripts/Export-DlpCopilotEvents.ps1"
      provides: "Module dependency declaration for ExchangeOnlineManagement"
      contains: "#Requires -Modules ExchangeOnlineManagement"
    - path: "deny-event-correlation-report/scripts/Invoke-DailyDenyReport.ps1"
      provides: "Module dependency declaration for ExchangeOnlineManagement, Az.Storage, Az.KeyVault"
      contains: "#Requires -Modules ExchangeOnlineManagement"
    - path: "deny-event-correlation-report/scripts/Export-RaiTelemetry.ps1"
      provides: "Module dependency declaration for Az.Accounts"
      contains: "#Requires -Modules Az.Accounts"
    - path: "pipeline-governance-cleanup/src/Get-PipelineInventory.ps1"
      provides: "Version declaration"
      contains: "#Requires -Version 7.0"
    - path: "pipeline-governance-cleanup/src/Send-OwnerNotifications.ps1"
      provides: "Module dependency declaration for Microsoft.Graph.Users.Actions"
      contains: "#Requires -Modules Microsoft.Graph.Users.Actions"
  key_links: []
---

<objective>
Add #Requires statements to 6 PowerShell scripts across 2 solutions (deny-event-correlation-report and pipeline-governance-cleanup).

Purpose: Pre-execution validation prevents scripts from starting and then failing mid-run when modules are missing, which can leave systems in inconsistent states. This is DEBT-03 (partial).
Output: 6 scripts with proper #Requires -Version and #Requires -Modules declarations.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-powershell-tech-debt/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add #Requires to deny-event-correlation-report scripts</name>
  <files>
/Users/admin/dev/FSI-AgentGov-Solutions/deny-event-correlation-report/scripts/Export-CopilotDenyEvents.ps1
/Users/admin/dev/FSI-AgentGov-Solutions/deny-event-correlation-report/scripts/Export-DlpCopilotEvents.ps1
/Users/admin/dev/FSI-AgentGov-Solutions/deny-event-correlation-report/scripts/Invoke-DailyDenyReport.ps1
/Users/admin/dev/FSI-AgentGov-Solutions/deny-event-correlation-report/scripts/Export-RaiTelemetry.ps1
  </files>
  <action>
Add `#Requires` statements to each script immediately after the closing `)` of the param block. Place them on the line following the param block closing parenthesis.

**Export-CopilotDenyEvents.ps1** — after line 54 (the `)`), add:
```powershell

#Requires -Version 7.0
#Requires -Modules ExchangeOnlineManagement
```
Module rationale: Uses `Connect-ExchangeOnline` and `Search-UnifiedAuditLog` from ExchangeOnlineManagement.

**Export-DlpCopilotEvents.ps1** — after line 57 (the `)`), add:
```powershell

#Requires -Version 7.0
#Requires -Modules ExchangeOnlineManagement
```
Module rationale: Uses `Connect-ExchangeOnline` and `Search-UnifiedAuditLog` from ExchangeOnlineManagement.

**Invoke-DailyDenyReport.ps1** — after line 101 (the `)`), add:
```powershell

#Requires -Version 7.0
#Requires -Modules ExchangeOnlineManagement, Az.Storage, Az.KeyVault
```
Module rationale: Orchestrates Exchange Online scripts (ExchangeOnlineManagement), uploads to blob storage (Az.Storage), retrieves secrets from Key Vault (Az.KeyVault via Get-AzKeyVaultSecret).

**Export-RaiTelemetry.ps1** — after line 77 (the `)`), add:
```powershell

#Requires -Version 7.0
#Requires -Modules Az.Accounts
```
Module rationale: Uses `Get-AzContext`, `Get-AzAccessToken`, `Connect-AzAccount` from Az.Accounts.

Do NOT modify any other lines in these scripts. Only insert the #Requires statements.
  </action>
  <verify>
For each script, verify #Requires statements exist:
```bash
grep -l "#Requires -Version 7.0" /Users/admin/dev/FSI-AgentGov-Solutions/deny-event-correlation-report/scripts/*.ps1 | wc -l
```
Expected: 4

```bash
grep "#Requires -Modules" /Users/admin/dev/FSI-AgentGov-Solutions/deny-event-correlation-report/scripts/*.ps1
```
Expected: 4 lines showing correct modules for each script.
  </verify>
  <done>All 4 deny-event-correlation-report scripts have #Requires -Version 7.0 and #Requires -Modules with the correct module dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Add #Requires to pipeline-governance-cleanup scripts</name>
  <files>
/Users/admin/dev/FSI-AgentGov-Solutions/pipeline-governance-cleanup/src/Get-PipelineInventory.ps1
/Users/admin/dev/FSI-AgentGov-Solutions/pipeline-governance-cleanup/src/Send-OwnerNotifications.ps1
  </files>
  <action>
Add `#Requires` statements to each script immediately after the closing `)` of the param block.

**Get-PipelineInventory.ps1** — after line 67 (the `)`), add:
```powershell

#Requires -Version 7.0
```
Module rationale: This script uses `pac` CLI (Power Platform CLI) which is an external tool, not a PowerShell module. It does NOT use any PowerShell modules. Only add the version requirement. Do NOT add a #Requires -Modules line.

**Send-OwnerNotifications.ps1** — after line 78 (the `)`), add:
```powershell

#Requires -Version 7.0
#Requires -Modules Microsoft.Graph.Users.Actions
```
Module rationale: Uses `Send-MgUserMail` from Microsoft.Graph.Users.Actions, and `Connect-MgGraph`/`Disconnect-MgGraph` from Microsoft.Graph.Authentication (auto-loaded as dependency of Users.Actions).

Do NOT modify any other lines in these scripts.
  </action>
  <verify>
```bash
grep "#Requires" /Users/admin/dev/FSI-AgentGov-Solutions/pipeline-governance-cleanup/src/Get-PipelineInventory.ps1
```
Expected: `#Requires -Version 7.0`

```bash
grep "#Requires" /Users/admin/dev/FSI-AgentGov-Solutions/pipeline-governance-cleanup/src/Send-OwnerNotifications.ps1
```
Expected: `#Requires -Version 7.0` and `#Requires -Modules Microsoft.Graph.Users.Actions`
  </verify>
  <done>Both pipeline-governance-cleanup scripts have #Requires -Version 7.0. Send-OwnerNotifications.ps1 additionally has #Requires -Modules Microsoft.Graph.Users.Actions.</done>
</task>

</tasks>

<verification>
1. All 6 scripts have `#Requires -Version 7.0`
2. Module-specific #Requires match actual cmdlet usage in each script
3. No existing script logic was modified (only insertions after param blocks)
4. Line count of each script increased by exactly 2-3 lines (the #Requires statements plus blank line)
</verification>

<success_criteria>
- 6 of 11 DEBT-03 scripts now have #Requires statements
- Correct module mappings: ExchangeOnlineManagement for Unified Audit Log scripts, Az.Accounts for RAI telemetry, Az.Storage + Az.KeyVault for orchestrator, Microsoft.Graph.Users.Actions for notification script
- Zero functional changes to any script
</success_criteria>

<output>
After completion, create `.planning/phases/01-powershell-tech-debt/01-02-SUMMARY.md`
</output>
