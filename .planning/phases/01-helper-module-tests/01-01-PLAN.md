---
phase: 1
plan: 1
title: "Helper module (.psm1) and manifest (.psd1)"
wave: 1
dependencies: []
must_haves:
  - "AuditComplianceHelpers.psm1 with 6 exported functions"
  - "AuditComplianceHelpers.psd1 module manifest v1.0.0"
  - "PowerShell 7.2 compatibility"
  - "Managed Identity auth only — NEVER interactive or hardcoded credentials"
---

# Phase 1 Plan A: Helper Module + Manifest

## Goal

Create the shared PowerShell helper module used by both ALCA detection and remediation runbooks, with proper module manifest for Azure Automation import.

## Tasks

### Task 1: Create AuditComplianceHelpers.psm1

**Acceptance criteria:**
- Exports 6 functions: `Invoke-WithRetry`, `Get-ManagedIdentityToken`, `Get-DataverseToken`, `Invoke-DataverseRequest`, `Write-DataverseComplianceRecord`, `Send-ComplianceNotification`
- `Invoke-WithRetry`: Exponential backoff with jitter for 429/503/504, configurable MaxRetries/InitialDelaySeconds/MaxDelaySeconds
- `Get-ManagedIdentityToken`: Azure Automation MI token via IDENTITY_ENDPOINT/IDENTITY_HEADER
- `Get-DataverseToken`: Dataverse-specific token with URL normalization (trailing slash handling)
- `Invoke-DataverseRequest`: Web API wrapper with OData headers, retry logic, GET/POST/PATCH/DELETE/PUT
- `Write-DataverseComplianceRecord`: Upsert by fsi_environmentid with option set mapping (Compliant=100000000, Non-Compliant=100000001, Remediation Pending=100000002, Error=100000003)
- `Send-ComplianceNotification`: Graph sendMail via shared mailbox with base64 attachment support

### Task 2: Create AuditComplianceHelpers.psd1

**Acceptance criteria:**
- ModuleVersion = 1.0.0
- PowerShellVersion = 7.2
- FunctionsToExport lists all 6 functions
- Proper GUID, description, tags
- Passes `Test-ModuleManifest`

### Task 3: Create solution scaffolding

**Acceptance criteria:**
- `audit-logging-compliance-automation/README.md` with solution overview
- `audit-logging-compliance-automation/CHANGELOG.md` with v1.0.0 entry
- Folder structure: `src/`, `docs/`

## File Manifest

| Action | File |
|--------|------|
| Create | `audit-logging-compliance-automation/README.md` |
| Create | `audit-logging-compliance-automation/CHANGELOG.md` |
| Create | `audit-logging-compliance-automation/src/AuditComplianceHelpers.psm1` |
| Create | `audit-logging-compliance-automation/src/AuditComplianceHelpers.psd1` |

## Verification

- [ ] `Import-Module .\AuditComplianceHelpers.psm1 -Force` succeeds
- [ ] `Get-Command -Module AuditComplianceHelpers` shows 6 functions
- [ ] `Test-ModuleManifest .\AuditComplianceHelpers.psd1` succeeds
- [ ] No interactive auth or hardcoded credentials anywhere in module
