---
phase: 1
plan: 3
wave: 1
dependencies: [01-01, 01-02]
---

# Plan 01-03: Orchestrator with Dry-Run Mode and Multi-Format Output

## Objective

Build Test-FileUploadCompliance.ps1 as the end-to-end orchestrator that combines enumeration, comparison, and reporting with dry-run mode and multiple output formats (Table, JSON, CSV).

## Requirements Covered

- **FUS-04**: Orchestrator with dry-run and multi-format output

## Actions

1. Build Test-FileUploadCompliance.ps1:
   - Parameter set: EnvironmentFilter, OutputFormat, DryRun, IncludeCompliant, BaselinePath
   - Orchestrate: Get-AgentFileUploadSettings → Compare-FileUploadCompliance → Format output
   - Dry-run mode: show what would be validated without querying Dataverse
   - Output formats: Table (console), JSON (file), CSV (file)
   - Summary statistics: total agents, compliant, violations by severity

2. Implement reporting:
   - Console table with color-coded severity
   - JSON output with full agent details and compliance status
   - CSV export for spreadsheet analysis
   - Summary banner with pass/fail/warning counts

## Success Criteria

- [ ] Test-FileUploadCompliance.ps1 orchestrates full validation pipeline
- [ ] Dry-run mode works without Dataverse queries
- [ ] Table, JSON, CSV output formats all functional
- [ ] Summary statistics accurate
