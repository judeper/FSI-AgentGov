---
phase: 07-solutions-functional-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/07-solutions-functional-testing/07-python-validation-results.md
autonomous: true

must_haves:
  truths:
    - "All 16 Python scripts across 5 solutions pass AST syntax validation"
    - "All Python import statements verified against requirements.txt for 4 solutions with requirements files"
    - "Deprecated API patterns detected and cataloged across all Python scripts"
    - "Error handling patterns verified in all Python scripts"
  artifacts:
    - path: ".planning/phases/07-solutions-functional-testing/07-python-validation-results.md"
      provides: "Python solution validation findings"
      contains: "ELM|FINRA|Compliance Dashboard|COI|Hallucination"
  key_links:
    - from: "Python validation results"
      to: "07-03 aggregation plan"
      via: "Results consumed by Plan 07-03 for final report"
      pattern: "07-python-validation-results"
---

<objective>
Validate all Python-based solutions (5 solutions, 16 scripts, 4 requirements.txt files) using AST syntax parsing, import-requirements alignment, API deprecation scanning, and error handling pattern verification.

Purpose: Python solutions include the most mature implementations (ELM with 11 scripts). Validating syntax, dependencies, and patterns ensures users can deploy these solutions without encountering import errors, missing dependencies, or deprecated API usage.

Output: `07-python-validation-results.md` with per-solution findings categorized by severity.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-solutions-functional-testing/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Python AST Syntax Validation + Import-Requirements Alignment</name>
  <files>
    .planning/phases/07-solutions-functional-testing/07-python-validation-results.md
  </files>
  <action>
Validate all 16 Python scripts across 5 solutions using Python AST parsing and import analysis. Work directly in Python (no external tools needed — Python 3.9.6 available, pwsh is NOT available on this machine).

**Solutions to validate (Python-based):**

1. **environment-lifecycle-management** (11 scripts): `/Users/admin/dev/FSI-AgentGov-Solutions/environment-lifecycle-management/scripts/`
   - create_field_security.py, create_business_rules.py, deploy.py, export_quarterly_evidence.py, register_service_principal.py, validate_immutability.py, create_dataverse_schema.py, create_views.py, elm_client.py, verify_role_privileges.py, create_security_roles.py
   - requirements.txt exists

2. **finra-supervision-workflow** (2 scripts): `/Users/admin/dev/FSI-AgentGov-Solutions/finra-supervision-workflow/scripts/`
   - deploy.py, export_supervision_evidence.py
   - requirements.txt exists

3. **compliance-dashboard** (1 script): `/Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/scripts/`
   - load_sample_data.py
   - requirements.txt exists

4. **coi-testing** (1 script): `/Users/admin/dev/FSI-AgentGov-Solutions/coi-testing/scripts/`
   - run_coi_tests.py
   - requirements.txt exists

5. **hallucination-tracker** (1 script): `/Users/admin/dev/FSI-AgentGov-Solutions/hallucination-tracker/scripts/`
   - analyze_patterns.py
   - NO requirements.txt (document as finding)

**For each Python script:**
1. Parse with `ast.parse()` — report any SyntaxError with line number and message
2. Extract all `import X` and `from X import Y` statements using AST walking
3. Classify imports as stdlib vs third-party (use known stdlib module list for Python 3.9)
4. Compare third-party imports against solution's requirements.txt
5. Flag any third-party imports NOT in requirements.txt as MISSING DEPENDENCY
6. Flag any requirements.txt entries not imported by any script as UNUSED DEPENDENCY
7. Check for local imports (e.g., `from elm_client import`) — verify referenced module exists in same directory

**For each solution, also check:**
- Deprecated API patterns: scan for `x-api-key`, `outlook.office365.com/EWS`, `/_api/web/`, hardcoded credentials (`password=`, `secret=` in string literals)
- Error handling: count try/except blocks, flag scripts with zero error handling
- Entry point: verify script has `if __name__ == "__main__":` or is clearly a library module

**Output format in 07-python-validation-results.md:**
```
# Python Solution Validation Results

## Summary
| Solution | Scripts | Syntax | Dependencies | Deprecations | Error Handling |
|----------|---------|--------|--------------|--------------|----------------|

## Per-Solution Details
### environment-lifecycle-management
[Detailed findings per script]
...
```

Categorize findings as: CRITICAL (syntax errors, missing dependencies), HIGH (deprecated APIs, no error handling), MEDIUM (unused dependencies, missing entry points), LOW (style issues).
  </action>
  <verify>
All 16 Python files parsed without Claude errors. 07-python-validation-results.md exists with findings table and per-solution sections for all 5 Python solutions.
  </verify>
  <done>
All 16 Python scripts across 5 solutions have AST validation results. Import-requirements alignment checked for all 4 solutions with requirements.txt. Hallucination-tracker flagged for missing requirements.txt. All findings categorized by severity.
  </done>
</task>

</tasks>

<verification>
- 07-python-validation-results.md exists in `.planning/phases/07-solutions-functional-testing/`
- Results cover all 5 Python-based solutions (ELM, FINRA, Compliance Dashboard, COI, Hallucination)
- All 16 Python scripts have individual validation results
- Import-requirements alignment checked for all 4 solutions with requirements.txt
- Findings categorized by severity (CRITICAL, HIGH, MEDIUM, LOW)
</verification>

<success_criteria>
- Every Python script has a PASS/FAIL syntax result
- Every third-party import mapped to requirements.txt entry (or flagged as missing)
- Deprecated API patterns scanned and results documented
- Error handling patterns assessed per script
- Results structured for consumption by Plan 07-03
</success_criteria>

<output>
After completion, create `.planning/phases/07-solutions-functional-testing/07-01-SUMMARY.md`
</output>
