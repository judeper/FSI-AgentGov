---
phase: 2
plan: 2
title: "Operational Health tab + template finalization + zone-aware thresholds"
wave: 2
dependencies: ["02-01-PLAN.md"]
must_haves:
  - "Operational Health tab with 7 visualizations (Q17-Q23)"
  - "Zone-aware threshold conditional formatting on KPI tiles across all 3 tabs"
  - "Complete deployable JSON template with all 3 tabs finalized"
  - "Template validation (JSON valid, all queries present, parameter references resolved)"
---

# Plan 02-02: Operational Health Tab + Template Finalization

**Phase:** 2 — Workbook Template Development
**Requirements:** WBK-03, WBK-04
**Wave:** 2 (depends on Plan 02-01 — same file)

## Goal

Complete the Azure Monitor Workbook JSON template by adding the Operational Health tab (7 visualizations from Q17-Q23), applying zone-aware threshold conditional formatting across all 3 tabs, and finalizing the template for deployment readiness. The output is a single complete `src/agent-usage-workbook.json` ready for ARM template import or manual Azure portal deployment.

## Tasks

### Task 1: Build Operational Health Tab (Q17-Q23)

**Acceptance Criteria:**
- [x] 7 visualization items (type 3) + markdown items (type 1) embedded within Tab 3 group:
  - Q17: Session Volume Anomaly Detection — `visualization: "timechart"` with `anomalycolumns` render option
  - Q18: Availability (Uptime Trend) — `visualization: "timechart"`, size 0
  - Q19: Exception Spike Detection — `visualization: "timechart"` with `anomalycolumns` render option
  - Q20: Generative Answers Quality — `visualization: "tiles"`, size 4 (dual KPI: Gen AI rate + fallback rate)
  - Q21: Dependency Health — `visualization: "table"`, size 1
  - Q22: DLP/Security Events — `visualization: "tiles"` + `visualization: "table"` (2 items: KPI tile + detail table)
  - Q23: Agent Health Summary — `visualization: "table"` (composite per-agent grid), size 0
- [x] Each KQL query matches Phase 1 KQL query library exactly (Q17-Q23 from `kql-query-library.md`)
- [x] Q17, Q19 (Anomaly Detection) use fixed 14-day lookback (no TimeRange parameter — intentional for stable baseline)
- [x] Q22 (DLP Events) has two visualization items (KPI tile + detail table) matching dual-query design
- [x] Q22 includes markdown caveat (type 1) noting custom telemetry dependency
- [x] Q18 includes markdown caveat (type 1) noting proxy metric limitation
- [x] Q21 does not apply AgentFilter/ChannelFilter (dependencies table limitation)
- [x] Each item has `title`, `resourceType: "microsoft.insights/components"`, `queryType: 0`

### Task 2: Apply Zone-Aware Threshold Formatting

**Acceptance Criteria:**
- [x] KPI tile items across all tabs include `conditionalSettings` (or `tileSettings` with `thresholds`) for zone-aware coloring:
  - Q06 (Resolution Rate): Green ≥80%, Yellow ≥60%, Red <60%
  - Q08 (Business Value): Neutral formatting (no threshold — informational)
  - Q12 (Connector Success Rate): Green ≥95%, Yellow ≥90%, Red <90%
  - Q14 (Topic Completion Rate): Green ≥80%, Yellow ≥60%, Red <60%
  - Q20 (Gen Answers Quality): Green fallback <15%, Yellow <25%, Red ≥25%
  - Q21 (Dependency Health): Green success ≥95%, Yellow ≥90%, Red <90%
  - Q23 (Agent Health Summary): Conditional row coloring based on completion rate
- [x] Threshold values aligned with Zone 3 (enterprise) defaults from KQL Library Appendix A
- [x] Markdown note (type 1) at workbook top documenting zone threshold customization guidance
- [x] Threshold comments in JSON reference which zone level they correspond to

### Task 3: Finalize Template Metadata

**Acceptance Criteria:**
- [x] Workbook header markdown (type 1) includes:
  - Title: "Agent Usage & Performance Workbook"
  - Version: "v1.0.0"
  - Framework reference: "FSI Agent Governance Framework v1.2.38"
  - Description: Solves ALM separation-of-duties gap (Control 2.8)
  - 3-tab overview table
  - Prerequisites summary (Application Insights, Copilot Studio telemetry enabled)
  - Zone threshold customization note
- [x] `$schema` property set correctly
- [x] `version: "Notebook/1.0"` at root
- [x] `fallbackResourceIds` contains parameterized placeholder
- [x] `isLocked: false`

### Task 4: Full Template Validation

**Acceptance Criteria:**
- [x] JSON is valid (parseable by any JSON parser — test with `python -c "import json; json.load(open('src/agent-usage-workbook.json'))"`)
- [x] Total visualization items:
  - Tab 1: 8 query items + 0-1 markdown items
  - Tab 2: 9 query items (Q15 dual) + 1 markdown item (latency caveat)
  - Tab 3: 9 query items (Q22 dual) + 2-3 markdown items (limitation caveats)
  - Total: ≥26 query items + ≥3 markdown items
- [x] All 23 KQL queries from query library are present in the workbook
- [x] All 5 global parameters are declared (P01-P05)
- [x] No `traces` table references anywhere in the file
- [x] No hardcoded Application Insights resource IDs (only placeholder)
- [x] All `{ParameterName}` references resolve to declared parameters
- [x] Zone-aware threshold formatting applied to applicable KPI tiles
- [x] Regulatory comments preserved in KQL query strings
- [x] `designMode == "False"` filtering present in all customEvents queries
- [x] File size is reasonable (~1200-1800 lines for a 3-tab workbook)

### Task 5: Cross-Reference Validation

**Acceptance Criteria:**
- [x] Every query ID (Q01-Q23) from `kql-query-library.md` has a corresponding workbook item
- [x] Visualization types match the `render` hints in the KQL library:
  - `render areachart` → `visualization: "timechart"` (area style)
  - `render timechart` → `visualization: "timechart"`
  - `render barchart` → `visualization: "barchart"`
  - `render piechart` → `visualization: "piechart"`
  - `render anomalychart` → `visualization: "timechart"` with anomaly columns
  - KPI tiles → `visualization: "tiles"`
  - Grid tables → `visualization: "table"`
- [x] Parameter types match KQL library specification:
  - P01 → type 4, P02/P03 → type 2, P04/P05 → type 1
- [x] All design decisions from `01-02-SUMMARY.md` are honored:
  - Q04 excludes ChannelFilter
  - Q10/Q11/Q19 lack agent/channel filters
  - Q17/Q19 use fixed 14d lookback
  - Q22 notes custom telemetry dependency
  - Q23 excludes per-agent error counts

## File Manifest

| File | Action | Description |
|------|--------|-------------|
| `src/agent-usage-workbook.json` | **Modify** | Add Tab 3 (Operational Health), zone thresholds, finalize template |

## Verification Steps

1. Parse `src/agent-usage-workbook.json` with `python -c "import json; json.load(open('src/agent-usage-workbook.json'))"` — must pass
2. Count all type 3 items — must be ≥26 (Q01-Q23 + Q15 dual + Q22 dual + Q23 composite)
3. Count all type 1 markdown items — must be ≥4 (header + latency caveat + DLP caveat + availability caveat)
4. Verify Tab 3 group has ≥7 query items
5. Verify zone-aware threshold settings present on applicable KPI tiles
6. Search for `traces` table — must return 0 matches
7. Search for `designMode` — must appear in all customEvents queries
8. Validate all `{ParameterName}` strings match declared parameter names
9. Verify file does not contain hardcoded subscription IDs or resource IDs (only placeholders)
10. Cross-reference query count: exactly 23 unique query titles matching KQL library

## Dependencies

- **Input:** Plan 02-01 output (`src/agent-usage-workbook.json` with shell + Tab 1 + Tab 2)
- **Input:** `.planning/phases/01-telemetry-research-kql-design/kql-query-library.md` (Q17-Q23 queries)
- **Output consumed by:** Phase 3 (deployment and customization guides reference the completed template)

## Notes

- Plan 02-02 MUST execute after Plan 02-01 (both modify `src/agent-usage-workbook.json`)
- Q22 (DLP Events) may show empty results in most deployments — include workbook-level note about custom telemetry requirement
- Q17 and Q19 anomaly detection queries use `anomalychart` which requires the `series_decompose_anomalies()` function — verify this renders correctly as `visualization: "timechart"` with `chartSettings` including anomaly columns in Azure Workbook JSON format
- Zone-aware thresholds default to Zone 3 (enterprise) — document how to adjust for Zone 1/2 in the workbook header markdown
- The finalized template should be importable via: Azure Portal → Monitor → Workbooks → New → Advanced Editor → paste JSON

---

*Plan created: 2026-02-11*
*Requirements: WBK-03 (Operational Health tab), WBK-04 (Deployable template finalization)*
*Estimated additions: ~9 query items + 3 markdown items + threshold formatting + metadata finalization*
