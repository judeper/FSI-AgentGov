---
phase: 3
plan: 1
wave: 1
autonomous: true
gap_closure: false
dependencies: []
must_haves:
  - Stale root output files deleted or gitignored
  - verify_excel_templates.py expects 62 controls
---

# Plan 03-01: Stale File Cleanup & Excel Fix

## Objective

Remove stale build/verify output files from repository root and fix the Excel template verification script to expect 62 controls (not 61).

**Requirements:** HSK-01, HSK-04

## Tasks

### Task 1: Delete Stale Root Output Files (HSK-01)

**Files to delete:**
- `build_analysis.txt`
- `build_combined.txt`
- `build_err.txt`
- `build_out2.txt`
- `build_output.txt`
- `build_stderr.txt`
- `build_stdout.txt`
- `build_validation_p5.txt`
- `verify_output.txt`
- `verify_out2.txt`

**Actions:**
1. Delete all 10 files via `git rm`
2. Add `*.txt` exclusion pattern to `.gitignore` for root-level output files (to prevent future accumulation)
3. Commit: `chore: remove stale build/verify output files from root`

**Acceptance criteria:**
- All 10 files removed from tracked files
- `.gitignore` updated to prevent recurrence

### Task 2: Fix Excel Expected Count (HSK-04)

**File:** `scripts/verify_excel_templates.py`

**Change:** Line 21 — `"governance-maturity-dashboard.xlsx": 61` → `"governance-maturity-dashboard.xlsx": 62`

**Rationale:** Framework has 62 controls (24 + 21 + 10 + 7) since Control 1.22 was added. The dashboard should validate against the correct total.

**Acceptance criteria:**
- EXPECTED_COUNTS value updated from 61 to 62
- `python scripts/verify_excel_templates.py` passes (or reports only pre-existing issues unrelated to count)
- Commit: `fix(scripts): update Excel expected control count from 61 to 62`
