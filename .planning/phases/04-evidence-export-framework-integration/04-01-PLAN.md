---
phase: 04-evidence-export-framework-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/Export-AuditValidationEvidence.ps1
  - C:/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/private/Get-ValidationResults.ps1
  - C:/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/Test-EvidenceIntegrity.ps1
autonomous: true

must_haves:
  truths:
    - "Export-AuditValidationEvidence produces a JSON file with metadata, summary, and validations sections"
    - "Every exported JSON file has a companion .sha256 file with standard hash format"
    - "Evidence JSON includes timestamp, validation type, overall status, and per-environment details"
    - "Test-EvidenceIntegrity verifies hash matches and returns true/false"
    - "Export supports both Tenant and Environment scope with date range filtering"
  artifacts:
    - path: "C:/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/Export-AuditValidationEvidence.ps1"
      provides: "Main evidence export with SHA-256 hashing"
      contains: "ConvertTo-Json -Depth 10"
    - path: "C:/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/private/Get-ValidationResults.ps1"
      provides: "Dataverse query helper for validation history"
      contains: "fsi_auditvalidationhistories"
    - path: "C:/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/Test-EvidenceIntegrity.ps1"
      provides: "SHA-256 hash verification"
      contains: "Get-FileHash"
  key_links:
    - from: "Export-AuditValidationEvidence.ps1"
      to: "Get-ValidationResults.ps1"
      via: "dot-sourced private helper"
      pattern: "Get-ValidationResults"
    - from: "Export-AuditValidationEvidence.ps1"
      to: "Get-FileHash"
      via: "SHA-256 hash generation"
      pattern: "Get-FileHash.*SHA256"
    - from: "Test-EvidenceIntegrity.ps1"
      to: ".sha256 companion file"
      via: "hash comparison"
      pattern: "Get-FileHash.*-Algorithm SHA256"
---

<objective>
Create PowerShell evidence export scripts that produce JSON files with full validation results and SHA-256 integrity hashes.

Purpose: Compliance evidence collection for FINRA/SEC examinations requires machine-readable exports with cryptographic integrity verification. This plan delivers the evidence export pipeline (EVID-01, EVID-02, EVID-04).
Output: 3 PowerShell scripts -- main export, Dataverse query helper, hash verification utility.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-evidence-export-framework-integration/04-RESEARCH.md

# Reference existing solution scripts for pattern consistency
@C:/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/private/Write-ValidationResult.ps1
@C:/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/private/Connect-PowerPlatform.ps1
@C:/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/Invoke-TenantAuditValidation.ps1
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Get-ValidationResults.ps1 private helper and Export-AuditValidationEvidence.ps1 main script</name>
  <files>
    C:/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/private/Get-ValidationResults.ps1
    C:/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/Export-AuditValidationEvidence.ps1
  </files>
  <action>
    **First, create `scripts/private/Get-ValidationResults.ps1`:**

    A private helper that queries Dataverse `fsi_auditvalidationhistories` table via Web API. Follow the pattern established in Write-ValidationResult.ps1 and Connect-PowerPlatform.ps1.

    Parameters:
    - `-DataverseUrl` (Mandatory, string) -- Dataverse org URL
    - `-AccessToken` (Mandatory, string) -- Bearer token
    - `-Scope` (Mandatory, ValidateSet 'Tenant','Environment') -- filter by scope
    - `-RunId` (Optional, string) -- filter to specific run
    - `-FromDate` (Optional, datetime, default: 30 days ago) -- date range start
    - `-ToDate` (Optional, datetime, default: now) -- date range end

    Implementation:
    - Build OData filter string: scope choice value (Tenant=727070000, Environment=727070001 -- check existing option set values in create_dataverse_schema.py), date range filter on fsi_timestamp, optional RunId filter
    - Use `Invoke-RestMethod` with Authorization header (same pattern as Write-ValidationResult.ps1)
    - Query: `fsi_auditvalidationhistories?$filter={filters}&$orderby=fsi_timestamp desc&$select=fsi_name,fsi_runid,fsi_scope,fsi_environmentid,fsi_zone,fsi_severity,fsi_validationtype,fsi_rawvalue,fsi_reason,fsi_timestamp`
    - Handle pagination with `@odata.nextLink` (while loop until no more pages)
    - Return array of result objects
    - Include `#Requires -Version 7.0` and full comment-based help (.SYNOPSIS, .DESCRIPTION, .PARAMETER, .EXAMPLE)
    - Error handling: try/catch with informative messages for 401 (auth), 404 (table not found), other HTTP errors

    **Then, create `scripts/Export-AuditValidationEvidence.ps1`:**

    Main export script that produces JSON evidence files with SHA-256 companion hashes.

    Parameters:
    - `-DataverseUrl` (Mandatory, string)
    - `-TenantId` (Mandatory, string)
    - `-Scope` (Mandatory, ValidateSet 'Tenant','Environment')
    - `-OutputDirectory` (Mandatory, string) -- directory for evidence files
    - `-RunId` (Optional, string) -- export specific run only
    - `-FromDate` (Optional, datetime, default: 30 days ago)
    - `-ToDate` (Optional, datetime, default: now)
    - `-Interactive` (switch) -- use interactive auth (default: service principal)
    - `-CertificateThumbprint` (Optional, string) -- for service principal auth
    - `-ClientId` (Optional, string) -- app registration client ID

    Implementation:
    1. Dot-source `./private/Connect-PowerPlatform.ps1` and `./private/Get-ValidationResults.ps1`
    2. Authenticate to Dataverse using Connect-PowerPlatform (same pattern as existing orchestrators)
    3. Call Get-ValidationResults with scope/date/RunId filters
    4. Build JSON evidence structure:
       ```
       {
         "metadata": {
           "exportedAt": ISO8601 timestamp,
           "scope": Tenant|Environment,
           "fromDate": ISO8601,
           "toDate": ISO8601,
           "runId": GUID or null,
           "exportVersion": "1.0.0",
           "recordCount": integer,
           "organizationUrl": DataverseUrl
         },
         "summary": {
           "overallStatus": computed (worst severity across all results),
           "validationsRun": count,
           "validationsPassed": count where severity=Passed,
           "validationsFailed": count where severity=Failed or Error,
           "validationsWarning": count where severity=Warning or GracePeriod
         },
         "validations": [ array of result objects with all fields ]
       }
       ```
    5. Convert to JSON with `ConvertTo-Json -Depth 10` (CRITICAL: not default depth 2)
    6. Write to file with `Out-File -Encoding utf8` -- filename: `{scope}-validation-{yyyyMMdd-HHmmss}.json`
    7. Generate SHA-256 hash: `Get-FileHash -Path $filePath -Algorithm SHA256`
    8. Write hash companion file: `"{hash}  {filename}"` format (two spaces) to `{filename}.sha256`
    9. Write output summary to console (evidence file path, hash file path, SHA-256 hash, record count)
    10. Return PSCustomObject with EvidenceFile, HashFile, SHA256, RecordCount, GeneratedAt

    Include `#Requires -Version 7.0` and full comment-based help with examples showing both interactive and service principal auth modes.

    Ensure OutputDirectory is created if it doesn't exist (`New-Item -ItemType Directory -Force`).

    Overall status computation: use severity priority (Error > Failed > GracePeriod > Warning > Passed). If ANY result is Error, overall is Error. If ANY is Failed, overall is Failed. Etc.
  </action>
  <verify>
    1. Both files exist and have #Requires -Version 7.0 header
    2. Get-ValidationResults.ps1 contains OData filter construction and pagination handling
    3. Export-AuditValidationEvidence.ps1 contains ConvertTo-Json -Depth 10 (not default depth)
    4. Export script generates .sha256 companion file with "{hash}  {filename}" format
    5. Both scripts have complete comment-based help (.SYNOPSIS, .DESCRIPTION, .PARAMETER, .EXAMPLE)
    6. Verify no "ensures compliance" or "guarantees" language (FSI-AgentGov language rules)
  </verify>
  <done>
    Get-ValidationResults.ps1 queries Dataverse with scope/date/RunId filters and handles pagination. Export-AuditValidationEvidence.ps1 produces structured JSON evidence with metadata/summary/validations sections and SHA-256 companion hash files. Both follow established solution script patterns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Test-EvidenceIntegrity.ps1 verification utility</name>
  <files>
    C:/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/Test-EvidenceIntegrity.ps1
  </files>
  <action>
    Create a public verification script that validates SHA-256 integrity of evidence files.

    Parameters:
    - `-EvidenceFilePath` (Mandatory, string) -- path to evidence JSON file
    - `-HashFilePath` (Optional, string) -- path to .sha256 file (defaults to "{EvidenceFilePath}.sha256")
    - `-Quiet` (switch) -- suppress console output, return boolean only

    Implementation:
    1. Validate evidence file exists (throw if not)
    2. Determine hash file path (parameter or default convention)
    3. Validate hash file exists (throw if not)
    4. Read expected hash from .sha256 file: parse first field (split on whitespace)
    5. Compute actual hash: `(Get-FileHash -Path $EvidenceFilePath -Algorithm SHA256).Hash`
    6. Compare hashes (case-insensitive, -eq handles this)
    7. If match: Write-Output verification message (unless -Quiet), return $true
    8. If mismatch: Write-Warning with expected vs actual hashes, return $false

    Include `#Requires -Version 5.1` (this script uses only basic cmdlets, compatible with PS 5.1+).

    Full comment-based help with:
    - .SYNOPSIS: "Verifies SHA-256 integrity of audit validation evidence files."
    - .DESCRIPTION: Explains the verification process
    - .EXAMPLE: Single file verification
    - .EXAMPLE: Batch verification of all evidence in a directory
    - .EXAMPLE: Quiet mode for automation

    The batch example should show:
    ```powershell
    Get-ChildItem .\exports\*.json | ForEach-Object {
        Test-EvidenceIntegrity -EvidenceFilePath $_.FullName
    }
    ```
  </action>
  <verify>
    1. File exists with #Requires -Version 5.1
    2. Script uses Get-FileHash -Algorithm SHA256
    3. Script handles missing evidence file and missing hash file with clear error messages
    4. Script returns boolean ($true/$false)
    5. Comment-based help includes all three examples (single, batch, quiet)
  </verify>
  <done>
    Test-EvidenceIntegrity.ps1 verifies SHA-256 hashes of evidence files by comparing computed hash against companion .sha256 file. Returns boolean for automation compatibility.
  </done>
</task>

</tasks>

<verification>
All three scripts exist in the audit-configuration-validator/scripts/ directory:
1. `Export-AuditValidationEvidence.ps1` -- main export producing JSON + .sha256
2. `private/Get-ValidationResults.ps1` -- Dataverse query helper
3. `Test-EvidenceIntegrity.ps1` -- hash verification utility

Requirements satisfied:
- EVID-01: JSON evidence export with full validation results (Export-AuditValidationEvidence.ps1)
- EVID-02: SHA-256 integrity hashing (.sha256 companion files)
- EVID-04: Evidence includes timestamp, validation type, overall status, per-environment details (JSON schema)
</verification>

<success_criteria>
- Export-AuditValidationEvidence.ps1 produces structured JSON with metadata, summary, validations sections
- JSON uses -Depth 10 to prevent nested object truncation
- .sha256 companion file uses standard format (hash + two spaces + filename)
- Get-ValidationResults.ps1 queries Dataverse with OData filters and handles pagination
- Test-EvidenceIntegrity.ps1 verifies hash integrity and returns boolean
- All scripts follow established solution patterns (#Requires, comment-based help, error handling)
</success_criteria>

<output>
After completion, create `.planning/phases/04-evidence-export-framework-integration/04-01-SUMMARY.md`

Git operations: Commit to FSI-AgentGov-Solutions repository (cd to C:/dev/FSI-AgentGov-Solutions first).
</output>
