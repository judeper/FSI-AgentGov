---
phase: 04-compliance-dashboard-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/scripts/load_sample_data.py
  - /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/scripts/requirements.txt
  - /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/sample-data/sample-assessments.json
  - /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/sample-data/sample-scores.json
  - /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/sample-data/sample-exceptions.json
autonomous: true

must_haves:
  truths:
    - "Sample data covers all 62 controls with assessments across all applicable zones"
    - "90 days of historical score data exists with realistic trend patterns"
    - "Exceptions exist with varied SLA statuses (On Track, At Risk, Breached)"
    - "Running --dry-run shows expected data distribution"
  artifacts:
    - path: "/Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/scripts/load_sample_data.py"
      provides: "Enhanced sample data loader with 90-day history and realistic distributions"
      contains: "generate_90day_scores"
    - path: "/Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/sample-data/sample-assessments.json"
      provides: "Pre-generated assessment data for demo/testing"
      min_lines: 100
    - path: "/Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/sample-data/sample-scores.json"
      provides: "90 days of compliance score snapshots"
      min_lines: 90
    - path: "/Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/sample-data/sample-exceptions.json"
      provides: "Sample exceptions with SLA status variety"
      min_lines: 20
  key_links:
    - from: "scripts/load_sample_data.py"
      to: "sample-data/control-master.json"
      via: "reads control definitions to generate assessments"
      pattern: "control-master\\.json"
---

<objective>
Enhance sample data generation to support full 90-day history with realistic compliance score distributions across all 62 controls and 3 zones.

Purpose: Sample data enables Power BI template testing and customer demonstrations. Without realistic data, DAX measures cannot be validated and the dashboard appears empty or unrealistic.

Output: Enhanced load_sample_data.py script and pre-generated JSON sample data files.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compliance-dashboard-completion/04-CONTEXT.md
@.planning/phases/04-compliance-dashboard-completion/04-RESEARCH.md

# Reference existing solution files
@/Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/scripts/load_sample_data.py
@/Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/sample-data/control-master.json
@/Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/docs/dataverse-schema.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance sample data generation with 90-day history</name>
  <files>
    /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/scripts/load_sample_data.py
    /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/scripts/requirements.txt
  </files>
  <action>
    Enhance load_sample_data.py with the following changes:

    1. Update generate_sample_assessments() to:
       - Generate weekly assessments for each control/zone combination over 90 days (not just one snapshot)
       - Use weighted status distribution: 50% compliant (1), 33% partial (2), 17% non-compliant (3)
       - Include gradual improvement trend (earlier dates slightly worse scores)
       - Add variance per control (some controls consistently worse than others)

    2. Update generate_sample_scores() to:
       - Generate exactly 90 days of daily score snapshots
       - Calculate pillar scores with realistic variance (-5 to +5 from overall)
       - Zone 3 scores consistently 5-10 points lower than overall (higher risk zone)
       - Ensure fsi_compliantcount + fsi_partialcount + fsi_noncompliantcount reasonably sums to ~62

    3. Update generate_sample_exceptions() to:
       - Generate 10-15 exceptions (not just 8)
       - Ensure distribution across SLA statuses: ~40% On Track, ~35% At Risk, ~25% Breached
       - Include exceptions for each severity level (Critical, High, Medium, Low)
       - Add realistic root cause text and remediation plans

    4. Add new function export_sample_data() to:
       - Write generated data to JSON files in sample-data/ directory
       - Use indent=2 for readability
       - Include generation timestamp in output

    5. Add --export flag to CLI that:
       - Generates all sample data
       - Writes to JSON files without Dataverse connection
       - Prints summary of generated records

    6. Update requirements.txt to ensure all dependencies listed (msal, requests already present)

    Implementation notes:
    - Use random.seed(42) for reproducible generation (deterministic sample data)
    - Score calculation: Compliant=100, Partial=50, Non-Compliant=0
    - Zone multipliers: Zone 1=1.0, Zone 2=1.5, Zone 3=2.0 (per docs/dataverse-schema.md)
  </action>
  <verify>
    Run: cd /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard && python scripts/load_sample_data.py --dry-run

    Expected output shows:
    - 62 control definitions loaded
    - Assessments: ~800+ records (62 controls x 13 weeks x applicable zones)
    - Daily Scores: 90 records
    - Exceptions: 10-15 records
  </verify>
  <done>
    - load_sample_data.py includes 90-day assessment generation
    - Status distribution weighted (50/33/17)
    - Zone 3 scores lower than other zones
    - --dry-run shows realistic data counts
  </done>
</task>

<task type="auto">
  <name>Task 2: Export pre-generated sample data to JSON files</name>
  <files>
    /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/sample-data/sample-assessments.json
    /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/sample-data/sample-scores.json
    /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard/sample-data/sample-exceptions.json
  </files>
  <action>
    Run the enhanced script to export sample data to JSON files.

    Execute: cd /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard && python scripts/load_sample_data.py --export

    Verify generated files:
    1. sample-assessments.json:
       - Contains assessment records for each control/zone/week combination
       - Each record has: fsi_controlid, fsi_zone, fsi_status, fsi_score, fsi_assessmentdate, fsi_notes
       - Dates span 90 days from current date

    2. sample-scores.json:
       - Contains 90 daily score records
       - Each record has: fsi_scoredate, fsi_overallscore, fsi_pillar1score through fsi_pillar4score, fsi_zone1score through fsi_zone3score, fsi_compliantcount, fsi_partialcount, fsi_noncompliantcount, fsi_exceptioncount

    3. sample-exceptions.json:
       - Contains 10-15 exception records
       - Each record has: fsi_name, fsi_controlid, fsi_severity, fsi_status, fsi_description, fsi_targetdate, fsi_daysopen, fsi_slastatus, fsi_rootcause, fsi_remediationplan

    After export:
    - Verify files are valid JSON (python -m json.tool)
    - Verify file sizes are reasonable (sample-assessments.json should be largest)
    - Verify data patterns match expected distributions
  </action>
  <verify>
    Run validation:
    - cd /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard
    - python -m json.tool sample-data/sample-assessments.json > /dev/null && echo "assessments: valid JSON"
    - python -m json.tool sample-data/sample-scores.json > /dev/null && echo "scores: valid JSON"
    - python -m json.tool sample-data/sample-exceptions.json > /dev/null && echo "exceptions: valid JSON"
    - wc -l sample-data/*.json (check line counts)
  </verify>
  <done>
    - Three JSON files exist in sample-data/ directory
    - All files contain valid JSON
    - sample-assessments.json has 800+ assessment records
    - sample-scores.json has exactly 90 score records
    - sample-exceptions.json has 10-15 exception records
  </done>
</task>

</tasks>

<verification>
1. load_sample_data.py passes syntax check: python -m py_compile scripts/load_sample_data.py
2. --dry-run mode works without Dataverse connection
3. --export mode creates valid JSON files
4. Sample data distributions match spec (50/33/17 status, Zone 3 lower scores)
5. No regression in existing functionality (--environment still works with token)
</verification>

<success_criteria>
- Enhanced load_sample_data.py with 90-day history generation
- Three sample data JSON files exported and valid
- Data patterns realistic (not all 100s, not uniform random)
- Script runs without errors in both --dry-run and --export modes
</success_criteria>

<output>
After completion, create `.planning/phases/04-compliance-dashboard-completion/04-01-SUMMARY.md`
</output>
