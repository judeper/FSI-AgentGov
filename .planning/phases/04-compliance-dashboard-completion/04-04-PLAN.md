---
phase: 04-compliance-dashboard-completion
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Power Platform solution package can be created with pac solution pack"
    - "Sample data generates realistic compliance distributions"
    - "Documentation is complete and accurate"
  artifacts: []
  key_links: []
---

<objective>
Verify all Phase 4 artifacts are complete and ready for production use. This plan validates that automated work is correct and documents any manual steps required.

Purpose: Final quality gate before marking solution as v1.0.0 production-ready.

Output: Verification confirmation or list of issues to address.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-compliance-dashboard-completion/04-CONTEXT.md
@.planning/phases/04-compliance-dashboard-completion/04-01-SUMMARY.md
@.planning/phases/04-compliance-dashboard-completion/04-02-SUMMARY.md
@.planning/phases/04-compliance-dashboard-completion/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify all artifacts exist and pass validation</name>
  <files></files>
  <action>
    Run comprehensive validation of all Phase 4 artifacts:

    1. Sample Data Validation:
       ```bash
       cd /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard

       # Verify script runs
       python scripts/load_sample_data.py --dry-run

       # Verify JSON files exist and are valid
       python -m json.tool sample-data/control-master.json > /dev/null
       python -m json.tool sample-data/sample-assessments.json > /dev/null
       python -m json.tool sample-data/sample-scores.json > /dev/null
       python -m json.tool sample-data/sample-exceptions.json > /dev/null

       # Count records
       python -c "import json; print('Assessments:', len(json.load(open('sample-data/sample-assessments.json'))))"
       python -c "import json; print('Scores:', len(json.load(open('sample-data/sample-scores.json'))))"
       python -c "import json; print('Exceptions:', len(json.load(open('sample-data/sample-exceptions.json'))))"
       ```

    2. Solution Package Validation:
       ```bash
       # Verify structure exists
       ls -la src/ComplianceDashboard/
       ls -la src/ComplianceDashboard/Other/
       ls -la src/ComplianceDashboard/Workflows/

       # Verify XML/JSON syntax
       python -m json.tool src/ComplianceDashboard/Workflows/CD-ScoreCalculator.json > /dev/null
       python -m json.tool src/ComplianceDashboard/Workflows/CD-ExceptionMonitor.json > /dev/null

       # Check Solution.xml contains version
       grep "1.0.0" src/ComplianceDashboard/Other/Solution.xml

       # Check Customizations.xml contains all tables
       grep -c "fsi_control" src/ComplianceDashboard/Other/Customizations.xml
       ```

    3. Documentation Validation:
       ```bash
       # Verify new docs exist
       ls -la docs/deployment-checklist.md
       ls -la docs/power-bi-template-spec.md

       # Verify README updated
       grep -q "Known Limitations" README.md && echo "README has Known Limitations"
       grep -q "1.0.0" README.md && echo "README has v1.0.0"
       ! grep -q "beta" README.md && echo "README has no beta reference"

       # Verify CHANGELOG updated
       head -20 CHANGELOG.md
       ```

    4. Cross-Reference Validation:
       - Verify docs/deployment-checklist.md references correct file paths
       - Verify docs/power-bi-template-spec.md references existing DAX measures
       - Verify README.md documentation table includes all docs

    Report any validation failures.
  </action>
  <verify>
    All validation commands complete without errors.
    Sample data counts match expected values.
    No "beta" references in README.
  </verify>
  <done>
    - All JSON files valid
    - All XML files present
    - Documentation complete
    - Cross-references valid
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Compliance Dashboard v1.0.0 artifacts:
    1. Enhanced sample data generation (90-day history, realistic distributions)
    2. Power Platform solution package source files (ready for pac solution pack)
    3. Power Automate flow definitions (CD-ScoreCalculator, CD-ExceptionMonitor)
    4. Deployment checklist and Power BI template specification
    5. Updated README with Known Limitations
  </what-built>
  <how-to-verify>
    Review the following artifacts:

    **1. Sample Data Quality:**
    ```bash
    cd /Users/admin/dev/FSI-AgentGov-Solutions/compliance-dashboard
    python scripts/load_sample_data.py --dry-run
    ```
    - Verify assessment count is ~800+ (62 controls x zones x weeks)
    - Verify score count is 90 (one per day)
    - Verify exception count is 10-15

    **2. Solution Package Structure:**
    ```bash
    ls -laR src/ComplianceDashboard/
    ```
    - Verify Solution.xml, Customizations.xml exist
    - Verify Workflows/ contains two flow JSON files
    - Verify connection references and environment variables defined

    **3. Documentation Review:**
    - Open docs/deployment-checklist.md - verify checklist is comprehensive
    - Open docs/power-bi-template-spec.md - verify all 5 pages specified
    - Open README.md - verify Known Limitations section exists
    - Open CHANGELOG.md - verify v1.0.0 entry at top

    **4. README Status:**
    - Confirm no "beta" or "Work In Progress" text
    - Confirm Known Limitations includes all 6 items per CONTEXT.md

    **Note on Power BI Template:**
    The actual .pbit file must be created manually using Power BI Desktop.
    docs/power-bi-template-spec.md provides step-by-step instructions.
    This is a known limitation documented in README.

    **Note on Solution Package:**
    To create the final .zip package, run:
    ```bash
    pac solution pack --zipfile templates/ComplianceDashboard_1_0_0.zip --folder src/ComplianceDashboard --packagetype Unmanaged
    ```
    This requires Power Platform CLI authentication.
  </how-to-verify>
  <resume-signal>Type "approved" if all artifacts look correct, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
Phase 4 complete when:
1. All automated artifacts pass validation
2. Human review confirms quality and completeness
3. Known limitations documented and accepted
</verification>

<success_criteria>
- Sample data generates 800+ assessments, 90 scores, 10-15 exceptions
- Solution package source ready for pac solution pack
- Documentation complete and accurate
- README reflects v1.0.0 production status
- Human approves artifact quality
</success_criteria>

<output>
After completion, create `.planning/phases/04-compliance-dashboard-completion/04-04-SUMMARY.md`
</output>
