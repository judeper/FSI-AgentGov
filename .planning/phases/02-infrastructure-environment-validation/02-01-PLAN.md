---
phase: 02-infrastructure-environment-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/README.md
  - /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/CHANGELOG.md
  - /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/requirements.txt
  - /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/acv_client.py
  - /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/create_dataverse_schema.py
  - /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/create_environment_variables.py
  - /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/create_connection_references.py
  - /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/deploy.py
autonomous: true

must_haves:
  truths:
    - "deploy.py creates all Dataverse tables, option sets, environment variables, and connection references in a single orchestrated run"
    - "Validation history table is organization-owned (immutable append-only) with fsi_ prefix on all schema names"
    - "Environment registry table tracks environments with zone classification, status, and override capability"
    - "Zone retention thresholds are stored as Dataverse environment variables (fsi_ACV_*), not hardcoded"
    - "deploy.py --dry-run previews all changes without modifying Dataverse"
  artifacts:
    - path: "/Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/acv_client.py"
      provides: "Dataverse Web API client with MSAL auth (interactive + service principal)"
      contains: "class ACVClient"
    - path: "/Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/create_dataverse_schema.py"
      provides: "Dataverse table creation (fsi_auditvalidationhistory, fsi_environmentregistry, option sets)"
      contains: "fsi_auditvalidationhistory"
    - path: "/Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/create_environment_variables.py"
      provides: "Environment variable creation (fsi_ACV_Zone1RetentionDays, etc.)"
      contains: "fsi_ACV_Zone"
    - path: "/Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/create_connection_references.py"
      provides: "Connection reference creation (fsi_cr_*)"
      contains: "fsi_cr_"
    - path: "/Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/deploy.py"
      provides: "Orchestrator for all Dataverse provisioning"
      contains: "def main"
  key_links:
    - from: "deploy.py"
      to: "acv_client.py"
      via: "imports ACVClient"
      pattern: "from acv_client import ACVClient"
    - from: "deploy.py"
      to: "create_dataverse_schema.py"
      via: "imports create_schema"
      pattern: "from create_dataverse_schema import"
    - from: "create_dataverse_schema.py"
      to: "acv_client.py"
      via: "uses ACVClient for API calls"
      pattern: "ACVClient"
---

<objective>
Create the Dataverse infrastructure for the Audit Configuration Validator solution: API client, tables (validation history + environment registry), option sets, environment variables for zone thresholds, connection references, and the deploy.py orchestrator. Also establish the solution folder structure (README, CHANGELOG, docs/, src/).

Purpose: This is the foundation all subsequent plans depend on. Without Dataverse tables and the API client, environment discovery and validation scripts cannot store or query results. The deploy.py script enables rapid lab testing per user's explicit request.

Output: 8 Python files (client, schema, env vars, connection refs, deploy orchestrator, requirements.txt) plus README.md and CHANGELOG.md. Solution folder structure matching the Tier 2 pattern from environment-lifecycle-management.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-infrastructure-environment-validation/02-RESEARCH.md
@.planning/phases/02-infrastructure-environment-validation/02-CONTEXT.md
@.planning/phases/01-core-validation-scripts/01-03-SUMMARY.md

# Reference pattern — follow this solution's structure exactly
@/Users/admin/dev/FSI-AgentGov-Solutions/environment-lifecycle-management/README.md
@/Users/admin/dev/FSI-AgentGov-Solutions/environment-lifecycle-management/scripts/elm_client.py
@/Users/admin/dev/FSI-AgentGov-Solutions/environment-lifecycle-management/scripts/deploy.py
@/Users/admin/dev/FSI-AgentGov-Solutions/environment-lifecycle-management/scripts/create_dataverse_schema.py
@/Users/admin/dev/FSI-AgentGov-Solutions/environment-lifecycle-management/scripts/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create solution folder structure, API client, and Dataverse schema scripts</name>
  <files>
    /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/README.md
    /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/CHANGELOG.md
    /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/docs/.gitkeep
    /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/src/.gitkeep
    /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/requirements.txt
    /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/acv_client.py
    /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/create_dataverse_schema.py
  </files>
  <action>
**Solution folder structure (INFR-01):**
Create docs/ and src/ directories (with .gitkeep). README.md with solution overview, prerequisites, quick start, zone requirements. CHANGELOG.md with v0.1.0 (Phase 1 scripts) and v0.2.0 (Phase 2 infrastructure). Follow the exact same section structure as /Users/admin/dev/FSI-AgentGov-Solutions/environment-lifecycle-management/README.md.

**requirements.txt:**
Same dependencies as ELM: msal>=1.30.0, requests>=2.32.0, azure-identity>=1.18.0, azure-keyvault-secrets>=4.7.0. Add comment header "Audit Configuration Validator - Python Dependencies" with Python 3.10+ note.

**acv_client.py (reuse elm_client.py pattern exactly):**
Create ACVClient class modeled directly on ELMClient from /Users/admin/dev/FSI-AgentGov-Solutions/environment-lifecycle-management/scripts/elm_client.py. Same MSAL auth pattern (PublicClientApplication for interactive, ConfidentialClientApplication for service principal). Same token caching. Same _get_headers() method. Same retry logic with requests.adapters.HTTPAdapter (Retry with total=3, backoff_factor=1, status_forcelist=[429, 500, 502, 503, 504]). Add helper methods: create_table(), create_option_set(), create_column(), create_record(), query(), check_table_exists() (for idempotency). The client must support --dry-run mode where API calls are logged but not executed.

**create_dataverse_schema.py (INFR-02, EVID-03):**
Create two tables and their supporting option sets following the ELM create_dataverse_schema.py pattern.

Global option sets to create:
1. fsi_acv_severity — Passed(1), Warning(2), GracePeriod(3), Failed(4), Error(5)
2. fsi_acv_scope — Tenant(100000000), Environment(100000001)
3. fsi_acv_zone — Unclassified(0), Zone1(1), Zone2(2), Zone3(3)
4. fsi_acv_envstatus — Active(1), Inactive(2)
5. fsi_acv_environmenttype — Production(1), Sandbox(2), Developer(3), Trial(4), Default(5)

Table 1: fsi_auditvalidationhistory (organization-owned, immutable)
- fsi_name (String, PrimaryName, 200 chars) — "ENV-{name}-{timestamp}" or "TENANT-{timestamp}"
- fsi_runid (String, 36 chars) — GUID correlating all records in one execution run
- fsi_scope (Picklist, fsi_acv_scope) — Tenant or Environment
- fsi_environmentid (String, 100 chars, optional) — Power Platform environment ID (null for tenant scope)
- fsi_environmentname (String, 200 chars, optional) — Display name for readability
- fsi_zone (Picklist, fsi_acv_zone) — Zone at time of validation (denormalized per user decision)
- fsi_severity (Picklist, fsi_acv_severity) — Overall validation result
- fsi_validationtype (String, 100 chars) — "UnifiedAuditLog", "MailboxAudit", "PurviewRetention", "EnvironmentAudit", "EnvironmentRetention", "Orchestrator"
- fsi_rawvalue (String, 4000 chars) — Actual config values checked (e.g., "AuditEnabled=true,RetentionDays=90")
- fsi_reason (String, 4000 chars) — Human-readable explanation
- fsi_remediationhint (String, 2000 chars, optional) — Suggested fix (Claude's discretion: include for Phase 3 alerting)
- fsi_timestamp (DateTime) — When validation ran
- fsi_checkcount (Integer, optional) — Number of individual checks in this result
CRITICAL: OwnershipType must be "OrganizationOwned". Document in code comment that security roles must remove Write/Delete privileges post-deployment.

Table 2: fsi_environmentregistry (organization-owned)
- fsi_name (String, PrimaryName, 200 chars) — Environment display name
- fsi_environmentid (String, 100 chars, unique) — Power Platform environment GUID
- fsi_zone (Picklist, fsi_acv_zone) — Assigned governance zone (Unclassified triggers alert)
- fsi_status (Picklist, fsi_acv_envstatus) — Active or Inactive
- fsi_environmenttype (Picklist, fsi_acv_environmenttype) — Production, Sandbox, etc.
- fsi_environmenturl (String, 500 chars, optional) — Dataverse URL for API calls
- fsi_discoveredon (DateTime) — When first discovered by API
- fsi_lastvalidated (DateTime, optional) — Last successful validation timestamp
- fsi_overrideinclude (Boolean, default false) — Admin override to include Trial/Dev environments
- fsi_notes (String, 2000 chars, optional) — Admin notes

Use idempotency approach: check_table_exists() before creating (Claude's discretion: use GET to EntityDefinitions filtering by SchemaName, skip creation if exists, log "Table already exists, skipping").

The create_schema() function should accept an ACVClient instance and return a results dict with success/failure counts.
  </action>
  <verify>
1. python -c "from acv_client import ACVClient; print('ACVClient imported successfully')" (run from scripts/ directory)
2. python -c "from create_dataverse_schema import OPTIONSETS, HISTORY_TABLE, REGISTRY_TABLE; print(f'Option sets: {len(OPTIONSETS)}, Tables: 2')"
3. Verify fsi_auditvalidationhistory has OwnershipType "OrganizationOwned" in TABLE definition
4. Verify all schema names start with "fsi_"
5. README.md exists with Prerequisites, Quick Start, Zone Requirements sections
6. CHANGELOG.md has v0.1.0 and v0.2.0 entries
7. requirements.txt has msal, requests dependencies
  </verify>
  <done>
ACVClient class importable with MSAL interactive/SP auth, retry logic, dry-run support, and idempotent helper methods. Dataverse schema script defines 5 option sets, 2 tables (history=org-owned, registry=org-owned) with all columns using fsi_ prefix. Solution has README, CHANGELOG, docs/, src/ directories per Tier 2 pattern (INFR-01, INFR-02, EVID-03).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create environment variables, connection references, and deploy orchestrator</name>
  <files>
    /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/create_environment_variables.py
    /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/create_connection_references.py
    /Users/admin/dev/FSI-AgentGov-Solutions/audit-configuration-validator/scripts/deploy.py
  </files>
  <action>
**create_environment_variables.py (INFR-04):**
Create Dataverse environment variables for configurable zone thresholds. Follow ELM pattern.

Environment variables to create:
1. fsi_ACV_Zone1RetentionDays — type: Decimal number, default: "180", description: "Minimum audit retention days for Zone 1 (Personal Productivity)"
2. fsi_ACV_Zone2RetentionDays — type: Decimal number, default: "365", description: "Minimum audit retention days for Zone 2 (Team Collaboration)"
3. fsi_ACV_Zone3RetentionDays — type: Decimal number, default: "730", description: "Minimum audit retention days for Zone 3 (Enterprise Managed)"
4. fsi_ACV_GracePeriodHours — type: Decimal number, default: "24", description: "Hours to wait after audit enablement before alerting"
5. fsi_ACV_CanaryWaitMinutes — type: Decimal number, default: "5", description: "Minutes to wait for canary event propagation"

For each variable:
- POST to /api/data/v9.2/environmentvariabledefinitions to create definition
- POST to /api/data/v9.2/environmentvariablevalues to set current value matching default
- Use idempotency: query existing env var definitions by schemaname before creating

The create_environment_variables() function should accept an ACVClient instance and return results dict.

**create_connection_references.py (INFR-03):**
Create connection references needed for the solution.

Connection references to create:
1. fsi_cr_dataverse_auditvalidation — connector: shared_commondataserviceforapps, display name: "Dataverse - Audit Configuration Validator"
2. fsi_cr_office365_auditvalidation — connector: shared_office365, display name: "Office 365 - Audit Configuration Validator"

For each:
- POST to /api/data/v9.2/connectionreferences
- Use connectionreferencelogicalname for the fsi_cr_* name
- connectorid follows Power Platform connector ID format: "/providers/Microsoft.PowerApps/apis/{connector_name}"
- Connection references are definition-only at creation time; actual connections are bound during solution import or runtime
- Idempotency: query existing by connectionreferencelogicalname, skip if exists

The create_connection_references() function should accept an ACVClient instance and return results dict.

**deploy.py orchestrator:**
Model on /Users/admin/dev/FSI-AgentGov-Solutions/environment-lifecycle-management/scripts/deploy.py exactly. Same argument pattern:
- --environment-url (required): Dataverse URL
- --tenant-id (required): Entra tenant ID
- --client-id (optional): App registration ID
- --client-secret (optional): Client secret
- --interactive (flag): Use browser auth
- --dry-run (flag): Preview without changes
- --tables-only (flag): Only create tables/schema
- --vars-only (flag): Only create environment variables
- --refs-only (flag): Only create connection references

Execution flow:
1. Print banner ("Audit Configuration Validator - Dataverse Deployment")
2. Parse args, create ACVClient
3. If not --tables-only/--vars-only/--refs-only, run all steps
4. Step 1: create_schema() — option sets + tables
5. Step 2: create_environment_variables() — zone thresholds + operational params
6. Step 3: create_connection_references() — Dataverse + Office 365
7. Print summary table with step name, status, created/skipped counts

Each step wrapped in try-except with clear error reporting. deploy.py imports from the three create_* modules. If --dry-run, ACVClient logs all would-be API calls without executing.
  </action>
  <verify>
1. python -c "from create_environment_variables import ENV_VARIABLES; print(f'Env vars: {len(ENV_VARIABLES)}')" — should print 5
2. python -c "from create_connection_references import CONNECTION_REFS; print(f'Connection refs: {len(CONNECTION_REFS)}')" — should print 2
3. python deploy.py --help — should show all argument flags (--environment-url, --tenant-id, --interactive, --dry-run, --tables-only, --vars-only, --refs-only)
4. Verify deploy.py imports from acv_client, create_dataverse_schema, create_environment_variables, create_connection_references
5. Verify all 5 environment variable schemanames start with "fsi_ACV_"
6. Verify all connection reference logical names start with "fsi_cr_"
  </verify>
  <done>
deploy.py orchestrates full Dataverse provisioning (schema + env vars + connection refs) with --dry-run preview, selective deployment flags, and idempotent execution. Environment variables store configurable zone thresholds (180d/365d/730d) and operational parameters (24h grace, 5min canary). Connection references (fsi_cr_*) define Dataverse and Office 365 connectors. All naming conventions honored (INFR-03, INFR-04).
  </done>
</task>

</tasks>

<verification>
From the scripts/ directory in FSI-AgentGov-Solutions/audit-configuration-validator/:

1. **File count**: 8 Python files exist (acv_client.py, create_dataverse_schema.py, create_environment_variables.py, create_connection_references.py, deploy.py, requirements.txt) plus existing Phase 1 PowerShell scripts
2. **Import chain**: `python -c "from deploy import main; print('deploy.py ready')"` succeeds
3. **Schema integrity**: All table SchemaName values start with "fsi_", OwnershipType is "OrganizationOwned" for both tables
4. **Naming conventions**: grep for "fsi_ACV_" in create_environment_variables.py finds 5 matches; grep for "fsi_cr_" in create_connection_references.py finds 2 matches
5. **Solution structure**: README.md, CHANGELOG.md, docs/, scripts/, src/ all exist
6. **deploy.py --help** runs without error
</verification>

<success_criteria>
- ACVClient class follows ELMClient pattern with MSAL auth, retry logic, dry-run mode
- 2 Dataverse tables defined: fsi_auditvalidationhistory (org-owned, immutable), fsi_environmentregistry (org-owned)
- 5 global option sets defined with correct values
- 5 environment variables with fsi_ACV_* names and correct defaults
- 2 connection references with fsi_cr_* names
- deploy.py orchestrates all creation with --dry-run and selective flags
- Solution follows Tier 2 pattern (README, CHANGELOG, docs/, scripts/, src/)
- Requirements covered: INFR-01, INFR-02, INFR-03, INFR-04, EVID-03
</success_criteria>

<output>
After completion, create `.planning/phases/02-infrastructure-environment-validation/02-01-SUMMARY.md`
</output>
