---
phase: 03-agent-365-strategic-architecture
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified: []
autonomous: true

must_haves:
  truths:
    - "mkdocs build --strict passes with zero errors"
    - "All cross-references between agent-365-architecture.md and controls resolve correctly"
    - "verify_controls.py reports 62/62 controls valid"
    - "No prohibited regulatory language exists in any new or modified content"
  artifacts: []
  key_links:
    - from: "docs/framework/agent-365-architecture.md"
      to: "docs/controls/pillar-1-security/1.2-agent-registry-and-integrated-apps-management.md"
      via: "Bidirectional cross-reference"
      pattern: "1.2"
    - from: "docs/controls/pillar-2-management/2.12-supervision-and-oversight-finra-rule-3110.md"
      to: "docs/framework/agent-365-architecture.md"
      via: "Cross-reference link"
      pattern: "agent-365-architecture"
---

<objective>
Validate that all Phase 3 deliverables build correctly, cross-references resolve, and regulatory language rules are followed.

Purpose: Phase 3 introduces a new framework document and updates 3 controls. Validation confirms the documentation site builds cleanly, all links work, and no prohibited language was introduced.

Output: Validation results confirming Phase 3 is complete.
</objective>

<execution_context>
@/Users/admin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-agent-365-strategic-architecture/03-01-SUMMARY.md
@.planning/phases/03-agent-365-strategic-architecture/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full build and structural validation</name>
  <files></files>
  <action>
Run the following validation commands and verify all pass:

1. `mkdocs build --strict` — Must produce zero errors and zero warnings. This validates that:
   - The new agent-365-architecture.md is correctly referenced in mkdocs.yml
   - All internal cross-references resolve (links from controls to framework doc and vice versa)
   - No markdown syntax errors exist

2. `python scripts/verify_controls.py` — Must report 62/62 controls valid. This validates that:
   - Control 1.2 still has valid 10-section structure after edits
   - Control 1.11 still has valid structure after edits
   - Control 2.12 still has valid structure after edits

3. Grep for prohibited language in all new/modified files:
   - `grep -i "ensures compliance" docs/framework/agent-365-architecture.md` — must return 0 matches
   - `grep -i "guarantees" docs/framework/agent-365-architecture.md` — must return 0 matches
   - `grep -i "will prevent" docs/framework/agent-365-architecture.md` — must return 0 matches
   - `grep -i "eliminates risk" docs/framework/agent-365-architecture.md` — must return 0 matches
   - Same checks on the three modified control files

4. Verify bidirectional cross-references:
   - agent-365-architecture.md links to controls 1.2, 1.11, 2.12, 3.6
   - Control 1.2 links to agent-365-architecture.md
   - Control 1.11 links to agent-365-architecture.md
   - Control 2.12 links to agent-365-architecture.md and agent-identity-architecture.md

If any validation fails, fix the issue in the relevant file and re-run validation until all checks pass.
  </action>
  <verify>
- mkdocs build --strict exits with code 0
- verify_controls.py reports 62/62 valid
- Zero matches for prohibited language patterns
- Bidirectional cross-references confirmed
  </verify>
  <done>All validation checks pass: clean build, 62 valid controls, no prohibited language, all cross-references bidirectional.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate researcher package</name>
  <files></files>
  <action>
Run `python scripts/compile_researcher_package.py` to regenerate the researcher package with the new Agent 365 architecture content and updated control cross-references.

This is required by the PostToolUse hook reminder whenever pillar control files are edited (Controls 1.2, 1.11, 2.12 were modified in Plan 03-02).

Verify the script completes successfully without errors.
  </action>
  <verify>
- compile_researcher_package.py exits with code 0
- No error output from the script
  </verify>
  <done>Researcher package regenerated to include Agent 365 architecture content and updated control cross-references.</done>
</task>

</tasks>

<verification>
1. mkdocs build --strict: zero errors, zero warnings
2. verify_controls.py: 62/62 controls valid
3. Prohibited language check: zero matches across all modified files
4. Cross-references: bidirectional between framework doc and controls
5. Researcher package: regenerated successfully
</verification>

<success_criteria>
- All validation checks pass on first run (or after minor fixes)
- Phase 3 success criteria from ROADMAP.md fully met:
  1. New framework document explains Agent 365 unified control plane concept
  2. Entra Agent ID architecture documented with sponsorship model and FINRA 3110 alignment
  3. FSI organizations have clear migration guidance
  4. Cross-references established between Agent 365 architecture and controls 1.2, 1.11, 2.12
</success_criteria>

<output>
After completion, create `.planning/phases/03-agent-365-strategic-architecture/03-03-SUMMARY.md`
</output>
