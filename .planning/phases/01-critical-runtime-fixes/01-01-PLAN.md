---
phase: 1
plan: 1
title: "CAA Runtime Fixes"
wave: 1
dependencies: []
must_haves:
  - "Flow JSON column names match Dataverse schema"
  - "Documentation URLs resolve to correct control pages"
  - "Test-PolicyCompliance.ps1 can execute without missing file errors"
  - "CA policy naming consistent between validators and playbook templates"
---

## Goal

Fix 5 critical/high runtime issues in the Conditional Access Automation solution that would cause deployment or execution failures.

## Tasks

### Task 1: Fix Dataverse column name mismatch (RTF-01)
- **Files:** `src/caa-daily-compliance-flow.json` (~L846), `src/caa-provisioning-hook-flow.json` (~L797)
- **Change:** In both flow files, find all instances of `fsi_result_json` and replace with `fsi_results_json` (plural)
- **Acceptance:** Column name matches `_validation_history_columns()` in `scripts/create_dataverse_schema.py`

### Task 2: Add missing Dataverse columns (RTF-02)
- **File:** `scripts/create_dataverse_schema.py` (~L277)
- **Change:** Add 6 columns to `_validation_history_columns()`: `fsi_name` (String 200), `fsi_overall_status` (String 50), `fsi_compliance_rate` (Decimal), `fsi_alert_severity` (String 50), `fsi_source` (String 100), `fsi_scan_scope` (String 200)
- **Acceptance:** Flow fallback record creation will succeed against deployed schema

### Task 3: Fix documentation URL slugs (RTF-03)
- **Files:** `src/adaptive-card-caa-alert.json` (~L430), `src/caa-daily-compliance-flow.json` (~L1200), `src/caa-provisioning-hook-flow.json` (~L1108)
- **Change:** Replace `1.11-conditional-access-policies/` with `controls/pillar-1-security/1.11-conditional-access-and-phishing-resistant-mfa/`
- **Acceptance:** URL matches actual MkDocs-generated slug

### Task 4: Create missing private helper scripts (RTF-04)
- **Files:** Create `scripts/private/Connect-GraphSession.ps1`, `scripts/private/Get-ZoneClassification.ps1`, `scripts/private/Test-ParameterValidation.ps1`
- **Change:** Implement or stub the 3 helper functions dot-sourced by `Test-PolicyCompliance.ps1` at lines 101-103
- **Acceptance:** `Test-PolicyCompliance.ps1` loads without file-not-found errors

### Task 5: Align CA policy naming convention (RTF-05)
- **Files:** `scripts/Start-CAAValidationRunbook.ps1` (~L227-236), `scripts/Test-PolicyCompliance.ps1` (~L126-135)
- **Change:** Update expected policy name list from CA-* pattern (e.g., `CA-M365Copilot-AllZones`) to FSI-* pattern (e.g., `FSI-Z1-AgentCreators-BaselineMFA`) matching playbook templates in `docs/playbooks/control-implementations/1.11/conditional-access-agent-templates.md`
- **Acceptance:** Compliance checker validates policies deployed per playbook instructions

## File Manifest

| Action | File |
|--------|------|
| Modify | `src/caa-daily-compliance-flow.json` |
| Modify | `src/caa-provisioning-hook-flow.json` |
| Modify | `src/adaptive-card-caa-alert.json` |
| Modify | `scripts/create_dataverse_schema.py` |
| Create | `scripts/private/Connect-GraphSession.ps1` |
| Create | `scripts/private/Get-ZoneClassification.ps1` |
| Create | `scripts/private/Test-ParameterValidation.ps1` |
| Modify | `scripts/Start-CAAValidationRunbook.ps1` |
| Modify | `scripts/Test-PolicyCompliance.ps1` |

## Verification

1. JSON validity: `python -c "import json; json.load(open('src/caa-daily-compliance-flow.json'))"`
2. PowerShell syntax: `pwsh -c "& { . ./scripts/Test-PolicyCompliance.ps1 -? }" 2>&1`
3. Column name grep: `grep -r "fsi_result_json" src/` returns 0 results (only `fsi_results_json`)
4. URL slug grep: `grep -r "1.11-conditional-access-policies" src/` returns 0 results
