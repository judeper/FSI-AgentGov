Let me check a few things to verify the nav orphan situation and property access patterns.
The files referenced in the nav orphan list actually exist in this worktree — the automated pre-pass finding is a false positive. Now let me compile the review results.
ISSUE_COUNT: 7
CRITICAL:
- docs/playbooks/control-implementations/1.28/powershell-setup.md: Script 3 uses `$agent.DisplayName`, `$agent.PublishStatus`, `$agent.LastModifiedTime`, `$agent.CreatedBy` (lines 309-316) instead of `$agent.Properties.DisplayName`, `$agent.Properties.PublishStatus`, etc. This is inconsistent with the correct `.Properties.` prefix used in 1.27's scripts (e.g., line 81 of 1.27/powershell-setup.md). `Get-AdminPowerAppChatbot` nests most properties under `.Properties`; without the prefix, these fields will return null/empty values.
WARNINGS:
- docs/playbooks/control-implementations/1.28/powershell-setup.md: Inconsistent DLP cmdlet naming — scripts use `Get-DlpPolicy`/`New-DlpPolicy` (lines 61, 212, 290) while Validation Commands section uses `Get-AdminDlpPolicy` (line 409). NEEDS_HUMAN_REVIEW — verify whether these are equivalent cmdlets or have different permissions/behavior.
- docs/playbooks/control-implementations/1.27/powershell-setup.md: Pre-pass flagged `https://yourorg.crm.dynamics.com` (HTTP -1) as broken — this is an intentional placeholder URL, properly documented in surrounding context (line 392-394: "Replace {orgUrl} with your Dataverse organization URL"). False positive from automated check.
- docs/playbooks/control-implementations/1.27/portal-walkthrough.md: Pre-pass flagged `https://copilotstudio.microsoft.com` (HTTP 405) — valid Microsoft portal URL; HTTP 405 is expected when automated tools access authentication-gated portals. False positive.
- docs/playbooks/control-implementations/1.28/portal-walkthrough.md: Pre-pass flagged `https://admin.powerplatform.microsoft.com` (HTTP 405) — same as above, valid portal URL returning expected HTTP 405 to automated crawlers. False positive.
- docs/playbooks/control-implementations/1.28/troubleshooting.md: `Get-AdminPowerAppEnvironmentGroup` cmdlet (line 371) — NEEDS_HUMAN_REVIEW to verify this cmdlet exists in current Microsoft.PowerApps.Administration.PowerShell module versions.
GAPS:
- docs/playbooks/control-implementations/1.28/verification-testing.md: Missing **Attestation Statement Template** section. Both 1.26/verification-testing.md (lines 84-117) and 1.27/verification-testing.md (lines 133-171) include this section with a structured markdown attestation template. Control 1.28 should include an equivalent template for consistency and compliance documentation completeness.
NOTE ON NAV ORPHANS: The automated pre-pass reported ~300 mkdocs.yml nav orphans ("Referenced in mkdocs.yml nav but file not found"). Manual verification confirms all referenced files exist in this worktree (e.g., `docs/controls/`, `docs/framework/`, `docs/reference/`, `docs/playbooks/` directories are fully populated). These are **false positives** from the automated tooling and are excluded from the issue count.
FIXES:
1. docs/playbooks/control-implementations/1.28/powershell-setup.md: In Script 3 (lines 308-316), change `$agent.DisplayName` → `$agent.Properties.DisplayName`, `$agent.PublishStatus` → `$agent.Properties.PublishStatus`, `$agent.LastModifiedTime` → `$agent.Properties.LastModifiedTime`, `$agent.CreatedBy` → `$agent.Properties.CreatedBy` to match the `Get-AdminPowerAppChatbot` object model used correctly in 1.27's scripts.
2. docs/playbooks/control-implementations/1.28/powershell-setup.md: Harmonize DLP cmdlet names — either use `Get-DlpPolicy` consistently throughout or `Get-AdminDlpPolicy` consistently. Verify which is correct for the target module version (NEEDS_HUMAN_REVIEW).
3. docs/playbooks/control-implementations/1.28/verification-testing.md: Add an Attestation Statement Template section (after the Evidence Package section, before Ongoing Monitoring) modeled on the templates in 1.26 and 1.27 verification-testing.md files, covering DLP policy compliance, approval workflow validation, security scan results, and environment promotion pipeline attestation.
STATUS: ISSUES_FOUND