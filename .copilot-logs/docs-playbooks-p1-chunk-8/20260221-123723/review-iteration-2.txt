Let me verify the nav orphan issue and check a few specific items.
All nav-referenced files exist under `docs/` — the pre-pass orphan findings are **false positives** (paths were checked from repo root instead of `docs/` which is mkdocs' `docs_dir`). The 3 broken URLs are also false positives (auth-required portals and intentional placeholder). Let me verify one final detail.
Here's my complete review:
ISSUE_COUNT: 2
CRITICAL:
NONE
WARNINGS:
- docs/playbooks/control-implementations/1.28/powershell-setup.md (line 378): Script 4 prints misleading success message `"✓ Approval workflows enabled for: ..."` (green) immediately after a warning that chatbot approval must be configured manually. The script calls `Set-AdminPowerAppEnvironment` with only `-EnvironmentName` and no actual settings, so nothing is enabled — the success message is deceptive.
- docs/playbooks/control-implementations/1.28/powershell-setup.md (line 149): Script 1 Zone 3 blocked connectors list includes wildcard path `"/providers/Microsoft.PowerApps/apis/*"` which the DLP API likely does not support. NEEDS_HUMAN_REVIEW — could cause silent failure or error at runtime.
GAPS:
NONE
FIXES:
1. docs/playbooks/control-implementations/1.28/powershell-setup.md (line 378): Change `Write-Host "  ✓ Approval workflows enabled for: $($env.DisplayName)" -ForegroundColor Green` to `Write-Host "  ⚠ Manual configuration required for: $($env.DisplayName)" -ForegroundColor Yellow` to avoid implying the script automated what it didn't.
NOTE ON PRE-PASS FINDINGS:
- **MKDOCS NAV ORPHANS (all entries):** FALSE POSITIVES. All referenced files exist under `docs/`. The pre-pass checked paths relative to the repo root, but mkdocs nav entries are relative to `docs_dir` (`docs/`). Verified via glob: `docs/controls/index.md`, `docs/framework/index.md`, `docs/downloads/index.md`, all control files, and all playbook files exist.
- **BROKEN EXTERNAL URLs (all 3):** FALSE POSITIVES. `https://copilotstudio.microsoft.com` and `https://admin.powerplatform.microsoft.com` are legitimate Microsoft portal URLs that return HTTP 405 to unauthenticated automated requests. `https://yourorg.crm.dynamics.com` is an intentional placeholder URL in a code example with explicit "Replace" instructions.
STATUS: ISSUES_FOUND
Now applying the one actionable fix:
The file is read-only in this environment. The fix cannot be applied. Here is the final review:
---
ISSUE_COUNT: 2
CRITICAL:
NONE
WARNINGS:
- docs/playbooks/control-implementations/1.28/powershell-setup.md (line 378): Script 4 prints misleading green `✓` success message "Approval workflows enabled for" but the script makes no actual configuration change — it only calls `Set-AdminPowerAppEnvironment` with `-EnvironmentName` alone. Should use `⚠` yellow to match the preceding warning on line 376.
- docs/playbooks/control-implementations/1.28/powershell-setup.md (line 149): Script 1 Zone 3 blocked connectors uses wildcard `"/providers/Microsoft.PowerApps/apis/*"` — DLP API likely does not support wildcard connector IDs. NEEDS_HUMAN_REVIEW.
GAPS:
NONE
FIXES:
1. docs/playbooks/control-implementations/1.28/powershell-setup.md line 378: Change `Write-Host "  ✓ Approval workflows enabled for: $($env.DisplayName)" -ForegroundColor Green` → `Write-Host "  ⚠ Manual configuration required for: $($env.DisplayName)" -ForegroundColor Yellow` (could not apply — filesystem read-only)
NOTE ON SECTION A PRE-PASS FINDINGS — ALL FALSE POSITIVES:
- **MKDOCS NAV ORPHANS (all ~300 entries):** Files exist under `docs/`. Pre-pass checked from repo root; mkdocs resolves nav paths relative to `docs_dir` (`docs/`). Confirmed via glob: `docs/controls/index.md` ✓, `docs/framework/index.md` ✓, `docs/downloads/index.md` ✓, all pillar control files ✓, all playbook files ✓.
- **3 BROKEN EXTERNAL URLs:** `copilotstudio.microsoft.com` and `admin.powerplatform.microsoft.com` are auth-gated portals (HTTP 405 expected from automated tooling). `yourorg.crm.dynamics.com` is an intentional placeholder in a code example with "Replace" instructions.
STATUS: ISSUES_FOUND