ISSUE_COUNT: 7
CRITICAL:
- docs/playbooks/control-implementations/1.28/powershell-setup.md: Script 3 uses `$agent.DisplayName`, `$agent.PublishStatus`, `$agent.LastModifiedTime`, `$agent.CreatedBy` (lines 309-316) instead of `$agent.Properties.DisplayName`, `$agent.Properties.PublishStatus`, etc. Same cmdlet (`Get-AdminPowerAppChatbot`) is used correctly with `.Properties.` prefix in 1.27/powershell-setup.md (line 80). Without the prefix these fields return null/empty values. (`ChatBotName` at line 310 is correct — it is a top-level property.)
WARNINGS:
- docs/playbooks/control-implementations/1.28/powershell-setup.md: DLP cmdlet mismatch — scripts use `Get-DlpPolicy`/`New-DlpPolicy` (lines 61, 212, 290) while Validation Commands section uses `Get-AdminDlpPolicy` (line 409). These are different cmdlets from different modules (`Microsoft.PowerApps.PowerShell` vs `Microsoft.PowerApps.Administration.PowerShell`). NEEDS_HUMAN_REVIEW — since the declared module is the Administration module, the `-Admin-` variants are likely correct, but do not auto-fix; human must confirm which module is targeted.
- docs/playbooks/control-implementations/1.27/portal-walkthrough.md: Pre-pass flagged `https://copilotstudio.microsoft.com` (HTTP 405) — valid auth-gated Microsoft portal URL. False positive from automated check.
- docs/playbooks/control-implementations/1.28/portal-walkthrough.md: Pre-pass flagged `https://admin.powerplatform.microsoft.com` (HTTP 405) — valid auth-gated Microsoft portal URL. False positive from automated check.
- docs/playbooks/control-implementations/1.27/powershell-setup.md: Pre-pass flagged `https://yourorg.crm.dynamics.com` (HTTP -1) — intentional placeholder URL, documented in context (lines 392-394). False positive.
- docs/playbooks/control-implementations/1.28/troubleshooting.md: `Get-AdminPowerAppEnvironmentGroup` cmdlet (line 371) — NEEDS_HUMAN_REVIEW to verify this cmdlet exists in current Microsoft.PowerApps.Administration.PowerShell module versions.
GAPS:
- docs/playbooks/control-implementations/1.28/verification-testing.md: Missing Attestation Statement Template section. Both 1.26 (lines 84-117) and 1.27 (lines 133-171) include structured attestation templates. 1.28 jumps from Evidence Package (line 383) directly to Ongoing Monitoring (line 415) with no attestation section between them.
FIXES:
1. docs/playbooks/control-implementations/1.28/powershell-setup.md: In Script 3 (lines 308-316), change `$agent.DisplayName` → `$agent.Properties.DisplayName`, `$agent.PublishStatus` → `$agent.Properties.PublishStatus`, `$agent.LastModifiedTime` → `$agent.Properties.LastModifiedTime`, `$agent.CreatedBy` → `$agent.Properties.CreatedBy`. Leave `$agent.ChatBotName` unchanged. This is unambiguous and should be applied first.
2. docs/playbooks/control-implementations/1.28/verification-testing.md: Add an Attestation Statement Template section between Evidence Package and Ongoing Monitoring, modeled on 1.26/1.27 templates, covering DLP policy compliance, approval workflow validation, security scan results, and environment promotion pipeline attestation. Independent additive content, no risk.
3. docs/playbooks/control-implementations/1.28/powershell-setup.md: DEFERRED — DLP cmdlet harmonization requires human verification. Likely correct cmdlets are `New-AdminDlpPolicy`/`Get-AdminDlpPolicy` (Administration module), but scripts may intentionally target the non-admin module. Do not auto-fix.
STATUS: ISSUES_FOUND