Now I have full evidence. Let me check one more thing — whether 1.28/powershell-setup.md has an API availability disclaimer like 1.27.
Got everything needed. Here's the analysis:
---
**HIGH_RISK_FIXES:**
- NONE. All three proposed fixes (1–3) are scoped to inline PowerShell code blocks within markdown files. They do not alter filenames, headings, cross-references, or `mkdocs.yml` nav entries. Zero risk to build or link integrity.
**SEVERITY_CORRECTIONS:**
- Fix 3 (`Add-AdminPowerAppEnvironmentToPolicy`): **Downgrade from WARNING to INFO.** The same file already uses the correct `Set-AdminDlpPolicy` approach in Script 1 (lines 68–155) for connector classification. `Add-AdminPowerAppEnvironmentToPolicy` is used only for *environment assignment* to policies (lines 208–238) — a distinct operation from connector classification. This cmdlet maps to a documented Power Platform Admin API surface (`Add-AdminPowerAppEnvironment*`). It is plausible, follows naming conventions, and is used correctly in context. The review's suggestion to "replace with `Set-AdminDlpPolicy` approach" conflates two different operations. **Recommend: leave as-is or add a verification comment, not a cmdlet replacement.**
**FIX_ORDERING:**
- Fixes 1 and 2 are independent — apply in any order. Both are simple find-and-replace of `Get-PowerAppChatBot` → `Get-AdminPowerAppChatbot`.
- Fix 1 (powershell-setup.md property access patterns) should be applied *after* the cmdlet rename in the same file, since both touch adjacent lines in Script 3.
- Fix 3 should be deferred pending human verification — it is lower severity than assessed and the cmdlet may be correct.
**OVER_ENGINEERED:**
- Fix 1's property access change (`$agent.DisplayName` → `$agent.Properties.DisplayName`). The review flags this for "consistency with 1.27 scripts," but 1.28's Script 3 does not have 1.27's API availability disclaimer, and the object schema returned by `Get-AdminPowerAppChatbot` may legitimately vary by `-EnvironmentName` query context. The property access pattern difference is suspicious but the reviewer correctly tagged it `NEEDS_HUMAN_REVIEW` — good. **However**, proposing both a cmdlet rename *and* a property schema change in one fix couples two independent uncertainties. These should be separate fixes: cmdlet rename (high confidence) and property path change (needs tenant verification).
- Fix 3's suggestion to replace with `Set-AdminDlpPolicy` is over-engineered — the file already uses `Set-AdminDlpPolicy` for a *different* purpose. Replacing would break the environment-assignment logic.
**CRITIQUE_SUMMARY:** The review correctly identified the `Get-PowerAppChatBot` inconsistency as the real issue (Fixes 1–2 are safe, high-confidence corrections). However, Fix 3 conflates two distinct DLP operations and should be downgraded; the property-path portion of Fix 1 should be decoupled from the cmdlet rename to avoid compounding risk.