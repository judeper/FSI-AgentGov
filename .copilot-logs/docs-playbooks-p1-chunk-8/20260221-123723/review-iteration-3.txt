Now let me verify the cmdlet inconsistency I spotted between 1.27 and 1.28 files.
Confirmed: 1.26 and 1.27 consistently use `Get-AdminPowerAppChatbot`; 1.28 files use the non-standard `Get-PowerAppChatBot`. Now producing the review report.
ISSUE_COUNT: 7
CRITICAL:
- NONE
WARNINGS:
- 1.28/powershell-setup.md: Script 3 uses `Get-PowerAppChatBot` (4 occurrences) instead of `Get-AdminPowerAppChatbot` — inconsistent with 1.26/1.27 scripts and likely a non-existent cmdlet. Also accesses properties directly (`$agent.DisplayName`) rather than via `.Properties.DisplayName` as in 1.27 scripts. NEEDS_HUMAN_REVIEW
- 1.28/troubleshooting.md: Uses `Get-PowerAppChatBot` in diagnostic commands (Issues 2 and 10, lines 93 and 574) — same cmdlet name issue. NEEDS_HUMAN_REVIEW
- 1.28/powershell-setup.md: Script 2 uses `Add-AdminPowerAppEnvironmentToPolicy` (3 occurrences) which may not be a standard Power Platform cmdlet — no matching cmdlet found in standard module documentation. NEEDS_HUMAN_REVIEW
- 1.27/portal-walkthrough.md: `https://copilotstudio.microsoft.com` returns HTTP 405 (Section A — false positive; auth-gated portal does not respond to HEAD requests)
- 1.27/powershell-setup.md: `https://yourorg.crm.dynamics.com` returns HTTP -1 (Section A — false positive; intentional placeholder URL, clearly documented as such in context)
- 1.28/portal-walkthrough.md: `https://admin.powerplatform.microsoft.com` returns HTTP 405 (Section A — false positive; auth-gated portal)
- mkdocs.yml: 300+ nav orphans (Section A — worktree limitation; the 9 batch files exist at correct paths; remaining orphans reference docs that exist in the main repo but not in this partial worktree)
GAPS:
- NONE
FIXES:
1. 1.28/powershell-setup.md: Replace `Get-PowerAppChatBot` with `Get-AdminPowerAppChatbot` in Script 3 (lines 275, 288) and update property access to use `.Properties` prefix (e.g., `$agent.Properties.DisplayName`) for consistency with 1.26/1.27 scripts. NEEDS_HUMAN_REVIEW — verify property schema before applying.
2. 1.28/troubleshooting.md: Replace `Get-PowerAppChatBot` with `Get-AdminPowerAppChatbot` in diagnostic code blocks (lines 93, 574). NEEDS_HUMAN_REVIEW.
3. 1.28/powershell-setup.md: Verify `Add-AdminPowerAppEnvironmentToPolicy` cmdlet exists in current module version; if not, replace with `Set-AdminDlpPolicy` approach or add a disclaimer note similar to 1.27's API availability warnings. NEEDS_HUMAN_REVIEW.
4. Broken external URLs (items 4-6): No fix needed — all are legitimate auth-gated Microsoft portals or intentional placeholder URLs.
5. Nav orphans: No fix within this batch — infrastructure issue from partial worktree.
STATUS: ISSUES_FOUND