Now let me check the property access patterns more closely — the review claims 1.28 uses `$agent.DisplayName` while 1.27 uses `$agent.Properties.DisplayName`.
Now let me check whether the 1.28 files have any API availability disclaimer like the 1.27 files do, and also check for any additional issues the reviewer might have missed.
Now I have enough evidence to produce the audit. Let me also quickly check if 1.28 has any other issues the reviewer may have missed.
Here is the audit result:
---
**FALSE_POSITIVES:**
- NONE. All 7 claimed issues are verified as real or correctly identified as false positives by the reviewer itself (items 4–6 for auth-gated URLs and item 7 for worktree nav orphans were correctly self-dismissed).
**MISSED_ISSUES:**
- **1.28/powershell-setup.md: Missing API availability disclaimer.** Controls 1.26 and 1.27 both include an `!!! danger "API Availability — Verify Before Use"` admonition warning that cmdlets are based on anticipated API schema. Control 1.28's powershell-setup.md has no such disclaimer despite using equally speculative cmdlets (`Get-PowerAppChatBot`, `Add-AdminPowerAppEnvironmentToPolicy`). This is a consistency gap the reviewer should have flagged.
- **1.28/powershell-setup.md: Missing `-ErrorAction SilentlyContinue` on `Get-PowerAppChatBot` calls.** Controls 1.26 and 1.27 consistently use `-ErrorAction SilentlyContinue` on all `Get-AdminPowerAppChatbot` calls to handle environments where the cmdlet may not return data. The 1.28 Script 3 calls at lines 275 and 288 omit this, which would cause terminating errors in environments without chatbot support.
- **1.28/powershell-setup.md: `$agent.PublishStatus` (line 302) and `$agent.ChatBotName` (lines 288, 301) property access.** The reviewer noted `$agent.DisplayName` as inconsistent with 1.27's `.Properties.DisplayName` pattern, but didn't flag `PublishStatus` and `ChatBotName` which are also accessed directly rather than via `.Properties`. These should either all use `.Properties` prefix or be documented as using a different object model.
**FIX_CONCERNS:**
- **Fix 1 (property access):** The reviewer correctly identified the `.Properties.` prefix inconsistency and marked it NEEDS_HUMAN_REVIEW. However, the fix should also cover `$agent.PublishStatus` (line 302) and `$agent.ChatBotName` (lines 288, 301), not just `$agent.DisplayName`. The scope of the property access fix is incomplete.
- **Fix 3 (`Add-AdminPowerAppEnvironmentToPolicy`):** The suggestion to replace with `Set-AdminDlpPolicy` is reasonable since `Set-AdminDlpPolicy` is already used in Script 1 of the same file (lines 71, 112, 155). However, `Add-AdminPowerAppEnvironmentToPolicy` may actually be a valid cmdlet for environment-scoping DLP policies (distinct from configuring connector groups). The NEEDS_HUMAN_REVIEW flag is appropriate here — the fix should not be auto-applied.
**CRITIQUE_SUMMARY:** The review correctly identified the core cmdlet naming inconsistency and appropriately flagged speculative fixes as NEEDS_HUMAN_REVIEW. However, it missed two related issues: the absence of an API availability disclaimer (present in 1.26/1.27 but missing in 1.28) and missing `-ErrorAction SilentlyContinue` error handling, both of which represent consistency gaps that compound the cmdlet naming problem.