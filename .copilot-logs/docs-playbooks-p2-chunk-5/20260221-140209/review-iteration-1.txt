Now I have all 15 files. Let me verify the specific code issue in 2.18/powershell-setup.md.
Good — no FSI prohibited language found. Here is my complete review:
---
ISSUE_COUNT: 10
CRITICAL:
- docs/playbooks/control-implementations/2.18/powershell-setup.md: `Evaluate-Criterion` function defined at line 131 but invoked at line 77 inside the same script block. PowerShell scripts execute top-to-bottom; this will throw `CommandNotFoundException` at runtime. The function must be moved before its first call.
WARNINGS:
- docs/playbooks/control-implementations/2.19/powershell-setup.md: Uses `Connect-CrmOnline` and `Get-CrmRecordsByFetch` from Microsoft.Xrm.Data.PowerShell module, which may be superseded by `ServiceClient`-based connections. NEEDS_HUMAN_REVIEW.
- docs/playbooks/control-implementations/2.16/portal-walkthrough.md: `https://copilotstudio.microsoft.com` returns HTTP 405 — this is expected for a SPA requiring authentication; the URL is correct for documentation. No fix needed.
- docs/playbooks/control-implementations/2.16/portal-walkthrough.md: Placeholder URLs (`https://company.sharepoint.com/sites/...`) flagged as broken — these are intentional example URLs in a YAML config block. No fix needed.
- docs/playbooks/control-implementations/2.16/powershell-setup.md: Same placeholder URL (`https://company.sharepoint.com/sites/research`) in PowerShell examples. No fix needed.
- docs/playbooks/control-implementations/2.19/powershell-setup.md: Placeholder URL (`https://yourorg.crm.dynamics.com`) in example code. No fix needed.
- docs/playbooks/control-implementations/2.16/powershell-setup.md: `[(Get-Date]` flagged as broken internal link — false positive; `$(Get-Date ...)` is inside fenced PowerShell code blocks (lines 100, 155). No fix needed.
- mkdocs.yml nav references ~300 files not present in this worktree — this is a systemic worktree-subset artifact, not a documentation defect. No fix within this batch scope.
GAPS:
- NONE
FIXES:
1. docs/playbooks/control-implementations/2.18/powershell-setup.md: Move the `Evaluate-Criterion` function definition (lines 131-149) to before the `foreach ($testFile ...)` loop (before line 58), so the function exists when called at line 77.
2. docs/playbooks/control-implementations/2.19/powershell-setup.md: NEEDS_HUMAN_REVIEW — verify `Connect-CrmOnline` / `Get-CrmRecordsByFetch` (Microsoft.Xrm.Data.PowerShell) are still the recommended Dataverse connection cmdlets, or update to `ServiceClient`-based approach.
STATUS: ISSUES_FOUND