ISSUE_COUNT: 7
CRITICAL:
NONE
WARNINGS:
- docs/playbooks/control-implementations/2.16/portal-walkthrough.md: `https://copilotstudio.microsoft.com` returns HTTP 405 — this is expected behavior for a SPA that redirects to auth; URL is valid
- docs/playbooks/control-implementations/2.16/portal-walkthrough.md: `https://company.sharepoint.com/sites/research/library` and `https://company.sharepoint.com/sites/products/docs` are intentional placeholder URLs inside a YAML example block (lines 106-107)
- docs/playbooks/control-implementations/2.16/powershell-setup.md: `https://company.sharepoint.com/sites/research` is an intentional placeholder URL inside a `.EXAMPLE` comment block (line 40)
- docs/playbooks/control-implementations/2.19/powershell-setup.md: `https://yourorg.crm.dynamics.com` is an intentional placeholder URL inside a code example (line 46)
- docs/playbooks/control-implementations/2.16/powershell-setup.md: Two `[(Get-Date]` "broken internal links" are false positives — these are PowerShell `(Get-Date)` expressions inside fenced code blocks, not markdown links
- MKDOCS NAV ORPHANS: The ~300 orphaned nav entries are a worktree artifact (this partial worktree doesn't contain the full `docs/` tree); the 15 in-scope files exist at their correct paths and are not individually broken
GAPS:
NONE
FIXES:
1. No code changes needed. All 5 broken external URLs are intentional placeholder/example URLs (`company.sharepoint.com`, `yourorg.crm.dynamics.com`) or valid SPA endpoints (`copilotstudio.microsoft.com`). These are standard documentation patterns for FSI example configurations.
2. No code changes needed. The 2 `[(Get-Date]` internal link findings are PowerShell syntax inside triple-backtick fenced code blocks — not markdown links.
3. No code changes needed for nav orphans. The worktree contains only a file subset; mkdocs.yml references the full site. This resolves when files are merged to the complete branch.
STATUS: CLEAN