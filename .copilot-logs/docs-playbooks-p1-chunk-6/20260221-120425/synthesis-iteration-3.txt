ISSUE_COUNT: 2
CRITICAL:
- NONE
WARNINGS:
- docs/playbooks/control-implementations/1.21/portal-walkthrough.md (lines 39-47, 54-57): KQL queries reference the `AuditLogs` Sentinel table and `TargetResources` field to detect adversarial prompt text (e.g., "ignore previous", "jailbreak") and Base64-encoded payloads. `AuditLogs` contains Entra ID audit events, not Copilot conversation content. Additionally, `TargetResources` is a JSON-structured field containing resource objects, not raw user input — even after correcting the table, the field reference and regex target would need updating. NEEDS_HUMAN_REVIEW: verify correct Sentinel table (e.g., `CopilotInteractions`, `CloudAppEvents` with `RawEventData`, or the current M365 Copilot data connector table) and the specific field containing user prompt text.
- docs/playbooks/control-implementations/1.21/powershell-setup.md (lines 90-95, 259-263): Same KQL table/field issue repeated in Sentinel analytics rule creation. Notably, the PowerShell sections in this same file correctly use `-RecordType "CopilotInteraction"` (lines 54-55, 219-220), confirming the author knew the correct data source — the KQL blocks are internally inconsistent with the PowerShell blocks. NEEDS_HUMAN_REVIEW.
GAPS:
- NONE
FIXES:
1. docs/playbooks/control-implementations/1.21/portal-walkthrough.md: Replace `AuditLogs` table and `TargetResources` field in KQL blocks (Steps 3-4) with the correct Sentinel table and field for M365 Copilot interaction content. Consult current Microsoft Sentinel schema documentation for the Copilot data connector before committing — deploying an unvalidated KQL correction is worse than leaving the current NEEDS_HUMAN_REVIEW marker. Do NOT commit a table swap without verification against live Sentinel schema.
2. docs/playbooks/control-implementations/1.21/powershell-setup.md: Apply the same KQL table/field correction in the Sentinel analytics rule query (lines 90-95) and complete configuration script KQL block (lines 259-263). This fix is **coupled** with Fix 1 — both files must use the identical replacement table/field and should be committed together to avoid cross-file inconsistency.
STATUS: ISSUES_FOUND