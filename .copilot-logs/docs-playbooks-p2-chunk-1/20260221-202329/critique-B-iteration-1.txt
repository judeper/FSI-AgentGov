Now let me check the heading levels around the duplicate steps to fully assess Fix 1.
I've examined all affected files. Here is my risk and impact analysis:
---
**HIGH_RISK_FIXES:**
NONE
All five proposed fixes are confined to content/formatting within individual playbook files. None modify filenames, cross-reference targets, mkdocs.yml navigation entries, or control structure. Zero risk of breaking `mkdocs build --strict`.
**SEVERITY_CORRECTIONS:**
- **CRITICAL #2 (date inconsistency in 2.1):** DOWNGRADE TO WARNING. Header "January 2026" vs footer "February 2026" is a minor metadata discrepancy. It does not cause administrator misconfiguration or compliance risk. The duplicate step numbering (CRITICAL #1) is correctly classified since it directly impacts task execution.
- **CRITICAL #3 (Azure AD → Entra ID in 2.4):** DOWNGRADE TO WARNING. The deprecated term appears in a service health alert configuration list (line 116), not in a procedural step or a role name. Admins will still understand "Azure Active Directory" in the M365 Admin Center health preferences context. It's a correctness issue, not a confusion/misconfig risk.
**FIX_ORDERING:**
- Fix 1 (renumber steps) and Fix 2 (date alignment) both target `2.1/portal-walkthrough.md` — apply in a single edit pass. Fix 1 first (structural), then Fix 2 (metadata).
- Fixes 3, 4, and 5 are fully independent of each other and of Fixes 1-2. No ordering constraints.
- **Note on Fix 1 scope:** The second set of steps uses `##` headings (lines 257+) while the first set uses `###` headings (lines 117+). Renumbering alone leaves a heading-level inconsistency. The fix should also normalize the second set to `###` to match. Additionally, the second set (tenant-level settings) appears *after* the Validation section (line 239) that already references those steps — the structural placement is awkward but renumbering alone is the safe minimal fix.
**OVER_ENGINEERED:**
- Fix 5 (remove duplicate `---` across 5 files): Not over-engineered, but LOW VALUE. The double horizontal rules are cosmetic — they render as two thin lines with a gap. Zero user impact. This is a "while we're here" fix. Acceptable to include but should not block the batch if it creates merge noise.
**CRITIQUE_SUMMARY:** The review is accurate and well-triaged; all findings are confirmed in source. Two of three CRITICAL items should be downgraded to WARNING (only the duplicate step numbering truly risks administrator error), and Fix 1 should also normalize the `##` → `###` heading level on the second set of steps to avoid leaving a structural inconsistency behind.