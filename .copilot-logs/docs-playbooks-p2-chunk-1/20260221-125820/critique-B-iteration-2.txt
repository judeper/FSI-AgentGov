Analysis complete. Here's the risk assessment:
---
**HIGH_RISK_FIXES:**
- NONE
**SEVERITY_CORRECTIONS:**
- Fix 4 (PowerShell property path): WARNING is **under-classified** — should be WARNING with elevated priority or near-CRITICAL. The grep results show `Properties.protectionLevel` is used by 4 controls (1.4, 1.8, 2.1, 2.15) while `Properties.governanceConfiguration.protectionLevel` is used only by 2.2. If 2.2's path is wrong, its scripts silently return incorrect managed-environment status — a compliance-evidence gap for FSI. The NEEDS_HUMAN_REVIEW flag is operationally correct, but the severity label should reflect that one code path definitively produces wrong audit evidence.
**FIX_ORDERING:**
- Fixes 1, 2, 3 are fully independent — execute in any order or in parallel
- Fix 4 blocks on human/tenant validation — do not apply until the correct property path is confirmed against a live tenant
- Fix 5 depends on nothing but should be deferred (see below)
**OVER_ENGINEERED:**
- **Fix 5 (footer nav style normalization across 2.3/2.4):** This reformats 6 files for cosmetic consistency. The 2.3/2.4 `## Related Playbooks` bullet-list style is internally consistent within each control folder and functionally equivalent to the inline format. Both portal-walkthrough files in 2.3/2.4 already use inline style, so there's no confusion within a single file. Touching 6 files to unify a cross-control aesthetic introduces unnecessary diff noise and typo risk for zero functional benefit. **Recommend: drop from this batch.** If style normalization is desired, it should be a separate dedicated pass across all ~284 playbooks, not a spot fix on 6 files.
**CRITIQUE_SUMMARY:** Fixes 1–3 are safe, low-risk metadata corrections that should ship. Fix 4 is correctly flagged for human review but under-severity-classified — the 4:1 usage ratio strongly suggests 2.2 is the outlier with the wrong API path, making this a latent script-correctness bug rather than a mere inconsistency. Fix 5 is scope creep and should be dropped from this batch.