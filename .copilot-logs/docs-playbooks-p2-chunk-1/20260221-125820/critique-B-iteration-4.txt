Now I have full context. Here's my risk analysis:
---
**HIGH_RISK_FIXES:**
- **Fix 1 (PowerShell property path):** Correctly flagged NEEDS_HUMAN_REVIEW. Both paths are used consistently within their respective control playbooks (2.1 uses `Properties.protectionLevel` in 6 places across 3 files; 2.2 uses `Properties.governanceConfiguration.protectionLevel` in 6 places across 3 files). Fixing the wrong side silently breaks all scripts in that control's playbooks. **Do not change either without live tenant verification.** Zero cross-references exist between 2.1 and 2.2 playbooks, so the fix is isolated to one control set, but picking the wrong one is a 6-file, silent-failure regression.
- **Fix 2 (Step reordering in 2.1/portal-walkthrough.md):** Steps 11–14 are **tenant-level** operations (Restrict Environment Creation, Tenant Isolation, Environment Routing, Security Groups) that are architecturally distinct from Steps 1–10 which are **per-environment** Managed Environment settings. The "Configuration by Governance Level" table at line 154 covers only Managed Environment settings (sharing, solution checker, usage insights) — i.e., Steps 1–10. Moving Steps 11–14 before this table would intermingle tenant-scope and environment-scope configurations, potentially creating *more* confusion, not less. The current placement may be intentional section scoping, not a sequencing error.
**SEVERITY_CORRECTIONS:**
- **Fix 2 should be downgraded from WARNING to GAP/SUGGESTION.** The step ordering is defensible as-is (tenant-level config after environment-level config). If addressed, the correct fix is adding a `## Tenant-Level Configuration` heading before Step 11 to make the scope break explicit — not reordering content.
- **Fix 4 (2.3 verification-testing.md) severity is accurate as GAP** but the comparison framing is misleading. The file has 20+ verification items across rollback triggers, decision matrices, and procedures — it's not as thin as "only 5 items" suggests. The gap is structural (no `TC-2.3-XX` test case IDs, no automated script), not a content deficit.
**FIX_ORDERING:**
- **Fix 3 first** (CHG-2025 → CHG-2026): Zero risk, zero dependencies, 1-line change. Ship immediately.
- **Fix 2 second** (but as a heading insertion, not a reorder): Add `## Tenant-Level Configuration` heading before Step 11. No content movement, no cross-reference risk.
- **Fix 4 third** (2.3 verification-testing.md expansion): New content authoring — requires careful PowerShell scripting. No dependencies on other fixes but needs subject matter review.
- **Fix 1 last** (property path): Blocked on live tenant verification. Do not execute until human confirms correct path.
**OVER_ENGINEERED:**
- **Fix 4 is over-engineered as stated.** Adding "automated PowerShell validation script" and a full test case table with IDs TC-2.3-01 through TC-2.3-N is new feature development, not a documentation fix. Control 2.3 (Change Management) has a fundamentally different verification profile than 2.1/2.2 — it validates process and governance (approval gates, audit trails, rollback procedures), not portal checkbox states. Blindly pattern-matching 2.1/2.2's automated script approach risks producing PowerShell that checks pipeline API endpoints which may not exist in all tenant configurations. **Right-size:** Add test case IDs to existing checklist items and a note that automated validation depends on pipeline API availability. Skip the full script authoring.
- **Fix 2 is over-engineered.** Restructuring step order across 130+ lines of content is high-churn for a readability concern. A single heading insertion (`## Tenant-Level Configuration`) before Step 11 achieves the same clarity with ~1 line changed instead of moving 4 sections.
**CRITIQUE_SUMMARY:** The review is thorough and the critical PowerShell property path inconsistency is a genuine finding, but Fix 2 misdiagnoses intentional scope separation as a sequencing error, and Fix 4 conflates structural formatting gaps with missing content — right-sizing both to minimal interventions (heading insertion + test case IDs on existing items) reduces churn and risk without sacrificing value.