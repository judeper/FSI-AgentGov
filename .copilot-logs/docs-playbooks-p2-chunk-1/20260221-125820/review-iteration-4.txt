Now let me read the remaining files (2.3 and 2.4 playbooks).
Let me check the file structure and verify the nav orphan situation.
Now I have all the information needed for my review. Let me compile the findings.
ISSUE_COUNT: 5
CRITICAL:
- 2.1/powershell-setup.md, 2.1/verification-testing.md, 2.1/troubleshooting.md: PowerShell property path `$_.Properties.protectionLevel` is inconsistent with the 2.2 playbooks which use `$_.Properties.governanceConfiguration.protectionLevel`. Only one path is correct on the `Get-AdminPowerAppEnvironment` return object. All 2.1 scripts using the shorter path will silently return `$null` if the nested path is correct (or vice versa). NEEDS_HUMAN_REVIEW — verify against a live tenant which property path returns the managed environment protection level.
WARNINGS:
- 2.1/portal-walkthrough.md: Steps 11–14 (Restrict Environment Creation, Configure Environment Routing, Enable Tenant Isolation, Assign Environment Security Groups) appear **after** the "FSI Example Configuration" YAML block and "Configuration by Governance Level" table, breaking the linear step sequence. Readers following top-to-bottom will miss these steps.
- 2.3/powershell-setup.md (line 335): Example `ChangeRequestId` value is `"CHG-2025-001234"` — uses year 2025 while the document is dated January 2026. Should use `"CHG-2026-001234"` for consistency.
GAPS:
- 2.3/verification-testing.md: Missing automated PowerShell validation script. Both 2.1 and 2.2 verification-testing playbooks include a dedicated `## Automated Validation Script` section with runnable PowerShell; 2.3 has none.
- 2.3/verification-testing.md: Verification checklist has only 5 high-level items compared to 2.1 (8 test cases with IDs TC-2.1-01 through TC-2.1-08) and 2.2 (10 test cases with IDs TC-2.2-01 through TC-2.2-10). Missing test case table with IDs, specific pass/fail columns, and scenarios for pipeline deployment, approval gate enforcement, and rollback validation.
FIXES:
1. 2.1/powershell-setup.md, 2.1/troubleshooting.md, 2.1/verification-testing.md: Verify correct PowerShell property path for managed environment detection. If `Properties.governanceConfiguration.protectionLevel` (used in 2.2) is correct, update all `Properties.protectionLevel` references in 2.1 playbooks. If `Properties.protectionLevel` (used in 2.1) is correct, update all 2.2 playbooks. NEEDS_HUMAN_REVIEW against live tenant.
2. 2.1/portal-walkthrough.md: Move Steps 11–14 to appear before the "Configuration by Governance Level" table and "FSI Example Configuration" sections, so all configuration steps are contiguous.
3. 2.3/powershell-setup.md (line 335): Change `"CHG-2025-001234"` to `"CHG-2026-001234"`.
4. 2.3/verification-testing.md: Add automated PowerShell validation script section and expand test case table with specific IDs (TC-2.3-01 through TC-2.3-N) covering pipeline existence, approval gate configuration, change record audit trail, and rollback procedure validation.
**Section A notes — Broken External URLs:** All 8 flagged URLs are false positives. Three are admin portal URLs (`admin.powerplatform.microsoft.com`, `copilotstudio.microsoft.com`) that return HTTP 405 to automated crawlers but work in browsers. One is an API endpoint pattern in a code block (`api.bap.microsoft.com`). Four are intentional placeholder/example URLs in code samples (`dev-fsi.crm.dynamics.com`, `prod-fsi.crm.dynamics.com`, `yourorg.crm.dynamics.com`, `yourorg-dr.crm.dynamics.com`, `org-dr.crm.dynamics.com`). No fixes needed.
**Section A notes — MkDocs Nav Orphans:** All 300+ flagged orphans are false positives. Manual verification confirms the referenced files exist at correct paths under `docs/` (the default `docs_dir`). The automated checker appears to have looked for files relative to the repo root rather than the `docs/` directory.
STATUS: ISSUES_FOUND