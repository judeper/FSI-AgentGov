ISSUE_COUNT: 6
CRITICAL:
- 2.1/powershell-setup.md, 2.1/verification-testing.md, 2.1/troubleshooting.md: PowerShell property path `$_.Properties.protectionLevel` (used in 6 places across 3 files in 2.1) is inconsistent with `$_.Properties.governanceConfiguration.protectionLevel` (used in 6 places across 3 files in 2.2). Only one path is correct on the `Get-AdminPowerAppEnvironment` return object. The incorrect set will silently return `$null`. No cross-references exist between the two control sets, so the fix is isolated to one side — but picking the wrong side is a 6-file silent-failure regression. NEEDS_HUMAN_REVIEW — verify against a live tenant before changing either side.
WARNINGS:
- 2.3/powershell-setup.md (line 335): Example `ChangeRequestId` value `"CHG-2025-001234"` uses year 2025 while the document footer reads "Updated: January 2026." Should use `"CHG-2026-001234"` for consistency.
GAPS:
- 2.1/portal-walkthrough.md: Steps 11–14 (tenant-level operations: Restrict Environment Creation, Tenant Isolation, Environment Routing, Security Groups) appear after the "Configuration by Governance Level" table and YAML block, which cover only the per-environment Managed Environment settings of Steps 1–10. The scope break from environment-level to tenant-level configuration is implicit and may confuse readers following top-to-bottom, but reordering would intermingle two architecturally distinct scopes. A section heading would clarify the intent.
- 2.3/verification-testing.md: Missing `## Automated Validation Script` section with runnable PowerShell. Both 2.1 (line 109) and 2.2 (line 126) include this section. The file contains 20+ verification items across rollback triggers, decision matrices, and procedures, so it is not thin — but it lacks a consolidated runnable script for automated validation.
- 2.3/verification-testing.md: Missing structured test case table with IDs (TC-2.3-01 through TC-2.3-N), pass/fail columns, and specific scenarios. 2.1 has TC-2.1-01 through TC-2.1-08; 2.2 has TC-2.2-01 through TC-2.2-10. 2.3 has a basic 5-row table without IDs. Key missing scenarios: pipeline deployment validation, approval gate enforcement, change record audit trail, rollback procedure validation.
- 2.4/verification-testing.md: Same two consistency gaps as 2.3 — no automated PowerShell validation script section and no TC-2.4-xx structured test case table. Both 2.3 and 2.4 are process-oriented controls (vs. 2.1/2.2's configuration focus), so this may reflect an intentional structural difference, but if the 2.1/2.2 pattern is the standard, both should be flagged.
FIXES:
1. 2.1 or 2.2 playbooks (powershell-setup.md, verification-testing.md, troubleshooting.md): Verify correct PowerShell property path for managed environment protection level against a live tenant. Update whichever control set uses the wrong path — do NOT change either side without tenant verification. NEEDS_HUMAN_REVIEW.
2. 2.1/portal-walkthrough.md: Add a `## Tenant-Level Configuration` heading before Step 11 (line ~239) to make the scope transition from per-environment to tenant-level settings explicit. Do not reorder steps — the current grouping is architecturally sound.
3. 2.3/powershell-setup.md (line 335): Change `"CHG-2025-001234"` to `"CHG-2026-001234"`.
4. 2.3/verification-testing.md: Add `## Automated Validation Script` section with runnable PowerShell covering pipeline existence, approval gate configuration, change record audit trail, and rollback readiness.
5. 2.3/verification-testing.md: Add structured test case table with IDs TC-2.3-01 through TC-2.3-N, pass/fail columns, and specific scenarios for pipeline deployment, approval gates, audit trail, and rollback.
6. 2.4/verification-testing.md: Add automated validation script section and structured test case table (TC-2.4-01 through TC-2.4-N), consistent with the pattern established in 2.1 and 2.2.
STATUS: ISSUES_FOUND