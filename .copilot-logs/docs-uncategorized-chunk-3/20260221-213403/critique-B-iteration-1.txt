Let me verify the actual control filenames and cross-reference the claims.
All data verified. Here's the analysis:
---
**HIGH_RISK_FIXES:**
- NONE. All 7 fixes target files under `reports/audit/` which are **not in mkdocs.yml navigation** and not published to GitHub Pages. No fix can break cross-references or the mkdocs build. These are internal audit artifacts only.
**SEVERITY_CORRECTIONS:**
- **02-inventory.md wrong titles**: Downgrade from CRITICAL to **HIGH**. The inventory is an internal audit reference file, not a published control doc. Wrong titles undermine report credibility but have zero runtime/deployment impact. The actual control files (`docs/controls/`) have correct titles and filenames — confirmed via filesystem.
- **06-remediation-backlog.md wrong filenames (items 11, 16, 25, 26)**: Downgrade from CRITICAL to **HIGH**. The backlog is already marked "ALL 87 ITEMS RESOLVED" with commit hashes. Wrong filenames are historical metadata errors in the report only — the actual remediation commits targeted the correct files. Impact is limited to someone re-tracing audit provenance.
- **03-learn-validation-matrix.md summary count mismatch**: CRITICAL classification is **accurate** — an audit document with Contradicted=15 when the body shows 22 is a material misstatement that could cause stakeholders to underestimate risk.
- **03-learn-validation-matrix.md wrong filenames**: CRITICAL is **accurate** — if someone uses these references to locate controls for remediation verification, they'll fail to find them.
- **Section A nav orphans (FALSE POSITIVE)**: CRITICAL classification is **accurate and well-handled** — correctly identifying a false positive and preventing unnecessary mkdocs.yml surgery is the highest-value finding in the review.
**FIX_ORDERING:**
1. **Fix 7 first (Section A false positive)**: Document the tooling bug finding immediately to prevent anyone from acting on the false-positive orphan list. Zero-risk, high-protection.
2. **Fix 3 then Fix 4 together (filename corrections)**: Learn-validation-matrix and remediation-backlog share the same 6 wrong filenames. Fix both atomically in one commit to maintain consistency.
3. **Fix 2 after Fix 3 (summary counts)**: Recount after filename corrections are done — verify no additional items were missed during the filename audit.
4. **Fix 5 (arithmetic)**: Independent, do anytime. Verify whether 136 or 144 is correct by checking if some rows were intentionally excluded.
5. **Fix 1 (inventory titles)**: Largest fix, most labor. Do last since it has lowest downstream impact.
6. **Fix 6 (duplicate URL)**: Independent, lowest priority. Verify which module path (`exchange/` vs `exchangepowershell/`) is the canonical Learn URL before removing.
**OVER_ENGINEERED:**
- **Fix 1 scope creep**: The proposal to "Expand Pillars 2-4 into individual rows with verified titles" is over-engineering. The collapsed format (e.g., "2.1–2.24") was an intentional authoring choice for a summary table. The review correctly flagged this as a GAP, not a bug — but the FIX instruction conflates the two. **Recommended**: Fix only Pillar 1's 23 wrong title mappings. Leave Pillars 2-4 collapsed unless the audit owner explicitly requests expansion.
- **Fix 5 ambiguity**: The fix says "either update header to 144 or adjust category breakdown to sum to 136" without determining which is correct. The sum (Medium=100, Low=44=**144**) is arithmetic fact from the table. The header "136" is the error. The fix should simply say "update header from 136 to 144" — no need to frame it as a choice.
**CRITIQUE_SUMMARY:** The review findings are substantively correct — all filename mismatches and arithmetic errors verified against the filesystem and document content. However, two of five CRITICAL items should be HIGH (they're in unpublished internal reports, not live documentation), and Fix 1's proposed Pillar 2-4 expansion is unnecessary scope creep that triples the work for marginal accuracy gain in a summary table.