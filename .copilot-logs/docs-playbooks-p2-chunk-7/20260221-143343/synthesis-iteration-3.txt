ISSUE_COUNT: 2
CRITICAL:
NONE
WARNINGS:
- docs/playbooks/control-implementations/2.24/powershell-setup.md (lines 276-281): PowerShell `.IndexOf()` called as instance method on `System.Object[]` arrays (e.g., `("Allowed","Restricted","Prohibited").IndexOf($val)`). `System.Object[]` does not expose an `IndexOf` instance method in PowerShell; would throw `MethodNotFound` at runtime. 5 occurrences. (NEEDS_HUMAN_REVIEW)
- docs/playbooks/control-implementations/2.24/powershell-setup.md (lines 472-484): `$null` comparison bug in expiration alert logic. When a feature has no `fsi_expirationdate`, `$daysToExpiration` is `$null`. Then `$null -le 30` evaluates to `$true` (because `$null` coerces to `0`), causing all features without expiration dates to be incorrectly flagged as `"EXPIRES SOON ( days)"`. Missing `$null` guard before numeric comparisons. (NEEDS_HUMAN_REVIEW)
GAPS:
NONE
FIXES:
1. docs/playbooks/control-implementations/2.24/powershell-setup.md (lines 276-281): Replace 5 `.IndexOf()` instance calls with `[array]::IndexOf()` static calls. Example: change `[int](("Allowed", "Restricted", "Prohibited").IndexOf($feature.Zone1))` to `[int]([array]::IndexOf(@("Allowed", "Restricted", "Prohibited"), $feature.Zone1))`. (NEEDS_HUMAN_REVIEW — verify against target PowerShell version before applying)
2. docs/playbooks/control-implementations/2.24/powershell-setup.md (lines 472-484): Add `$null` check before numeric expiration comparisons, e.g., `if ($null -eq $daysToExpiration) { "N/A" } elseif ($daysToExpiration -le 30) { "EXPIRES SOON ($daysToExpiration days)" } elseif (...)`. (NEEDS_HUMAN_REVIEW)
STATUS: ISSUES_FOUND