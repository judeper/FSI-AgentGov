All claims verified against the files. Here is the synthesized final review:
ISSUE_COUNT: 8
CRITICAL:
- docs/playbooks/control-implementations/4.7/verification-testing.md (lines 82-83): PowerShell validation script counts ALL licensed users (`Get-MgUser -Filter "assignedLicenses/any()" -All`) instead of filtering by Copilot SKUs. In a Control 4.7 (Copilot Data Governance) verification context, an FSI tenant with thousands of E3/E5 users would get a meaningless count. The powershell-setup.md (lines 36-55) correctly filters by Copilot SKU — the verification script should match.
WARNINGS:
- docs/playbooks/control-implementations/4.7/powershell-setup.md: 6 placeholder URLs in code blocks (lines 24, 97-100, 215) are intentional examples, not clickable links. Adding explicit `# Replace with your tenant URL` comments would silence future scanners and improve copy-paste usability.
- docs/playbooks/control-implementations/4.7/troubleshooting.md: 1 placeholder URL in code blocks (lines 17, 73) — same pattern as above.
- docs/playbooks/control-implementations/4.7/powershell-setup.md (line 309): `finally` block uses `Get-SPOSite -Limit 1 -ErrorAction SilentlyContinue` as a connection test before `Disconnect-SPOService`. This issues an unnecessary API call; `Disconnect-SPOService -ErrorAction SilentlyContinue` alone is sufficient.
- docs/playbooks/control-implementations/4.7/powershell-setup.md (lines 201-203): `.DESCRIPTION` claims the Complete Configuration Script performs "1. Inventories Copilot licenses" but the script body (lines 230-312) never includes a Copilot license inventory step. The individual snippet at lines 36-59 does this correctly, but the consolidated script omits it entirely. The description overpromises relative to what the script does.
- docs/playbooks/control-implementations/4.7/powershell-setup.md (lines 11-12): Inconsistent `-Scope` on module install. Line 11 uses `-Scope CurrentUser` for Microsoft.Graph but line 12 omits it for Microsoft.Online.SharePoint.PowerShell, defaulting to system-wide scope requiring elevated privileges.
GAPS:
- docs/playbooks/control-implementations/4.7/verification-testing.md: The checklist item "Graph Connector ACL mappings validated (if applicable)" (line 30) has no corresponding test procedure or cross-reference. The powershell-setup.md (line 163+) has a full "Graph Connector ACL Validation" section and troubleshooting.md (lines 76-79) has a diagnostic command, but verification-testing.md does not link to either.
- Scope note: portal-walkthrough.md was not included in the original review scope. No critical issues found upon inspection, but noted for audit completeness.
FIXES:
1. docs/playbooks/control-implementations/4.7/verification-testing.md (lines 81-83): Replace generic licensed-user count with Copilot-specific filter matching powershell-setup.md approach:
   ```powershell
   # Count Copilot-licensed users
   $copilotSkus = Get-MgSubscribedSku -All | Where-Object {
       $_.SkuPartNumber -match 'Copilot'
   } | Select-Object -ExpandProperty SkuId
   $copilotUsers = Get-MgUser -Filter "assignedLicenses/any()" -All |
       Where-Object { $_.AssignedLicenses | Where-Object { $_.SkuId -in $copilotSkus } }
   Write-Host "Copilot-licensed users: $($copilotUsers.Count)"
   ```
2. docs/playbooks/control-implementations/4.7/powershell-setup.md (line 24): Add inline comment `# Replace 'tenant' with your actual tenant name` after the placeholder URL.
3. docs/playbooks/control-implementations/4.7/powershell-setup.md (lines 95-96): Add inline comment `# Replace these URLs with your actual sensitive site URLs` above the array.
4. docs/playbooks/control-implementations/4.7/powershell-setup.md (lines 308-312): Replace connection-test cleanup with simpler pattern:
   ```powershell
   finally {
       # Cleanup connections
       Disconnect-MgGraph -ErrorAction SilentlyContinue
       Disconnect-SPOService -ErrorAction SilentlyContinue
   }
   ```
5. docs/playbooks/control-implementations/4.7/powershell-setup.md (line 203): Remove or correct the `.DESCRIPTION` item "1. Inventories Copilot licenses" — either delete the claim or renumber the remaining items to reflect what the script actually does (audit exclusions, exclude sensitive sites, generate recommendations).
6. docs/playbooks/control-implementations/4.7/powershell-setup.md (line 12): Add `-Scope CurrentUser` to the SPO module install for consistency: `Install-Module -Name Microsoft.Online.SharePoint.PowerShell -Scope CurrentUser -Force`.
7. docs/playbooks/control-implementations/4.7/troubleshooting.md (line 17): Add inline comment `# Replace with your site URL` to the example.
8. docs/playbooks/control-implementations/4.7/verification-testing.md (line 30): Add a cross-reference after the Graph Connector checklist item pointing to powershell-setup.md's Graph Connector ACL Validation section, rather than duplicating the full procedure.
STATUS: ISSUES_FOUND