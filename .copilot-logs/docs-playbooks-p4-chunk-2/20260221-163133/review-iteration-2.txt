All 13 files have been read. Let me now check for cross-references within the batch and verify a few details.
Good — no FSI language violations. Here is my review:
---
ISSUE_COUNT: 31
CRITICAL:
- NONE
WARNINGS:
- docs/playbooks/control-implementations/4.5/portal-walkthrough.md: Broken external URL `https://admin.sharepoint.com` (HTTP -1) — this is the generic SharePoint Admin Center URL; it requires tenant authentication to resolve and is a valid documentation reference. No fix needed.
- docs/playbooks/control-implementations/4.5/powershell-setup.md: Placeholder URL `https://contoso-admin.sharepoint.com` in code block — intentional example tenant URL. No fix needed.
- docs/playbooks/control-implementations/4.6/portal-walkthrough.md: Placeholder URLs `https://tenant-admin.sharepoint.com` and `https://contoso.sharepoint.com/sites/ApprovedKnowledge` in code blocks — intentional examples. No fix needed.
- docs/playbooks/control-implementations/4.6/powershell-setup.md: 4 placeholder URLs (`tenant.sharepoint.com/sites/DraftDocuments`, `tenant.sharepoint.com/sites/ProductKnowledge`, `contoso.sharepoint.com/sites/ApprovedContent`, `contoso.sharepoint.com/sites/OldContent`) in code blocks — intentional examples. No fix needed.
- docs/playbooks/control-implementations/4.6/troubleshooting.md: 3 placeholder URLs (`tenant.sharepoint.com/sites/SiteName`, `contoso.sharepoint.com/sites/LowPriority`, `contoso.sharepoint.com/sites/AllowedSite`) in code blocks — intentional examples. No fix needed.
- docs/playbooks/control-implementations/4.6/verification-testing.md: Placeholder URL `https://tenant.sharepoint.com/sites/DraftDocs` in code block — intentional example. No fix needed.
- docs/playbooks/control-implementations/4.7/powershell-setup.md: 4 placeholder URLs (`tenant.sharepoint.com/sites/ExecutiveCompensation`, `MergerTarget`, `LegalHold`, `ComplianceInvestigations`) in code blocks — intentional examples. No fix needed.
- 13 mkdocs.yml nav orphans for all 13 batch files (e.g., `playbooks/control-implementations/4.4/verification-testing.md` referenced in nav but reported as not found) — all 13 files DO exist at their expected paths in this worktree; this is likely a worktree sparse-checkout or scanner-timing artifact. Verify docs/ directory completeness in main branch.
- docs/playbooks/control-implementations/4.5/powershell-setup.md (line 284) and 4.7/powershell-setup.md (line 309): Fragile cleanup pattern in `finally` block — uses `Get-SPOSite -Limit 1` to test connection before calling `Disconnect-SPOService`. This can throw unexpected errors; simpler to call `Disconnect-SPOService -ErrorAction SilentlyContinue` directly.
- 10 of 13 playbook files missing back-navigation links (only portal-walkthrough files include `[Back to Control X.X]` cross-links at bottom). This appears to be a consistent pattern (portal-walkthrough = entry point), but adding nav links to all playbook types would improve discoverability.
GAPS:
- NONE
FIXES:
1. docs/playbooks/control-implementations/4.5/powershell-setup.md (line 284): Replace `if (Get-SPOSite -Limit 1 -ErrorAction SilentlyContinue) { Disconnect-SPOService -ErrorAction SilentlyContinue }` with `Disconnect-SPOService -ErrorAction SilentlyContinue` in the finally block.
2. docs/playbooks/control-implementations/4.7/powershell-setup.md (line 309): Same fix — replace fragile `Get-SPOSite` connection test with direct `Disconnect-SPOService -ErrorAction SilentlyContinue`.
3. All 16 placeholder/example URLs: No fix needed — these are intentional tenant-specific example URLs in PowerShell code blocks and SharePoint Admin Center references. Scanners flag them because they require authentication or are fictional tenants.
4. 13 nav orphans: Verify main branch `docs/` directory contains all files. Files exist in this worktree; orphan reports are likely a scanner artifact.
STATUS: ISSUES_FOUND