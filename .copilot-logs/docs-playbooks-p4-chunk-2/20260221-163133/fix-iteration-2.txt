Now I see both `try/catch` blocks. The first one (line 140) is inside a function that connects via `Connect-PnPOnline` per-site — the `finally` with `Disconnect-SPOService` should wrap the outer function scope. The second (line 217) is a bulk operation loop. Let me check the full function boundary for the first block and the connection context for the second.
Let me check how other Pillar 4 files handle the cleanup pattern for reference.
Now I understand both blocks. Let me apply the fixes — adding `finally { Disconnect-SPOService }` cleanup to both code blocks.
Let me verify the changes look correct:
Both `finally` blocks are correctly placed. Now let me verify the file has valid markdown and commit.
Both `Disconnect-SPOService` calls are present. The file now matches the cleanup pattern used in every other Pillar 4 powershell-setup file.
CHANGES:
- docs/playbooks/control-implementations/4.6/powershell-setup.md: Added `try/finally` with `Disconnect-SPOService -ErrorAction SilentlyContinue` wrapping the SPO-connected code in `Get-CopilotReadySites` function (around lines 136-160), ensuring connection cleanup on error or interruption.
- docs/playbooks/control-implementations/4.6/powershell-setup.md: Added outer `try/finally` with `Disconnect-SPOService -ErrorAction SilentlyContinue` wrapping the bulk CSV foreach loop (around lines 221-233), ensuring connection cleanup after batch operations.
FIX_STATUS: COMPLETE