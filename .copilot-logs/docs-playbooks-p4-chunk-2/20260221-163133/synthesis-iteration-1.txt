ISSUE_COUNT: 6
CRITICAL:
NONE
WARNINGS:
- 4.7/portal-walkthrough.md (line 41): Step 2 says `set to **Restricted**` for the RCD UI, but 4.6/portal-walkthrough.md (lines 41-47) correctly documents this as an **On/Off toggle** with an explicit admonition note. Inconsistent UI description within the batch; an admin would look for a dropdown that doesn't exist.
- 4.7/verification-testing.md (lines 82-83): PowerShell validation counts ALL licensed users (`Get-MgUser -Filter "assignedLicenses/any()"`) instead of filtering for Copilot-licensed users. The sibling powershell-setup.md (lines 37-55) has the correct Copilot-specific SKU filter pattern.
- 4.5/powershell-setup.md (lines 10-12): Prerequisites install only `Microsoft.Online.SharePoint.PowerShell` and `ExchangeOnlineManagement`, but the file uses `Get-MgUserMemberOf` (line 174) which requires `Microsoft.Graph`. The same Graph cmdlets appear in 4.5/verification-testing.md and 4.5/troubleshooting.md. By contrast, 4.7/powershell-setup.md correctly lists `Microsoft.Graph`. An admin following the 4.5 playbook from scratch would encounter errors.
- NEEDS_HUMAN_REVIEW: 4.6/powershell-setup.md & 4.6/portal-walkthrough.md: Restricted Search cmdlets (`Add-SPOTenantRestrictedSearchAllowedList`, `Remove-SPOTenantRestrictedSearchAllowedList`, `Get-SPOTenantRestrictedSearchAllowedList`, `Set-SPOTenant -EnableRestrictedSearchAllList`) should be verified as current GA names. The doc includes a self-aware note to verify with `Get-Command`.
GAPS:
- 4.7/portal-walkthrough.md Step 2 is missing a reindexing/propagation delay warning after configuring RCD exclusions. 4.6/portal-walkthrough.md includes a `!!! warning "Reindexing After Scope Changes"` admonition noting 24-48 hours; 4.7 should have the same since it also configures RCD.
- index.md: Stale control counts — Pillar 1 listed as 25 (should be 28), Pillar 2 as 22 (should be 24), Pillar 3 as 10 (should be 12). Pre-existing and cross-pillar; low priority for this batch.
FIXES:
1. 4.7/portal-walkthrough.md (line 41): Change `Under **Microsoft 365 Copilot**, set to **Restricted**` to `Under **Microsoft 365 Copilot**, locate **Restrict content from Microsoft 365 Copilot** and toggle to **On**` to match 4.6's documented toggle behavior.
2. 4.7/verification-testing.md (lines 82-83): Replace `$licensedUsers = Get-MgUser -Filter "assignedLicenses/any()" -All` / `Write-Host "Users with licenses: $($licensedUsers.Count)"` with the Copilot-specific SKU filter pattern from 4.7/powershell-setup.md (query `Get-MgSubscribedSku` for Copilot SKUs, then filter users by those SKU IDs).
3. 4.7/portal-walkthrough.md: Add a propagation delay admonition after Step 2 configuration steps: `!!! warning "Reindexing After Scope Changes"` noting 24-48 hours before validating exclusions, matching 4.6's pattern.
4. 4.5/powershell-setup.md (lines 10-12): Add `Microsoft.Graph` to the `Install-Module` prerequisites block, consistent with 4.7/powershell-setup.md.
STATUS: ISSUES_FOUND