Let me check the worktree contents to understand the nav orphan context.
The files exist at the expected paths — the nav orphan findings appear to be false positives from the automated checker. Now I have all the information needed to compile my review report.
ISSUE_COUNT: 4
CRITICAL:
- NONE
WARNINGS:
- docs/playbooks/control-implementations/2.23/powershell-setup.md: Script 3 (`Get-ConsentRecords.ps1`) defines helper function `Get-AccessToken` at line 376 AFTER it is called at line 335. In PowerShell script execution (top-to-bottom), this causes a runtime error. Function must be moved before the `try` block or extracted to a separate module. (NEEDS_HUMAN_REVIEW)
- docs/playbooks/control-implementations/2.23/powershell-setup.md: Script 2 (`Get-AgentDisclosureInventory.ps1`) uses `Get-AdminPowerAppChatBotTopic` (line 195) which does not appear to be a real cmdlet in `Microsoft.PowerApps.Administration.PowerShell`. Similarly, `Get-AdminPowerAppChatBot` (line 188) — verify these cmdlet names against current module documentation. (NEEDS_HUMAN_REVIEW)
- docs/playbooks/control-implementations/2.21/verification-testing.md: Attestation template references "Framework Version: 1.1" (line 153) but the current framework version is 1.2.x. Should be updated to match current version.
- docs/playbooks/control-implementations/2.19/verification-testing.md, 2.20/*, 2.21/*, 2.23/*: Missing version footers. The 2.22 playbooks consistently include `*Updated: February 2026 | Version: v1.3*` footers, but 2.19, 2.20, 2.21, and 2.23 playbooks do not have version footers, creating inconsistency across the batch.
GAPS:
- NONE
FIXES:
1. docs/playbooks/control-implementations/2.23/powershell-setup.md: Move the `Get-AccessToken` function definition (lines 376-383) above the `try` block (before line 330) in Script 3, or replace the placeholder with actual authentication logic using `Connect-AzAccount`/`Get-AzAccessToken` as used in the 2.22 playbooks.
2. docs/playbooks/control-implementations/2.23/powershell-setup.md: Verify `Get-AdminPowerAppChatBot` and `Get-AdminPowerAppChatBotTopic` cmdlet names against current `Microsoft.PowerApps.Administration.PowerShell` module; update or add a comment marking these as conceptual if no real equivalent exists.
3. docs/playbooks/control-implementations/2.21/verification-testing.md: Update "Framework Version: 1.1" to "Framework Version: 1.2" in the attestation template (line 153).
4. docs/playbooks/control-implementations/2.19/verification-testing.md, 2.20/*, 2.21/*, 2.23/*: Add version footers consistent with the 2.22 pattern (`*Updated: [Month Year] | Version: v[X.X]*`) to all playbook files missing them.
NOTE ON SECTION A AUTOMATED FINDINGS:
- **Broken external URLs (16 entries):** All are false positives — admin portal URLs rejecting HEAD requests (admin.powerplatform.microsoft.com, copilotstudio.microsoft.com, make.powerautomate.com), placeholder/example URLs (contoso.*, org12345.*, tenant.*), API endpoints requiring authentication (api.bap.microsoft.com, graph.microsoft.com), or URLs where the link checker incorrectly included trailing backticks from inline code formatting. No documentation fixes needed.
- **MkDocs nav orphans (~300 entries):** False positives — verified that referenced files exist at expected paths under `docs/`. The automated checker appears to have had a path resolution issue. No fixes needed for this batch.
STATUS: ISSUES_FOUND