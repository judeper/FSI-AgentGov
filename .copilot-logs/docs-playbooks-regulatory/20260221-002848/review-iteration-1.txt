Let me verify the nav entries and check for undefined references in the files.
All 3 files and their cross-referenced controls exist. The automated orphan finding for these 3 files is a **false positive** — files exist at the correct nav paths. Now compiling the full review.
ISSUE_COUNT: 7
CRITICAL:
- `colorado-ai-impact-assessment.md`: Footnote markers `<sup>[11]</sup>` and `<sup>[12]</sup>` used 21 times throughout (lines 4–151) but **no references/footnotes section exists** — readers cannot verify which statutory sections are being cited. These appear to be internal research source markers that were not resolved into proper citations.
- `supply-chain-risk-register-entry.md`: Citation markers `[web:21]`, `[web:92]`, and `<sup>[3]</sup>` used on lines 5, 6, 7, 69, and 114 but **no references section defines them**. Same issue — internal research markers leaked into published content.
WARNINGS:
- `colorado-ai-act-readiness.md` (line 73): "Colorado AI Act Implementation Timeline" links to generic `https://www.colorado.gov` — unlikely to resolve to useful content; should link to a specific page or be removed.
- `supply-chain-risk-register-entry.md` (line 7): Related controls (2.7, 2.4, 2.3, 1.7, 3.3) listed as plain text without hyperlinks, inconsistent with `colorado-ai-act-readiness.md` (lines 64–66) which provides full relative hyperlinks to related control docs.
- `colorado-ai-impact-assessment.md` (line 5): References "SB 25B-004" as the bill extending the effective date from Feb 1 to June 30, 2026 — NEEDS_HUMAN_REVIEW to confirm bill number accuracy.
- `supply-chain-risk-register-entry.md` (lines 5–6): References "FINRA's 2026 oversight messaging" and "SEC 2026 priorities" as specific regulatory positions with `[web:XX]` markers — NEEDS_HUMAN_REVIEW to confirm accuracy of these forward-looking regulatory claims.
GAPS:
- No `index.md` landing page exists for the `docs/playbooks/regulatory-modules/` directory, though the section has 3 documents and appears as a nav group in mkdocs.yml (line 635).
FIXES:
1. `colorado-ai-impact-assessment.md`: Add a `## References` section at the end defining what `[11]` and `[12]` cite (e.g., specific SB24-205 section numbers or bill analysis sources), or replace superscript markers with inline statutory citations (e.g., "§ 6-1-1702(1)" instead of `<sup>[12]</sup>`).
2. `supply-chain-risk-register-entry.md`: Add a `## References` section defining `[web:21]`, `[web:92]`, and `[3]`, or replace with inline citations (e.g., "FINRA Regulatory Notice 24-XX" instead of `[web:21]`).
3. `supply-chain-risk-register-entry.md` (line 7): Convert plain-text control references to hyperlinks matching the style in `colorado-ai-act-readiness.md`, e.g., `[2.7 Vendor and Third-Party Risk Management](../../controls/pillar-2-management/2.7-vendor-and-third-party-risk-management.md)`.
4. `colorado-ai-act-readiness.md` (line 73): Replace generic `https://www.colorado.gov` with a specific URL for the Colorado AG's AI Act guidance page, or remove the link if no specific page exists.
NOTE ON SECTION A: The automated pre-pass flagged the 3 in-scope files as mkdocs nav orphans, but all 3 files exist at their declared nav paths (`docs/playbooks/regulatory-modules/`). The nav entries (mkdocs.yml lines 636–638) are structurally correct. These are false positives for the in-scope files. The remaining ~300 orphans are outside this review's scope.
STATUS: ISSUES_FOUND