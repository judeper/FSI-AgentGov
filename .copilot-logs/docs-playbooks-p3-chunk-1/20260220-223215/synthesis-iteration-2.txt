All issues verified against the actual files. Here is the synthesized final review:
---
ISSUE_COUNT: 8
CRITICAL:
- 3.1/powershell-setup.md: Hash filename mismatch — section script (line 127) writes to `AgentInventory_Hashes.csv`, but Complete Configuration Script (line 335) writes to `Hashes.csv`. The verification-testing.md validator (line 178) checks for `AgentInventory_Hashes.csv`, so admins using the complete script get a silent hash validation failure.
- 3.4/powershell-setup.md: `Send-CriticalIncidentNotification` called at line 121 but never defined in any 3.4 playbook file. Critical-severity incidents hit a `CommandNotFoundException` at runtime.
WARNINGS:
- 3.3/portal-walkthrough.md, 3.4/portal-walkthrough.md, 3.5/portal-walkthrough.md, 3.6/portal-walkthrough.md: Missing standardized header metadata (`**Last Updated**`, `**Portal**`, `**Estimated Time**`) present in 3.1 and 3.2 portal walkthroughs. Files jump from intro quote directly to `---` then Prerequisites.
- 3.3, 3.4, 3.5, 3.6 powershell-setup/troubleshooting/verification-testing files: Use "Next Steps" bullet-list footer navigation instead of the inline pipe-delimited `[Back to Control X.X](...) | [PowerShell Setup](...) | ...` format used consistently in 3.1 and 3.2.
- 3.5/powershell-setup.md: `Invoke-AzCostManagementQuery -Query $costQuery` (line 171) — the `-Query` parameter does not exist on this cmdlet. The Az.CostManagement module uses individual parameters (`-Timeframe`, `-Type`, `-DatasetAggregation`, `-DatasetGrouping`, etc.). The entire query-object pattern on lines 148–171 is incompatible with the cmdlet's actual interface.
GAPS:
- 3.3–3.6 troubleshooting playbooks lack the "How to Confirm Configuration is Active" section (portal + PowerShell quick-check) present in 3.1 and 3.2 troubleshooting guides.
- 3.3–3.6 verification-testing playbooks lack automated validation scripts and governance tier-specific testing sections present in 3.1 and 3.2.
FIXES:
1. 3.1/powershell-setup.md (line 335): Change `"$ExportPath\Hashes.csv"` to `"$ExportPath\AgentInventory_Hashes.csv"` so the complete configuration script matches the hash filename used in the section scripts and the validation script.
2. 3.4/powershell-setup.md (line 121): Replace `Send-CriticalIncidentNotification -IncidentId $incidentId -Details $incident` with a `Write-Warning "CRITICAL INCIDENT $incidentId - implement organization-specific escalation notification"` stub so the script does not reference an undefined function. Do not use `Send-MailMessage` (deprecated in PowerShell 7+).
3. 3.3/portal-walkthrough.md: Add header metadata block after the intro quote (before the first `---`): `**Last Updated:** January 2026`, `**Portal:** Microsoft Purview, SharePoint Admin Center, Power Automate`, `**Estimated Time:** 2-3 hours`.
4. 3.4/portal-walkthrough.md: Add header metadata block after the intro quote (before the first `---`): `**Last Updated:** January 2026`, `**Portal:** SharePoint Admin Center, Power Automate, Microsoft Sentinel`, `**Estimated Time:** 2-3 hours`.
5. 3.5/portal-walkthrough.md: Add header metadata block after the intro quote (before the first `---`): `**Last Updated:** January 2026`, `**Portal:** Power Platform Admin Center, M365 Admin Center, Azure Portal`, `**Estimated Time:** 1-2 hours`.
6. 3.6/portal-walkthrough.md: Add header metadata block after the intro quote (before the first `---`): `**Last Updated:** January 2026`, `**Portal:** Power Platform Admin Center, SharePoint Admin Center, M365 Admin Center`, `**Estimated Time:** 1-2 hours`.
7. 3.5/powershell-setup.md (lines 148-171): Replace the `$costQuery` hashtable and `Invoke-AzCostManagementQuery -Query $costQuery` call with the cmdlet's actual parameter interface (individual parameters for `-Timeframe`, `-Type`, `-Scope`, `-DatasetAggregation`, `-DatasetGrouping`).
STATUS: ISSUES_FOUND