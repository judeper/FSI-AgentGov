Now I have full context on the actual file contents and cross-repo usage patterns. Here's my risk analysis:
---
**HIGH_RISK_FIXES:**
- Fix #4 (Microsoft Foundry → Azure AI Foundry): HIGH RISK. "Microsoft Foundry" appears **30+ times** across 15+ files (controls 1.2, 1.5, 1.6, 1.11, 1.12, 1.24, 2.1, 2.9, 2.17, 3.3, 3.8, 3.11, framework/agent-identity-architecture.md, and multiple playbooks). Changing only the 2 instances in portal-walkthrough.md would create **terminology inconsistency** across the entire repo. This is either the repo's intentional branding choice or a repo-wide issue — either way, a 2-line spot fix is wrong. NEEDS_HUMAN_REVIEW classification is correct; do NOT auto-fix.
**SEVERITY_CORRECTIONS:**
- Fixes #1/#2/#3 (microsoft.openai/accounts): Severity is **correctly classified as CRITICAL**. The queries already include `microsoft.cognitiveservices/accounts` on lines 77, 127, 288 — so Azure OpenAI resources with `kind == 'OpenAI'` are already captured by that line. The `microsoft.openai/accounts` entries are dead code that return zero results, giving admins false confidence in inventory completeness if they rely on per-type counts. Fix risk is LOW — removing one line from an array literal has no downstream impact.
- Fix #5 (Extension names): Severity is correctly WARNING/NEEDS_HUMAN_REVIEW. `SensitiveDataDiscovery` and `AgentlessDiscoveryForKubernetes` are real Defender CSPM extensions but are **not** the AI-SPM extension. The script would report extensions-enabled status incorrectly. However, this requires confirming the current API extension name — do not guess.
**FIX_ORDERING:**
- Fixes #1, #2, #3 are independent and identical in nature — apply in any order (or parallel). No cross-references exist between these three query instances.
- Fixes #4 and #5 are NEEDS_HUMAN_REVIEW and should not be applied without confirmation. They have no dependency on #1-#3.
**OVER_ENGINEERED:**
- Fix #1 proposed alternative (`| where type =~ 'microsoft.cognitiveservices/accounts' and (kind =~ 'OpenAI' or kind == '')`) is over-engineered. The line `microsoft.cognitiveservices/accounts` is already in the `type in~()` array on lines 77/127/288. Simply **removing** the `microsoft.openai/accounts` line is sufficient — Azure OpenAI resources already match via `cognitiveservices`. Adding a kind filter would change query semantics (filtering OUT non-OpenAI Cognitive Services resources that should be inventoried).
- The review's dismissal of broken URLs and nav orphans as false positives is correct and well-reasoned — no over-engineering concern there.
**CRITIQUE_SUMMARY:** Fixes #1-#3 are safe, correctly critical, but the proposed replacement query is over-engineered — a simple line deletion is the correct minimal fix. Fixes #4-#5 are correctly flagged NEEDS_HUMAN_REVIEW; fix #4 in particular must not be applied in isolation given 30+ consistent usages of "Microsoft Foundry" repo-wide.