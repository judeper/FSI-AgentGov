All findings confirmed. Now I'll produce the final synthesized review.
ISSUE_COUNT: 4
CRITICAL:
- NONE
WARNINGS:
- docs/playbooks/control-implementations/1.24/powershell-setup.md (line 132): `Get-AzAccessToken` `.Token` property is accessed as a plain string, but Az.Accounts 5.0+ returns a `SecureString`. The 1.25/powershell-setup.md (lines 25-32) correctly handles this with a type-check and conversion. Without this, the `Bearer $token` header will contain the literal string `System.Security.SecureString`, causing 401 errors on modern Az module versions.
- docs/playbooks/control-implementations/1.24/powershell-setup.md (lines 46-47): `$aiSpm` filters extensions by `SensitiveDataDiscovery` and `AgentlessDiscoveryForKubernetes` (Defender CSPM extensions, not the AI-SPM extension). Additionally, `$aiSpm` is dead code — it is assigned but never referenced; line 47 outputs `$extensions.Name` instead. NEEDS_HUMAN_REVIEW for correct AI-SPM extension name.
- docs/playbooks/control-implementations/1.24/portal-walkthrough.md (lines 11, 41): "Microsoft Foundry" appears twice — the official product name is "Azure AI Foundry" (rebranded from Azure AI Studio in November 2024).
- NONE (pre-pass broken URLs and mkdocs orphans confirmed as false positives; no action needed)
GAPS:
- NONE
FIXES:
1. docs/playbooks/control-implementations/1.24/powershell-setup.md (line 132): Replace `$token = (Get-AzAccessToken -ResourceUrl "https://management.azure.com").Token` with the SecureString-safe pattern from 1.25/powershell-setup.md: acquire the token object, check if `.Token -is [System.Security.SecureString]`, convert with `Marshal::SecureStringToBSTR` if so, else use directly.
2. docs/playbooks/control-implementations/1.24/powershell-setup.md (lines 46-47): Either (a) remove the dead `$aiSpm` assignment entirely, or (b) replace it with a check for the actual AI-SPM extension name and use the result in the output on line 47. NEEDS_HUMAN_REVIEW for the authoritative AI-SPM extension identifier before applying.
3. docs/playbooks/control-implementations/1.24/portal-walkthrough.md (line 11): Change "Microsoft Foundry" to "Azure AI Foundry".
4. docs/playbooks/control-implementations/1.24/portal-walkthrough.md (line 41): Change "Microsoft Foundry projects" to "Azure AI Foundry projects".
STATUS: ISSUES_FOUND