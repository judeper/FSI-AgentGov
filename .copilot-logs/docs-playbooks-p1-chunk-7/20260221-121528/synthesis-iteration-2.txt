All claims verified against the source files. Here is the synthesized final review:
---
ISSUE_COUNT: 6
CRITICAL:
- 1.24/powershell-setup.md (lines 80, 290) and 1.24/verification-testing.md (line 129): Resource type `microsoft.openai/accounts` is not a valid Azure Resource Graph type. Azure OpenAI resources are registered under `microsoft.cognitiveservices/accounts` (with `kind == 'OpenAI'`), which is already present in each query. These lines silently return zero results, inflating false confidence in per-type inventory counts.
WARNINGS:
- 1.24/powershell-setup.md (lines 46-47): `$aiSpm` variable filters for `SensitiveDataDiscovery` and `AgentlessDiscoveryForKubernetes` (neither is the AI-SPM extension) and is then never referenced — line 47 outputs `$extensions.Name` (all extensions), not `$aiSpm`. This is dead code that does not actually verify AI-SPM enablement. NEEDS_HUMAN_REVIEW.
- 1.24/troubleshooting.md (line 121): Regex `"cognitiveservices|machinelearning|openai"` includes an `openai` branch that will never match any Azure ResourceType — Azure OpenAI resources have ResourceType `Microsoft.CognitiveServices/accounts`. The `cognitiveservices` branch already catches them, making `openai` a dead branch consistent with the same misconception flagged as critical above.
- 1.24/portal-walkthrough.md (lines 11, 41): "Microsoft Foundry" may be outdated — current branding is "Azure AI Foundry". However, this term appears 30+ times across 15+ files repo-wide (controls 1.2, 1.5, 1.6, 1.11, 1.12, 1.24, 2.1, 2.9, 2.17, 3.3, 3.8, 3.11, framework docs, and playbooks). Spot-fixing only these 2 instances would create terminology inconsistency. NEEDS_HUMAN_REVIEW as a repo-wide branding decision.
GAPS:
- NONE
FIXES:
1. 1.24/powershell-setup.md (line 80): Remove the `'microsoft.openai/accounts'` line from the `type in~` list. Do NOT add kind-based filtering — the query intentionally discovers all Cognitive Services resources, and `microsoft.cognitiveservices/accounts` on line 77 already captures Azure OpenAI.
2. 1.24/powershell-setup.md (line 290): Same as Fix #1 — remove `'microsoft.openai/accounts'` from the Complete Configuration Script query. After this fix, the inventory queries will be consistent with the validation query on line 209 (which already correctly omits this type).
3. 1.24/verification-testing.md (line 129): Same as Fix #1 — remove `'microsoft.openai/accounts'` from the KQL Resource Graph query.
4. 1.24/troubleshooting.md (line 121): Remove `|openai` from the regex, changing to `"cognitiveservices|machinelearning"`. The `cognitiveservices` branch already matches Azure OpenAI resources.
5. 1.24/powershell-setup.md (lines 46-47): NEEDS_HUMAN_REVIEW — verify the correct Defender CSPM extension name for AI security posture management, update the `Where-Object` filter, and use `$aiSpm` in the output (or remove the dead variable if the check is not needed).
6. 1.24/portal-walkthrough.md (lines 11, 41): NEEDS_HUMAN_REVIEW — confirm current product branding. If "Azure AI Foundry" is correct, apply the rename repo-wide (30+ occurrences across 15+ files), not just in these 2 instances.
STATUS: ISSUES_FOUND