All 15 files have been read and analyzed. The nav orphan files all exist in this worktree (verified), and the flagged URLs are API endpoints / auth-required portals. Here is my review:
ISSUE_COUNT: 3
CRITICAL:
- NONE
WARNINGS:
- docs/playbooks/control-implementations/1.24/portal-walkthrough.md: Line 11 references "Microsoft Foundry" — the official product name may be "Azure AI Foundry" (rebranded Nov 2024). Also appears on line 42 as "Microsoft Foundry projects". NEEDS_HUMAN_REVIEW — verify current product branding.
- docs/playbooks/control-implementations/1.26/powershell-setup.md: Uses `Set-AdminPowerAppChatbot -FileUploadEnabled $false` (lines 142-144, 426-428). This cmdlet and parameter may not exist in the official `Microsoft.PowerApps.Administration.PowerShell` module. NEEDS_HUMAN_REVIEW — verify cmdlet availability.
- docs/playbooks/control-implementations/1.24/powershell-setup.md: Uses REST API `api-version=2024-01-01` for `Microsoft.Security/attackPaths` (line 140). This API version may be outdated for a January 2026 document. NEEDS_HUMAN_REVIEW.
GAPS:
- NONE
FIXES:
1. docs/playbooks/control-implementations/1.24/portal-walkthrough.md: Verify "Microsoft Foundry" is the current official product name; if not, replace with "Azure AI Foundry" on lines 11 and 42.
2. docs/playbooks/control-implementations/1.26/powershell-setup.md: Verify `Set-AdminPowerAppChatbot` cmdlet with `-FileUploadEnabled` parameter exists in the Power Platform admin module; if not, replace with the correct cmdlet or document that this is a conceptual placeholder.
3. docs/playbooks/control-implementations/1.24/powershell-setup.md: Verify the `2024-01-01` API version for `Microsoft.Security/attackPaths` is current; update if a newer stable version exists.
NOTES ON SECTION A PRE-PASS FINDINGS:
- **Broken URLs (4):** All false positives. `https://management.azure.com` URLs are Azure REST API endpoints used correctly in PowerShell code (HTTP 400 expected without auth). `https://admin.powerplatform.microsoft.com` and `https://copilotstudio.microsoft.com` are valid portal URLs that return HTTP 405 to automated scanners (require browser auth). No fixes needed.
- **MkDocs nav orphans (~300):** All false positives. Every file listed as "not found" EXISTS in this worktree (verified `docs/controls/`, `docs/playbooks/`, `docs/framework/`, etc.). The orphan detection tool had a path resolution issue. No fixes needed.
STATUS: ISSUES_FOUND