Let me see more context around these usages to understand the full script structure and find the right fix approach.
Now let me also check the second occurrence's context:
Now I understand the issue. The `Set-AdminPowerAppChatbot` cmdlet with `-FileUploadEnabled` parameter is not a verified cmdlet in the official Power Platform admin module. The review flags this as NEEDS_HUMAN_REVIEW. The appropriate fix is to add a caveat/admonition noting that this cmdlet and parameter need verification, rather than removing functional script code.
Let me also check what the "Microsoft Foundry" warning says — the review explicitly says NOT to fix it per-file, so I should skip that one.
Now let me add appropriate admonitions to the PowerShell file:
Now let me find the heading for the second script section:
I'll add a note before each of the two script sections that use `Set-AdminPowerAppChatbot`. The first is in "Bulk Disable File Upload for Zone 3 Agents" (line 86) and the second is in "Complete Configuration Script" (line 339).
Now let me verify the edits look correct and commit:
The diff looks correct. Let me try the commit with explicit git commands:
Commit successful. The "Microsoft Foundry" warning is skipped per the review's explicit instruction — it requires a repo-wide branding sweep across 30+ files, not a per-file fix.
CHANGES:
- docs/playbooks/control-implementations/1.26/powershell-setup.md: Added two `!!! warning` admonitions before the "Bulk Disable File Upload for Zone 3 Agents" and "Complete Configuration Script" sections, noting that `Set-AdminPowerAppChatbot` with `-FileUploadEnabled` may not be available in all module versions and advising verification with `Get-Command`.
- docs/playbooks/control-implementations/1.24/portal-walkthrough.md: SKIPPED — "Microsoft Foundry" branding issue requires a repo-wide sweep across 30+ files per review guidance, not a per-file fix.
FIX_STATUS: COMPLETE