Now let me verify the docs directory structure to confirm the nav orphan situation.
ISSUE_COUNT: 8
CRITICAL:
- 1.24/powershell-setup.md (lines 80, 290) and 1.24/verification-testing.md (line 129): Resource type `microsoft.openai/accounts` is not a valid Azure Resource Graph type. Azure OpenAI resources are registered under `microsoft.cognitiveservices/accounts` with `kind == 'OpenAI'`. The Resource Graph queries will silently miss Azure OpenAI resources.
WARNINGS:
- 1.24/portal-walkthrough.md (lines 11, 41): Product name "Microsoft Foundry" may be incorrect — current branding is "Azure AI Foundry". NEEDS_HUMAN_REVIEW.
- 1.24/powershell-setup.md (lines 46-47): AI-SPM extension check filters for `SensitiveDataDiscovery` and `AgentlessDiscoveryForKubernetes` — neither is the AI security posture management extension name. NEEDS_HUMAN_REVIEW.
- Section A broken URLs (4 entries): All are false positives — `https://management.azure.com` is an ARM API endpoint used in PowerShell code (expected 400 without auth); `https://admin.powerplatform.microsoft.com` and `https://copilotstudio.microsoft.com` are legitimate portal URLs returning 405 to automated crawlers. No fix needed.
- Section A MKDOCS NAV ORPHANS: False positives — all 300+ referenced files exist under `docs/`. Verified `docs/controls/`, `docs/framework/`, `docs/index.md`, and all 16 batch playbook files are present. Likely a path resolution issue in the automated checker.
GAPS:
- NONE
FIXES:
1. 1.24/powershell-setup.md (line 80): Replace `'microsoft.openai/accounts'` with a kind-based filter. Change the Resource Graph query to use `| where type =~ 'microsoft.cognitiveservices/accounts' and (kind =~ 'OpenAI' or kind == '')` or remove the `microsoft.openai/accounts` line and add a note that Azure OpenAI appears under `microsoft.cognitiveservices/accounts`.
2. 1.24/powershell-setup.md (line 290): Same fix as #1 for the duplicate query in the Complete Configuration Script.
3. 1.24/verification-testing.md (line 129): Same fix as #1 for the KQL Resource Graph query example.
4. 1.24/portal-walkthrough.md (lines 11, 41): NEEDS_HUMAN_REVIEW — verify current product name; if rebranded, replace "Microsoft Foundry" with "Azure AI Foundry".
5. 1.24/powershell-setup.md (lines 46-47): NEEDS_HUMAN_REVIEW — verify the correct Defender CSPM extension name for AI security posture management and update the `Where-Object` filter accordingly.
STATUS: ISSUES_FOUND