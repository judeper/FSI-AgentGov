Now I have all the data needed.
---
**HIGH_RISK_FIXES:**
- **Fix 2 (Microsoft Foundry → Azure AI Foundry):** HIGH RISK. The term "Microsoft Foundry" appears in **24+ files** across framework docs, controls, and playbooks — not just 1.24/portal-walkthrough.md. Changing it in one file creates terminology inconsistency across the entire site. This fix must either be scoped as a repo-wide rename (separate task) or deferred entirely. Fixing only line 12 of one file would make it the outlier.
- **Fix 5 (Get-AdminPowerAppChatbot cmdlets):** MODERATE RISK. These cmdlets also appear in **1.27/powershell-setup.md** and **1.27/troubleshooting.md**. If the cmdlets are invalid, fixing only 1.26 leaves 1.27 broken. The review scoped this to batch `playbooks-p1-chunk-7` but the blast radius extends beyond it.
**SEVERITY_CORRECTIONS:**
- **Fix 1 (microsoft.openai/accounts) — CRITICAL is correct.** `microsoft.openai/accounts` is not a valid Azure resource provider. Scripts using this will return zero results, giving administrators false assurance that no AI resources exist. A silent failure in a security posture control is correctly classified as critical.
- **Fix 2 (Microsoft Foundry) — WARNING is under-classified as a standalone fix, but correctly classified if deferred.** Since the term is used consistently across 24+ files, it's either a deliberate project convention or a systemic issue. Either way, fixing one instance is worse than fixing none. Recommend: reclassify as **DEFERRED — repo-wide rename needed**.
- **Fix 3 (SecureString token) — WARNING is correct.** Scripts will break on Az.Accounts 5.0+ but still work on current widespread versions. The pattern fix already exists in 1.25 as a proven template.
- **Fix 4 (AI-SPM extension name) — WARNING is correct.** NEEDS_HUMAN_REVIEW is appropriate; incorrect extension names cause silent pass-through (no error, just no match), same failure class as Fix 1 but lower blast radius (one check vs. core query logic).
- **Fix 5 (PowerApps chatbot cmdlets) — WARNING is correct.** NEEDS_HUMAN_REVIEW is appropriate given uncertainty. If cmdlets don't exist, scripts fail loudly (command-not-found), which is less dangerous than silent failures.
**FIX_ORDERING:**
1. **Fix 1 first** (microsoft.openai/accounts → CognitiveServices filter) — Critical severity, self-contained to 3 locations in 1.24 files only, zero cross-reference risk, no dependencies on other fixes.
2. **Fix 3 second** (SecureString token pattern) — Copy proven pattern from 1.25 lines 27-32. Single file, single location. Independent of Fix 1.
3. **Fix 4 third** (AI-SPM extension name) — Requires human verification of correct extension identifier before applying. Blocked until verified.
4. **Fix 5 fourth** (PowerApps chatbot cmdlets) — Requires human verification AND scope expansion to include 1.27 files. Blocked until verified and batch scope is confirmed.
5. **Fix 2 last / deferred** — Requires repo-wide decision on "Microsoft Foundry" vs "Azure AI Foundry" terminology across 24+ files. Must not be applied to a single file in isolation.
**OVER_ENGINEERED:**
- NONE. All proposed fixes are appropriately scoped to the actual problems. Fix 1's suggestion to add `kind =~ 'OpenAI'` filtering is the correct Azure Resource Graph pattern, not over-engineering.
**CRITIQUE_SUMMARY:** The review correctly identified real technical issues — the `microsoft.openai/accounts` resource provider finding (Fix 1) is the highest-value catch as it causes silent security gaps. However, the review under-scoped Fix 2 (Microsoft Foundry appears in 24+ files, not just one) and Fix 5 (cmdlets also used in 1.27), meaning applying those fixes as written would create inconsistencies worse than the current state. Recommend: apply Fixes 1 and 3 immediately, defer Fix 2 to a repo-wide rename task, and gate Fixes 4 and 5 on human verification with expanded scope.