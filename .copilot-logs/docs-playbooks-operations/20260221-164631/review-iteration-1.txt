Let me verify the nav orphan situation and check file existence.
All referenced files exist under `docs/` — the nav orphan findings from the automated pre-pass are **false positives** (the checker likely resolved paths from the repo root instead of the `docs/` directory, which is mkdocs' default `docs_dir`).
Now for the content analysis:
ISSUE_COUNT: 1
CRITICAL:
NONE
WARNINGS:
- docs/playbooks/getting-started/phase-1-minimal-viable-controls.md (line 202): "per FINRA 3110" should be "per FINRA Rule 3110" for consistency with the actual control name (Control 2.12: "Supervision and Oversight (FINRA Rule 3110)") and standard regulatory citation format.
GAPS:
NONE
FIXES:
1. docs/playbooks/getting-started/phase-1-minimal-viable-controls.md (line 202): Change "per FINRA 3110" to "per FINRA Rule 3110"
NOTES ON SECTION A (MKDOCS NAV ORPHANS):
All 472 files listed as nav orphans by the automated pre-pass **actually exist** under the `docs/` directory. The automated checker appears to have resolved nav paths relative to the repository root instead of `docs/` (mkdocs default `docs_dir`). These are false positives and should not be counted. Verified by inspecting `docs/controls/pillar-1-security/`, `docs/framework/`, `docs/reference/`, `docs/playbooks/getting-started/`, and the `mkdocs.yml` configuration (no custom `docs_dir` set).
STATUS: ISSUES_FOUND