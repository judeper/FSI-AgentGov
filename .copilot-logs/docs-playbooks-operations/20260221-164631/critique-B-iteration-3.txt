Now I have full context. Here's the risk analysis:
---
**HIGH_RISK_FIXES:**
- Fix 1 (mkdocs.yml: create 470 missing files) — **HIGH RISK.** Creating 470 stub files in a worktree scoped to only 3 files is out of scope and dangerous. Those files likely exist in the main worktree but are absent from this branch's context. Attempting to create them here would produce empty/stub content that could overwrite real files on merge. This fix must be deferred to the main branch or a dedicated worktree. **Do not attempt in this batch.**
**SEVERITY_CORRECTIONS:**
- mkdocs.yml 473 CRITICAL → **DOWNGRADE to INFORMATIONAL for this batch.** The review correctly notes only 3 files are in-scope and 470 are absent from this worktree. These are not missing files — they exist in the base repo. Flagging them as CRITICAL is misleading in context; they are a pre-existing condition, not a regression introduced by this batch.
- phase-2-hardening.md backward nav WARNING → **CORRECT as WARNING.** Low-risk cosmetic fix, no build impact, accurately classified.
- Control 2.11 duplication WARNING → **CORRECT as WARNING.** The Phase 1 version ("Bias Testing" — establish process, line 235) and Phase 2 version ("Comprehensive Bias Testing" — full program, line 156) are genuinely progressive but the titles alone don't convey this. Warning is appropriate.
- phase-1-minimal-viable-controls.md retention language GAP → **UPGRADE to WARNING.** Line 185 states "10-year retention for Zone 3 regulatory requirements" as a stated regulatory fact. Per FSI language rules, this is a compliance overclaim — it presents an organizational policy choice as a regulatory mandate. SEC 17a-4 requires 6 years, FINRA 4511 requires 6 years. This violates the repo's own prohibited-language rules (`.github/instructions/fsi-language-rules.instructions.md`). Should be WARNING, not just a GAP, because it's a language compliance violation.
**FIX_ORDERING:**
1. **Fix 3 first** (retention language, phase-1) — standalone text edit, no dependencies, addresses a language-rule violation
2. **Fix 2 second** (backward nav link, phase-2) — standalone text addition before footer, no dependencies
3. **Fix 1 last / DEFER** (470 missing files) — out of batch scope entirely, must not be attempted here
**OVER_ENGINEERED:**
- Fix 3's proposed rewording ("retention period aligned with organizational policy (typically exceeding regulatory minimums of 6–7 years)") is slightly over-engineered. The parenthetical with specific year ranges invites debate about which minimums apply. Simpler: change line 185 to `"Configure data retention per organizational policy to support regulatory record-keeping requirements"` and move the specific period to the Steps section (line 191, "Configure retention period per policy"). This keeps the Purpose statement clean and avoids embedding debatable numbers in two places.
- The review's suggestion to "explicitly state progressive scope increase" for Control 2.11 duplication was appropriately left as a warning without a prescriptive fix — no over-engineering there.
**CRITIQUE_SUMMARY:** The review is thorough and accurately identifies the real issues, but the 470-file CRITICAL finding is misleadingly scoped — it's a worktree artifact, not a batch regression, and attempting that fix here would be destructive. The retention language issue (Fix 3) is the only fix with genuine compliance risk and should be prioritized.