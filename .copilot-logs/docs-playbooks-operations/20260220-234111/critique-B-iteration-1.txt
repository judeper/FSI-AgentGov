Here is the risk and impact analysis:
---
**HIGH_RISK_FIXES:**
- Fix 3 (add hyperlinks to all control references): **MEDIUM-HIGH RISK.** There are ~28 control references across the 3 files, each requiring a correct relative path from `docs/playbooks/getting-started/` to `docs/controls/pillar-N-*/`. Any miscalculated path will break `mkdocs build --strict`. Large diff also complicates review.
- Fix 1 (FINRA 3110 → FINRA Rule 3110): **LOW-BUT-NOTABLE RISK.** The actual control doc `2.12-supervision-and-oversight-finra-rule-3110.md` line 253 also uses bare "FINRA 3110" without "Rule". Fixing only the playbook creates an inconsistency with the control doc itself. The control filename includes "Rule" but the body text doesn't always. Fix should be applied to both files or neither.
**SEVERITY_CORRECTIONS:**
- Fix 2 (Purview portal name) is correctly NEEDS_HUMAN_REVIEW, but the scope is understated. Grep shows **50+ files** across the repo use varying forms: "Microsoft Purview Compliance Portal", "Microsoft Purview compliance portal", "Microsoft Purview portal", "Purview compliance portal". Fixing one instance in phase-0 without addressing the repo-wide inconsistency creates a one-off deviation. Should be escalated to a **separate repo-wide standardization task**, not fixed in this batch.
**FIX_ORDERING:**
1. Fix 1 first (single-line text change, lowest risk — but extend to control 2.12 body text too)
2. Fix 4 second (small addition at end of phase-2-hardening.md, no dependencies)
3. Fix 2 deferred (NEEDS_HUMAN_REVIEW + repo-wide scope → separate task)
4. Fix 3 last or deferred (largest scope, highest risk, should be its own batch)
**OVER_ENGINEERED:**
- **Fix 3 is over-engineered for this batch.** Adding ~28 hyperlinks across 3 files is a navigation enhancement, not an operational fix. It inflates diff size, increases broken-link risk, and should be a dedicated "cross-reference linking" task with its own verification pass. The current control-number-only format is readable and consistent with how the getting-started playbooks work as implementation checklists.
- Fix 4 is borderline — the asymmetry is real (Phase 0→1→2 has forward links but Phase 2 has no back-link), but this is a minor polish item, not a gap that affects operations.
**CRITIQUE_SUMMARY:** The review correctly identified real issues but two of the four proposed fixes (Fix 2 Purview naming, Fix 3 hyperlinks) have scope that exceeds this batch — Fix 2 is a repo-wide naming inconsistency and Fix 3 is a navigation enhancement that should be its own task. Only Fix 1 (with scope extended to the source control doc) and Fix 4 are safe to ship in this batch.