Good — this is a full worktree with all content present. Now I have enough context for the risk analysis.
HIGH_RISK_FIXES:
- **Fix 8 (mkdocs.yml / 473 nav orphans): HIGH RISK.** The reviewer claims 473 nav entries are missing, but the worktree clearly contains all pillar directories with all 28 P1 controls, framework/, playbooks/, etc. This finding appears to be a false positive caused by a flawed file-existence check (likely a path resolution bug — `docs/` prefix issue). **Do not act on this fix.** Running `mkdocs build --strict` from the full repo would confirm. Any nav restructuring based on this finding could catastrophically break the site.
- **Fix 1 (CONTROL-INDEX.md version):** Low risk. Simple footer text change. However, changing to "v1.2.51" may conflict with an intentional version bump to v1.3 already in progress (this file and the template both say v1.3, suggesting a coordinated pre-release update). Verify intent before downgrading.
SEVERITY_CORRECTIONS:
- **Fix 5 (template broken links) — Downgrade from CRITICAL to INFORMATIONAL.** The template uses `[X.X]` placeholder paths *by design*. These are not broken links — they're authoring instructions. `mkdocs build --strict` only validates pages in `nav:`, and the template is not navigated to as a user-facing page. No fix needed.
- **Fix 4 (broken URL `tenant-admin.sharepoint.com`) — Downgrade from CRITICAL to INFORMATIONAL.** This URL appears inside a PowerShell code block as a placeholder for the admin to substitute. It is not a clickable hyperlink in rendered docs. Standard practice for admin scripts.
- **Fix 8 (473 nav orphans) — Downgrade from CRITICAL to INVALID.** The files exist; the finding is a false positive as verified above (all pillar dirs populated, 552 .md files in docs/).
- **Fix 7 (FINRA 25-07 in CONTROL-INDEX) — Upgrade from WARNING to valid.** FINRA 25-07 is explicitly listed in the project's target regulations (AGENTS.md, copilot-instructions.md) but absent from the Regulatory Alignment section. This is a genuine gap.
FIX_ORDERING:
- **Fix 3 (prohibited language)** → Apply first. Zero dependencies, zero risk, directly violates documented FSI language rules. Single word change.
- **Fixes 4 & 5 (citation cleanup in evidence-pack-assembly.md and scope-creep-detection.md)** → Apply second. Independent of each other; can be parallel. ~22 total citation markers to remove/convert. Low risk but labor-intensive — verify no citations have corresponding footnotes before removing.
- **Fix 6 (templates/README.md)** → Apply third. One-line table addition, zero risk.
- **Fixes 1 & 2 (version footer corrections)** → Apply last and together. These are coupled (both reference v1.3). **Requires decision:** is v1.3 intentional (pre-release) or a mistake? The parent commit `211bb19` message is "chore: bump version references to v1.2.51" — if that commit intentionally left these as v1.3, it may signal a planned version. If not, both should revert to v1.2.51.
- **Fix 7 (FINRA 25-07)** → Apply with fixes 1 & 2 since it touches the same file (CONTROL-INDEX.md).
- **Fix 8** → Do not apply. Invalid finding.
OVER_ENGINEERED:
- **Fix 8 (473 nav orphans):** Entirely spurious. The worktree has 552 .md files under `docs/` and all pillar directories are fully populated. Investigating or "ensuring all referenced docs exist" would be wasted effort on a false positive.
- **Fix 5 (template broken links):** The template's placeholder paths are intentional authoring scaffolding. "Fixing" them would either remove useful guidance or require inventing concrete paths that would themselves go stale.
- **Fixes 4 & 5 (citation cleanup):** The review correctly identifies the issue but the proposed fix ("convert to proper footnotes or inline references") is over-scoped. Simply removing the orphaned citation markers is sufficient — there are no corresponding reference lists to convert *to*. Adding a bibliography section (mentioned in GAPS) would be scope creep beyond the review's mandate.
CRITIQUE_SUMMARY: The review correctly identifies 3 real issues (prohibited language, orphaned citations, incomplete README) and 2 version-footer mismatches requiring intent verification, but inflates severity by classifying template placeholders and a false-positive nav count as CRITICAL — the true actionable critical count is 1 (the prohibited language violation), not 6. The claimed 488 issue count is dominated by the invalid 473 nav orphan finding.