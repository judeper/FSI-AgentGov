I've now read all 25 files in scope. Let me compile the review findings.
ISSUE_COUNT: 10
CRITICAL:
- NONE
WARNINGS:
- docs/playbooks/control-implementations/2.9/powershell-setup.md: Uses `$Tier` parameter/variable (lines 78-93) instead of framework-standard "Zone" terminology; the comment even says "Zone 2 governance classification" but the param is `$Tier`
- docs/playbooks/control-implementations/2.11/verification-testing.md: Protected class #9 lists "Good Faith Exercise of CCPA Rights" (line 19) — "CCPA" here means Consumer Credit Protection Act but will be confused with California Consumer Privacy Act; NEEDS_HUMAN_REVIEW
- docs/playbooks/control-implementations/2.7/powershell-setup.md: `$connectorUsage = @()` initialized on line 34 but never populated; the "Get Connectors in Use" section collects data to console only
- Formatting inconsistency: 2.7 playbooks use blockquote intro + footer metadata (`*Updated: January 2026 | Version: v1.2*`); 2.8–2.13 playbooks use header metadata (`**Last Updated:** January 2026`) with bottom nav links and no footer — should be uniform
- 6 pre-pass broken URLs are all false positives: 3 are authenticated Microsoft portal URLs returning HTTP 405 to unauthenticated GET (`admin.powerplatform.microsoft.com`, `make.powerautomate.com`, `copilotstudio.microsoft.com`); 3 use `tenant` as an intentional placeholder in example scripts (`tenant.sharepoint.com`, `tenant-admin.sharepoint.com`)
- ~300 pre-pass nav orphans are a worktree artifact — mkdocs.yml references the full site but this worktree contains only the files under active review; not a content defect
GAPS:
- NONE
FIXES:
1. docs/playbooks/control-implementations/2.9/powershell-setup.md: Rename parameter from `$Tier` to `$Zone` and update the `ValidateSet`, `$thresholds` hash keys, and all display strings (`"Tier $Tier"` → `"Zone $Zone"`) in the "Check Agent Error Rates" script to match framework terminology
2. docs/playbooks/control-implementations/2.7/powershell-setup.md: Either populate `$connectorUsage` with connector/flow data inside the foreach loops or remove the unused variable initialization on line 34
3. docs/playbooks/control-implementations/2.11/verification-testing.md: Clarify protected class #9 — expand "CCPA" to "Consumer Credit Protection Act" to avoid confusion with California Consumer Privacy Act (NEEDS_HUMAN_REVIEW)
4. docs/playbooks/control-implementations/2.8/portal-walkthrough.md through 2.13/powershell-setup.md (22 files): Add footer metadata line (`*Updated: January 2026 | Version: v1.2*`) for consistency with 2.7 playbooks, or conversely update 2.7 playbooks to use the header metadata format
STATUS: ISSUES_FOUND