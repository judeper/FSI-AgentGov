All findings verified against the actual files. Here is the risk analysis:
---
**HIGH_RISK_FIXES:**
NONE
All proposed fixes target `index.md` content or individual control metadata. No fix modifies cross-reference targets, filenames, or `mkdocs.yml` navigation entries. The files already exist in nav (lines 157-158 of mkdocs.yml), so adding them to the index page's markdown list carries zero build risk. Fix 7 (removing the sardine.ai link) slightly reduces reference material but the link is confirmed 404—keeping it is worse.
**SEVERITY_CORRECTIONS:**
- Fix 4 (footer version/date): Classified as WARNING but is actually **LOW/COSMETIC**. The footer `v1.2 - January 2026` is an informational stamp on the index page only; it doesn't affect control validity or build. Updating it is fine but should not block shipping the critical fixes.
- Fix 5 (2.22 missing `Last Verified`): Correctly classified as WARNING. Confirmed: 2.22 is the only control out of 24 missing this field. All other 23 controls have it. However, note that 2.22 *does* have `**Last UI Verified:** February 2026` (line 6), so this is a missing *supplementary* field, not a missing verification date entirely. Severity could be downgraded to LOW.
- Fix 8 (SEC/FTC 403s): Correctly classified as NEEDS_HUMAN_REVIEW. These are government sites known to block automated crawlers. Do NOT auto-fix these URLs—they are almost certainly valid in a browser. Risk of "fixing" them is introducing incorrect replacements.
**FIX_ORDERING:**
1. **Fix 1 + Fix 2 + Fix 3 together** (index.md count, list, and categories table) — these are interdependent edits to the same file and must ship as one atomic commit. Doing Fix 1 without Fix 2 creates an inconsistency (claims 24 controls but only lists 22).
2. **Fix 4** (footer date) — same file, bundle with above.
3. **Fix 5** (2.22 Last Verified) — independent, can go in separate commit.
4. **Fix 6** (2.24 duplicate URL) — independent, can go in separate commit.
5. **Fix 7** (2.17 sardine.ai removal) — independent, can go in separate commit.
6. **Fix 8** (SEC/FTC URLs) — deferred to human review; do NOT auto-fix.
**OVER_ENGINEERED:**
- Fix 3 (categories table restructuring): The proposal to create new category names ("Adversarial & Security Testing", rename "Customer Transparency" to "Customer Transparency & Marketing", add "Feature & Session Governance") is **over-engineered**. The simpler fix is to extend the existing table with one or two rows using straightforward category names that match the control titles, rather than renaming existing categories. Renaming "Customer Transparency" risks confusing readers familiar with the current taxonomy. Recommend: add two rows (`| Adversarial & Security Testing | 2.20 | Red team exercises |` and `| Feature & Session Governance | 2.21-2.24 | Marketing claims, timeouts, consent, feature restrictions |`) without renaming existing rows.
**CRITIQUE_SUMMARY:** The review is accurate and thorough—all 3 critical findings (count, list, categories) are confirmed against the actual files and must be fixed together as one atomic change. The SEC/FTC 403 URLs are correctly deferred to human review, and the only over-engineering risk is in the categories table restructuring which should be simplified to additive rows rather than taxonomy renames.