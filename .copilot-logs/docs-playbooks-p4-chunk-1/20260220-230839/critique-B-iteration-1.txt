Now let me verify the H1 patterns and footer navigation claims.
Let me also check which files have footer links and which files are in scope for 4.7.
Now I have all the data needed. Here is my risk analysis:
---
**HIGH_RISK_FIXES:**
- NONE — All proposed fixes are low-risk documentation edits (no code, no build config, no nav changes to `mkdocs.yml`). Fixes 1-2 correct PowerShell property/cmdlet names within markdown code blocks; fixes 3-14 add footer navigation links. None alter `mkdocs.yml` or cross-reference link targets, so `mkdocs build --strict` breakage risk is negligible.
**SEVERITY_CORRECTIONS:**
- Fixes 1-2 (property `RestrictedSearchEnabled` / cmdlet `Get-SPOSearchSiteConfiguration`): **UPGRADE from WARNING to CRITICAL-adjacent.** These are incorrect PowerShell property/cmdlet names in a troubleshooting guide. An FSI admin copy-pasting these commands will get errors or wrong results. The 4.1/powershell-setup.md (same control) uses `EnableRestrictedSearchAllList` and `Get-SPOTenantRestrictedSearchAllowedList`; all 4.6 files confirm that pattern (8 occurrences). The troubleshooting file is internally inconsistent with its own sibling playbook — this is a functional accuracy bug, not a style issue. However, the review correctly flags NEEDS_HUMAN_REVIEW since the actual SPO cmdlet should be verified against a live tenant before committing.
- Fix 15 (H1 title standardization): Severity is correct as WARNING but see OVER_ENGINEERED below.
- Fix 16 (placeholder URLs): Correctly classified as no-action-needed. Agree.
**FIX_ORDERING:**
- **Fixes 1-2 should be applied together** — they're in the same file (`4.1/troubleshooting.md`) and both address the same PowerShell accuracy issue. Apply as a single atomic commit.
- **Fixes 3-14 are independent and can be parallelized** — all 12 files get the same structural addition (footer nav links). No interdependencies. Apply as a single batch commit.
- **Fix 15 (H1 standardization) should be deferred or done last** — it touches 8 files across 4.1 and 4.2, is cosmetic, and has the widest blast radius. Should NOT be bundled with fixes 1-2 or 3-14.
- No ordering dependencies between fixes 1-2 and 3-14.
**OVER_ENGINEERED:**
- **Fix 15 (H1 title standardization across 8 files):** Over-engineered for this batch scope. The review correctly identifies the inconsistency but renaming H1 titles across 8 files for 4.1-4.2 is cosmetic normalization, not a correctness fix. It changes rendered page headings visible to users, touches files that may be owned by a different worktree/batch, and the two patterns (`# Type: Control X.X - Name` vs `# Control X.X: Name - Type`) are both clear to readers. Recommend deferring to a dedicated normalization pass across ALL pillars rather than fixing only P4 controls 4.1-4.2 — partial normalization is worse than consistent inconsistency.
- **Fixes 3-14 (footer links for 12 files):** Borderline. The review correctly identifies that 4.3-4.6 portal-walkthrough files already HAVE footer links, but the other 3 playbook types per control do not. This is a real gap. However, adding 12 footer links is mechanical and low-value compared to fixes 1-2. Not truly over-engineered, but should be lower priority than the PowerShell accuracy fixes.
**CRITIQUE_SUMMARY:** The review is thorough and accurate — all 16 issues verified against actual file content. The only severity miss is fixes 1-2, which should be treated as higher priority than WARNING since they contain incorrect PowerShell commands that will fail when executed by admins; fix 15 should be deferred as it's cosmetic normalization with a wide blast radius better suited to a cross-pillar sweep.