Now I have all the evidence needed to complete the risk analysis.
---
**HIGH_RISK_FIXES:**
- **Fixes 2-3 (4.1/troubleshooting.md cmdlet changes):** HIGH RISK. The troubleshooting file uses `Get-SPOTenant | Select-Object RestrictedSearchEnabled` and `Get-SPOSearchSiteConfiguration` — these may be intentionally different diagnostic cmdlets (checking tenant-level search config vs. the setup script's allow-list management cmdlets `EnableRestrictedSearchAllList` / `Get-SPOTenantRestrictedSearchAllowedList`). Blindly aligning to powershell-setup.md assumes the setup file is authoritative without verifying against actual Microsoft SPO module documentation. If neither property name is correct, both files are wrong and the fix propagates the error. The NEEDS_HUMAN_REVIEW tag is essential — **do not auto-apply these fixes.**
**SEVERITY_CORRECTIONS:**
- **SOX 404 → SOX 802 (Fix 4):** Severity is correctly WARNED but should arguably be **CRITICAL**. SOX 404 governs internal controls assessment; SOX 802 (18 U.S.C. §1520) governs record retention/destruction. Citing the wrong section in a regulatory retention table for FSI administrators is a substantive regulatory error, not a cosmetic warning. The powershell-setup.md at line 87 already has the correct "SOX 802" citation, confirming this is a clear mistake.
- **GLBA "5-7 years" (WARNING):** Correctly classified. GLBA does not mandate specific retention periods; this is industry practice. Leaving it as NEEDS_HUMAN_REVIEW is appropriate — the fix would be to add a caveat like "(industry practice)" rather than remove it.
- **Governance Level 1/2/3/4 (WARNING):** Correctly classified as informational since no fix was proposed. However, "Level 4" having no framework definition is a content accuracy issue that warrants a backlog item.
**FIX_ORDERING:**
- **Fixes 2 + 3 must be atomic** — both modify 4.1/troubleshooting.md and are logically coupled (property name + cmdlet name for the same feature). Apply together or not at all.
- **Fix 4 (SOX citation) is independent** — safe to apply without dependencies.
- **Fix 1 (4.7 RCD UI text) is independent** — safe to apply. Aligns with confirmed UI descriptions in 4.1 (line 37) and 4.6 (line 42).
- **Fixes 5-16 (cross-nav footers) are independent of each other** — can be applied in any order. Must be inserted *before* the `*Updated:*` footer line in 4.3-4.6 files.
**OVER_ENGINEERED:**
- **Fixes 5-16 (12 cross-nav footer additions):** Over-engineered for a "fix" batch. Missing cross-nav links are a **consistency gap**, not an error — they don't mislead administrators or create incorrect behavior. These 12 files function correctly without them. This is enhancement work that should be a separate commit or backlog item, not bundled with correctness fixes. Mixing cosmetic improvements with substantive error corrections inflates the changeset and makes review harder.
- **Title format inconsistency (WARNING, no fix proposed):** Correctly excluded from fixes — this is a style normalization task, not an error.
**CRITIQUE_SUMMARY:** Fixes 1 and 4 are safe, low-risk corrections with clear evidence in the codebase. Fixes 2-3 carry genuine risk of replacing one wrong cmdlet with another wrong cmdlet and must not be applied without Microsoft SPO module verification. Fixes 5-16 should be split into a separate consistency-improvement commit to keep the error-correction changeset clean and reviewable.