Now I have all the data needed. Here's the risk and impact analysis:
---
**HIGH_RISK_FIXES:**
- **Fix #1 (mkdocs.yml / worktree — 473 missing files): INVALID FIX — DO NOT EXECUTE.** The review's CRITICAL finding is **factually wrong**. All 476 nav-referenced `docs/` files are present in this worktree. Controls, framework, playbooks, reference, getting-started, and downloads directories all exist with full content. Executing this "fix" could introduce confusion (re-running hooks, re-checking out files) with zero benefit. The 473-issue count that inflates the total to 488 is fabricated from a false premise.
**SEVERITY_CORRECTIONS:**
- **CRITICAL → INVALID: mkdocs.yml 473 nav entries.** Files exist. `mkdocs build --strict` will not fail for missing referenced files. The entire premise is wrong. This eliminates 473 of the reported 488 issues, reducing the **real issue count to ~15**.
- **CRITICAL → INFO: contoso.com URLs.** The review itself correctly self-dismisses these as intentional placeholder values ("not actual hyperlinks"), then contradictorily classifies them as CRITICAL. These are example data inside screenshot capture specs for `EXPECTED.md` files that are local-only and never published. Severity should be INFO at most. Adding `*.contoso.com` to a URL-checker exclusion list is a reasonable tooling improvement but is not a content fix.
- **WARNING → LOW: Format inconsistency (13 table-format vs detailed-format).** Real observation, but `EXPECTED.md` files are local-only verification checklists (gitignored images, never published to GitHub Pages). Format consistency across internal verification files is a nice-to-have, not a warning. Additionally, the review only examined 25 of the 70 EXPECTED.md files in this repo — the full format distribution is unknown and may differ from what's reported.
**FIX_ORDERING:**
- Fix #1: **SKIP** — no fix needed, files already exist.
- Fix #2: **OPTIONAL** — tooling improvement only (add contoso.com to URL exclusion list). No content change required. Can be done independently at any time.
- Fix #3 before Fix #4: If proceeding with format standardization (Fix #3, reformatting 13 files), do it **before** adding back-links (Fix #4), since the reformatting may change the file structure where the back-link would be inserted. However, both are low-priority.
**OVER_ENGINEERED:**
- **Fix #3 (Standardize 13 EXPECTED.md files to detailed format): OVER-ENGINEERED.** Reformatting 13 local-only verification checklists from a working table format to a verbose `### Screenshot N:` subsection format adds significant change volume (~13 files × substantial restructuring) for zero user-facing impact. These files are never published, never built by mkdocs, and serve only as maintainer checklists for screenshot verification. Both formats accomplish the same goal. The review also only analyzed 25 of 70 EXPECTED.md files — standardizing 13 while leaving 45 others unexamined creates incomplete consistency anyway.
- **Fix #4 (Add back-links to 24+ EXPECTED.md files): OVER-ENGINEERED.** Back-links in local-only files provide marginal navigability improvement for maintainers who already know the `docs/images/{control-id}/` → `docs/controls/pillar-N/` mapping. With 70 EXPECTED.md files (not the 25 reviewed), this would actually require touching 69 files, not 24. Low value, high churn.
**CRITIQUE_SUMMARY:** The review's headline finding (473 missing files, CRITICAL) is **false** — all referenced docs exist in this worktree — which inflates the issue count from ~15 real observations to 488 and creates a phantom Fix #1 that should not be executed. The remaining findings (format inconsistency, missing back-links) are real but low-severity cosmetic issues in local-only files, and the proposed fixes are over-engineered relative to their impact.