Now I have all 25 EXPECTED.md files reviewed and the nav orphan count (92 + 80 + 301 = 473). Here's the final report:
ISSUE_COUNT: 488
CRITICAL:
- mkdocs.yml: 473 nav entries reference `docs/` files not present in this worktree — `mkdocs build --strict` will fail. This is a systemic worktree-scoping issue; all controls, framework, playbooks, reference, getting-started, and downloads files are missing from the working tree while `mkdocs.yml` references them.
- docs/images/3.11/EXPECTED.md: 2 example URLs using contoso.com domain flagged as unreachable (lines 90, 451: `https://sharepoint.contoso.com/agents/` and `https://sharepoint.contoso.com/governance/decommissioned/`). These are intentional placeholder values inside "What to capture" descriptions, not actual hyperlinks — but confirmed broken by automated pre-pass.
WARNINGS:
- docs/images/1.1/EXPECTED.md, 1.6, 1.7, 1.8, 1.9, 1.12, 1.15, 2.10, 2.11, 3.2, 3.3, 3.4, 3.5 (13 files): Use inconsistent format — table-style layout with `## Expected Screenshots` heading and no "Screenshot Specifications" in title, versus the detailed-section format with `## Required Screenshots` and `### Screenshot N:` subsections used by the other 12 files (1.22, 1.23, 1.24, 1.25, 2.16, 2.17, 2.18, 2.19, 2.20, 2.21, 3.10, 3.11).
GAPS:
- 13 table-format EXPECTED.md files (1.1, 1.6, 1.7, 1.8, 1.9, 1.12, 1.15, 2.10, 2.11, 3.2, 3.3, 3.4, 3.5) lack a "Notes for Verification" section present in all detailed-format files — missing guidance on environment preferences, data sensitivity, timestamp requirements, and UI verification notes.
- Only docs/images/3.11/EXPECTED.md includes a back-link to its parent control doc; 24 other EXPECTED.md files do not, reducing navigability.
FIXES:
1. mkdocs.yml / worktree: Ensure all 473 referenced `docs/` content files are present in the worktree. These files likely exist in the main repo — the worktree post-create hook or `git-wt step copy-ignored` may need to include `docs/` content directories, or this worktree should be checked out with the full file tree.
2. docs/images/3.11/EXPECTED.md: No content fix needed for contoso.com URLs — they are intentional example metadata values in screenshot capture specifications. Consider adding `*.contoso.com` to the automated URL-checker exclusion list to prevent false positives.
3. 13 EXPECTED.md files (1.1, 1.6, 1.7, 1.8, 1.9, 1.12, 1.15, 2.10, 2.11, 3.2, 3.3, 3.4, 3.5): Standardize to the detailed format — add "Screenshot Specifications" to title, rename `## Expected Screenshots` to `## Required Screenshots`, convert table rows to `### Screenshot N:` subsections with portal path and capture details, and add a `## Notes for Verification` section.
4. All 24 EXPECTED.md files (excluding 3.11): Add back-link to parent control doc at end of file, following the pattern: `[Back to Control X.X](../../controls/pillar-N-name/X.X-control-name.md)`
STATUS: ISSUES_FOUND