Now I have all 25 files. Let me compile the review findings.
ISSUE_COUNT: 7
CRITICAL:
- 1.24/powershell-setup.md: `'microsoft.openai/accounts'` is not a valid Azure resource type — Azure OpenAI resources use `microsoft.cognitiveservices/accounts` with `kind = 'OpenAI'`; Resource Graph queries referencing this type will silently return zero results (appears in lines 81, 290)
- 1.24/verification-testing.md: Same invalid resource type `'microsoft.openai/accounts'` in KQL query at line 130; will produce incomplete AI inventory
- 1.24/powershell-setup.md (line 133): `(Get-AzAccessToken -ResourceUrl "https://management.azure.com").Token` used as plain string, but Az.Accounts 5.0+ returns SecureString — will cause `Invoke-RestMethod` auth failure; the 1.25 playbook correctly handles both formats at lines 27-32 but 1.24 does not
- mkdocs.yml: 300+ nav entries reference files not found in this worktree (systemic — likely expected for isolated worktree checkout; `mkdocs build --strict` will fail until all docs are present)
WARNINGS:
- 1.24/powershell-setup.md (lines 46-47): AI-SPM extension check uses `SensitiveDataDiscovery` and `AgentlessDiscoveryForKubernetes` as proxy checks rather than the actual AI security posture management extension name — NEEDS_HUMAN_REVIEW
- 1.20/portal-walkthrough.md: `https://admin.powerplatform.microsoft.com` flagged HTTP 405 by automated scan — false positive (authenticated portal URL, no change needed)
- 1.24/powershell-setup.md: `https://management.azure.com` endpoints flagged HTTP 400 by automated scan — false positive (REST API endpoints with runtime `$subscriptionId` variable, no change needed)
GAPS:
- NONE
FIXES:
1. 1.24/powershell-setup.md: Replace all instances of `'microsoft.openai/accounts'` in Resource Graph queries with a kind-based filter, e.g. change `'microsoft.openai/accounts'` to include `| where kind =~ 'OpenAI'` as an additional filter on `microsoft.cognitiveservices/accounts`, or simply remove the non-existent resource type
2. 1.24/verification-testing.md: Replace `'microsoft.openai/accounts'` in the KQL Resource Graph query (line 130) with same approach as fix #1
3. 1.24/powershell-setup.md: Add SecureString handling for `Get-AzAccessToken` token at line 133 — use the pattern from 1.25/powershell-setup.md lines 27-32 that checks `$azToken.Token -is [System.Security.SecureString]` and marshals accordingly
4. mkdocs.yml nav orphans: Ensure all 300+ referenced files exist under `docs/` before running `mkdocs build --strict` (likely a worktree isolation artifact — no action needed if building from the main worktree)
5. 1.20/portal-walkthrough.md: No change needed for `https://admin.powerplatform.microsoft.com` — valid authenticated portal URL (automated scan false positive)
6. 1.24/powershell-setup.md: No change needed for `https://management.azure.com` URLs — valid API endpoints used with runtime authentication (automated scan false positive)
STATUS: ISSUES_FOUND