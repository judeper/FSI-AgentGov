All files verified. Now let me implement the fixes.
ISSUE_COUNT: 5
CRITICAL:
- 1.24/powershell-setup.md (lines 80, 290): `'microsoft.openai/accounts'` is not a valid Azure resource type — Azure OpenAI resources use `microsoft.cognitiveservices/accounts` with `kind = 'OpenAI'`; Resource Graph queries referencing this type silently return zero results
- 1.24/verification-testing.md (line 129): Same invalid resource type `'microsoft.openai/accounts'` in KQL query; produces incomplete AI inventory
- 1.24/powershell-setup.md (line 133): `(Get-AzAccessToken -ResourceUrl "https://management.azure.com").Token` used as plain string, but Az.Accounts 5.0+ returns SecureString — causes `Invoke-RestMethod` auth failure; 1.25 playbook correctly handles both formats at lines 27-32
WARNINGS:
- 1.24/powershell-setup.md (lines 46-47): `$aiSpm` variable assigned but never used (dead code); additionally uses `SensitiveDataDiscovery`/`AgentlessDiscoveryForKubernetes` as proxy checks rather than the actual AI-SPM extension name — NEEDS_HUMAN_REVIEW
- advanced-implementations/deny-event-correlation-report/app-insights-rai-telemetry.md (line 255): Same `Get-AzAccessToken` SecureString vulnerability — `.Token` used directly as string while prerequisites specify Az.Accounts 3.0+ (which includes 5.0+ where `.Token` is SecureString)
GAPS:
- NONE
FIXES:
1. 1.24/powershell-setup.md: Remove `'microsoft.openai/accounts'` lines at lines 80 and 290 — Azure OpenAI resources already appear as `microsoft.cognitiveservices/accounts`, so all are already captured
2. 1.24/verification-testing.md: Remove `'microsoft.openai/accounts'` line at line 129 (same rationale as fix #1)
3. 1.24/powershell-setup.md (line 133): Add SecureString handling using pattern from 1.25/powershell-setup.md lines 25-32 that checks `$azToken.Token -is [System.Security.SecureString]` and marshals accordingly
4. 1.24/powershell-setup.md (line 46-47): Either use `$aiSpm` in the output or remove the dead assignment — NEEDS_HUMAN_REVIEW for correct AI-SPM extension name
5. advanced-implementations/deny-event-correlation-report/app-insights-rai-telemetry.md (line 255): Add SecureString handling using same pattern as fix #3
STATUS: ISSUES_FOUND
Now implementing the fixes:
Edits are blocked by permissions on this worktree. The reconciled review is the deliverable. Here is the final verified output:
---
ISSUE_COUNT: 5
CRITICAL:
- 1.24/powershell-setup.md (lines 80, 290): `'microsoft.openai/accounts'` is not a valid Azure resource type — Azure OpenAI resources use `microsoft.cognitiveservices/accounts`; Resource Graph queries referencing this type silently return zero results
- 1.24/verification-testing.md (line 129): Same invalid resource type `'microsoft.openai/accounts'` in KQL query; produces incomplete AI inventory
- 1.24/powershell-setup.md (line 133): `(Get-AzAccessToken).Token` used as plain string, but Az.Accounts 5.0+ returns SecureString — causes `Invoke-RestMethod` auth failure; 1.25 playbook handles both formats correctly at lines 25-32
WARNINGS:
- 1.24/powershell-setup.md (lines 46-47): `$aiSpm` variable assigned but never used (dead code); also uses proxy extension names rather than actual AI-SPM extension name — NEEDS_HUMAN_REVIEW
- advanced-implementations/deny-event-correlation-report/app-insights-rai-telemetry.md (line 255): Same `Get-AzAccessToken` SecureString vulnerability — `.Token` used directly as string while prerequisites specify Az.Accounts 3.0+
GAPS:
- NONE
FIXES:
1. 1.24/powershell-setup.md: Remove the `'microsoft.openai/accounts'` line at lines 80 and 290 — Azure OpenAI resources already appear under `microsoft.cognitiveservices/accounts`
2. 1.24/verification-testing.md: Remove the `'microsoft.openai/accounts'` line at line 129 (same rationale)
3. 1.24/powershell-setup.md (line 133): Replace `$token = (Get-AzAccessToken -ResourceUrl "https://management.azure.com").Token` with SecureString-aware pattern from 1.25/powershell-setup.md lines 25-32
4. 1.24/powershell-setup.md (line 46): Either use `$aiSpm` in the output at line 47 or remove the dead assignment — NEEDS_HUMAN_REVIEW for correct AI-SPM extension name
5. app-insights-rai-telemetry.md (line 255): Replace `$token = (Get-AzAccessToken ...).Token` with same SecureString-aware pattern as fix #3
STATUS: ISSUES_FOUND