Now I have full context. Here's the risk analysis:
---
**HIGH_RISK_FIXES:**
- NONE. All three substantive fixes (Fix #1, #2, #3) are scoped to code blocks inside markdown files. They do not alter file names, headings, anchors, or cross-references. `mkdocs build` is unaffected by inline code-block content changes.
**SEVERITY_CORRECTIONS:**
- Fix #4 (mkdocs.yml nav orphans) classified CRITICAL is **over-classified → downgrade to INFO**. This is a worktree isolation artifact, not a documentation defect. The review itself acknowledges "no action needed if building from the main worktree." It should not be listed as CRITICAL alongside genuine code errors.
- The AI-SPM extension name WARNING (lines 46-47) severity is **correctly classified**. The code uses `SensitiveDataDiscovery` and `AgentlessDiscoveryForKubernetes` as proxy checks — these are real CSPM extension names but are not the AI-SPM extension itself. The NEEDS_HUMAN_REVIEW tag is appropriate since the correct extension name depends on current Azure API surface.
**FIX_ORDERING:**
- Fix #1 and #2 are the same logical change (replace `microsoft.openai/accounts` with a valid filter) applied to two files. Apply together as a single atomic commit.
- Fix #3 (SecureString handling) is independent. Apply as a separate commit after Fix #1/#2 since it touches the same file (`1.24/powershell-setup.md`) — applying sequentially avoids merge noise.
- Recommended order: **Fix #1+#2 → Fix #3**. Fixes #4/#5/#6 require no action.
**OVER_ENGINEERED:**
- Fix #1/#2 proposed approach ("add `| where kind =~ 'OpenAI'` as additional filter on `microsoft.cognitiveservices/accounts`") is slightly over-engineered. The simplest correct fix is to **remove the `'microsoft.openai/accounts'` line entirely** — Azure OpenAI resources already appear as `microsoft.cognitiveservices/accounts` with `kind = 'OpenAI'`. The existing query on line 77/288 already includes `microsoft.cognitiveservices/accounts`, so all OpenAI resources are already captured. Adding a `kind` filter would actually *narrow* the result set and potentially miss non-OpenAI cognitive services resources that are relevant to AI inventory. Just delete the phantom resource type line.
**CRITIQUE_SUMMARY:** The three CRITICAL code-level findings (invalid resource type × 2, SecureString handling) are genuine, correctly identified, and safe to fix — they are confined to fenced code blocks with zero cross-reference or build risk. The mkdocs.yml nav finding should be downgraded from CRITICAL to INFO since it's an expected worktree artifact, and the proposed fix for the resource type should be simplified from "add kind filter" to "remove phantom line."