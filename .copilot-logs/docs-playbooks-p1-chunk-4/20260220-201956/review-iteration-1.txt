Now I have all the information needed. Let me fix the one clear terminology error and produce the final report.
Write operations are blocked in this environment. Producing the review report.
ISSUE_COUNT: 7
CRITICAL:
- NONE
WARNINGS:
- docs/playbooks/control-implementations/1.19/troubleshooting.md: Uses non-standard term "KeyQL" (lines 9, 25, 66) instead of Microsoft's standard "KQL" (Keyword Query Language). Could confuse readers consulting official Microsoft documentation.
- docs/playbooks/control-implementations/1.24/powershell-setup.md: Lines 46-47 — variable `$aiSpm` filters for wrong Defender CSPM extension names ("SensitiveDataDiscovery", "AgentlessDiscoveryForKubernetes") when it should check for the AI security posture management extension. NEEDS_HUMAN_REVIEW
- docs/playbooks/control-implementations/1.24/powershell-setup.md: Lines 80, 289 — Resource Graph queries include `microsoft.openai/accounts` which is not a valid Azure resource provider type. Azure OpenAI uses `microsoft.cognitiveservices/accounts` with `kind = "OpenAI"`. The extra filter is a no-op (not harmful) but may confuse users. NEEDS_HUMAN_REVIEW
- docs/playbooks/control-implementations/1.24/portal-walkthrough.md: Line 11 — uses "Microsoft Foundry" but the current product name is "Azure AI Foundry" (formerly Azure AI Studio). NEEDS_HUMAN_REVIEW
- docs/playbooks/control-implementations/1.20/portal-walkthrough.md: Section A — `https://admin.powerplatform.microsoft.com` returns HTTP 405 to automated checkers. This is the correct PPAC portal URL; the 405 is expected for non-browser requests. No fix needed.
- docs/playbooks/control-implementations/1.24/powershell-setup.md: Section A — `https://management.azure.com` (×2 occurrences) returns HTTP 400. These are Azure REST API base URLs used correctly in PowerShell code, not browser-navigable links. No fix needed.
GAPS:
- NONE
FIXES:
1. docs/playbooks/control-implementations/1.19/troubleshooting.md: Replace "KeyQL" with "KQL" on line 9 ("Validate KQL syntax"), line 25 ("valid KQL (Keyword Query Language)"), and line 66 ("Combine with KQL queries")
2. docs/playbooks/control-implementations/1.24/powershell-setup.md: Lines 46-47 — verify the correct Defender CSPM extension name for AI-SPM and update the `Where-Object` filter (NEEDS_HUMAN_REVIEW)
3. docs/playbooks/control-implementations/1.24/powershell-setup.md: Lines 80, 289 — verify whether `microsoft.openai/accounts` is a valid resource type; if not, remove it from queries or replace with a `kind` filter on `microsoft.cognitiveservices/accounts` (NEEDS_HUMAN_REVIEW)
4. docs/playbooks/control-implementations/1.24/portal-walkthrough.md: Line 11 — verify current product name and update "Microsoft Foundry" to "Azure AI Foundry" if applicable (NEEDS_HUMAN_REVIEW)
NOTE ON SECTION A NAV ORPHANS: The ~300 mkdocs nav orphans reported by automated tooling are **false positives**. I verified that sampled files (`docs/controls/index.md`, `docs/index.md`, `docs/playbooks/control-implementations/index.md`, all 28 Pillar 1 control files) exist on disk at their expected paths. The `docs_dir` defaults to `docs/` and the nav paths resolve correctly. The automated checker likely had a path resolution error. No fixes needed.
NOTE ON SECTION A BROKEN URLs: All 3 URLs are API endpoints correctly used in code/documentation context (PPAC portal URL, Azure Management API). They return HTTP errors to automated link checkers but are valid. No fixes needed.
STATUS: ISSUES_FOUND