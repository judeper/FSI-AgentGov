Now let me verify the specific Az.Accounts token issue in 1.24 vs 1.25.
Confirmed: 1.24 `Get-AzAccessToken` token extraction doesn't handle SecureString (Az.Accounts 5.0+), while 1.25 does. Let me fix this and write the report.
Cannot write to the worktree (permission denied). Reporting review findings only.
ISSUE_COUNT: 5
CRITICAL:
- docs/playbooks/control-implementations/1.24/powershell-setup.md: Line 131 `$token = (Get-AzAccessToken -ResourceUrl "https://management.azure.com").Token` does not handle Az.Accounts 5.0+ where `.Token` returns a `SecureString`. The bearer header would contain `"Bearer System.Security.SecureString"` instead of the actual token, causing `Invoke-RestMethod` to fail. The 1.25 playbook correctly handles both formats (lines 25-32).
WARNINGS:
- docs/playbooks/control-implementations/1.20/portal-walkthrough.md: Pre-verified URL `https://admin.powerplatform.microsoft.com` returns HTTP 405 — this is a false positive; the URL is correct but the portal rejects unauthenticated HEAD/GET requests. No documentation change needed.
- docs/playbooks/control-implementations/1.24/powershell-setup.md: Pre-verified URLs `https://management.azure.com` and the attackPaths REST URI return HTTP 400 — these are API endpoints used in code, not browsable links. False positives from URL checker. No documentation change needed.
- mkdocs.yml: ~300 nav entries reference files not present in this worktree. This is a systemic worktree artifact (the worktree contains only the playbook subset), not a documentation defect. No fix needed within scoped files.
GAPS:
- NONE
FIXES:
1. docs/playbooks/control-implementations/1.24/powershell-setup.md (line 131): Replace `$token = (Get-AzAccessToken -ResourceUrl "https://management.azure.com").Token` with the SecureString-safe pattern used in 1.25/powershell-setup.md:
   ```powershell
   $azToken = (Get-AzAccessToken -ResourceUrl "https://management.azure.com")
   if ($azToken.Token -is [System.Security.SecureString]) {
       $token = [System.Runtime.InteropServices.Marshal]::PtrToStringBSTR(
           [System.Runtime.InteropServices.Marshal]::SecureStringToBSTR($azToken.Token))
   } else {
       $token = $azToken.Token
   }
   ```
STATUS: ISSUES_FOUND