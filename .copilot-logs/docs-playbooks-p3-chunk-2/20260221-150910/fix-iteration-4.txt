Now let me see the footer of the 3.8 troubleshooting file and also get more context on the 3.7 powershell-setup.md DLP line:
Now I have all the context. Let me apply all 8 fixes:
Wait — the `AddedToGroup` fix isn't right. The issue is that `createdDateTime` isn't available in `AdditionalProperties` for group members. The property should be removed or replaced with something that actually returns useful data. Let me reconsider — the field is meant to show when a user was added to the group, but `Get-MgGroupMember` doesn't expose that directly. A better approach is to just remove that unreliable field entirely, or note it's not available.
The `@odata.type` value isn't useful for "AddedToGroup" either. Since `Get-MgGroupMember` doesn't expose when a member was added, the most honest fix is to replace with the user's `CreatedDateTime` (account creation date, which is a valid property) and rename the field:
Now let me verify all changes are correct:
Some files have changes from previous iterations. Let me verify my specific changes look correct:
All 8 fixes are applied correctly. Let me commit the changes:
The commit was blocked by permissions, but all file edits are staged and applied. Here's the summary:
CHANGES:
- docs/playbooks/control-implementations/3.8/powershell-setup.md: Changed `.NOTES` date from "January 2026" to "February 2026"; replaced unreliable `$member.AdditionalProperties.createdDateTime` with `$user.CreatedDateTime` (renamed field to `AccountCreated`)
- docs/playbooks/control-implementations/3.8/troubleshooting.md: Changed footer from "January 2026 | Version: v1.2" to "February 2026 | Version: v1.3"
- docs/playbooks/control-implementations/3.5/troubleshooting.md: Fixed `CurrentSpend` to `@{N='CurrentSpend';E={$_.CurrentSpend.Amount}}` (correct property path)
- docs/playbooks/control-implementations/3.7/troubleshooting.md: Replaced invalid `IsAdmin` with `EnvironmentType, IsDefault`; added caveat on undocumented `Internal.properties` access
- docs/playbooks/control-implementations/3.6/troubleshooting.md: Added caveat comment on undocumented `Internal.properties.isDisabled` access
- docs/playbooks/control-implementations/3.7/powershell-setup.md: Fixed `Get-DlpPolicy` to retrieve at tenant level then filter by environment name
FIX_STATUS: COMPLETE