I've verified all findings against the actual files. Here's the risk assessment:
---
**HIGH_RISK_FIXES:**
- **Fix 5 (SEC 17a-4 retention alignment):** HIGH RISK. The 1.2 reference ("7 years") and 1.4 reference ("6 years / 2190 days") may both be correct for different record types under SEC 17a-4. SEC 17a-4(a) requires 6 years for most records but SEC 17a-4(b) requires "life of the enterprise plus 3 years" for certain records, and some broker-dealer records have a 7-year obligation. Blindly aligning to one number without a domain expert risks introducing a regulatory inaccuracy. **Do not fix without human review.**
**SEVERITY_CORRECTIONS:**
- **Fix 1 (heading hierarchy) is correctly CRITICAL.** Confirmed: Steps 0–5 and 7–9 are `###` (H3), Step 6 alone is `##` (H2). This breaks mkdocs TOC nesting and reader navigation. Safe, zero-risk fix.
- **Fix 2 (date conflict) is correctly CRITICAL.** Confirmed: header says "January 2026", footer says "February 2026". Contradictory metadata in a compliance document undermines credibility. Safe fix.
- **Fixes 3 & 4 (1.4 title format) should be DOWNGRADED from WARNING to INFO/DEFERRED.** The repo has two coexisting title patterns across all pillars — `# Type: Control X.X - Name` (used by 1.17, 1.19, 1.27, 2.14) vs `# Control X.X: Name - Type` (used by 2.4, 2.5, 2.6, 3.5, 4.7). Control 1.4 is not uniquely inconsistent; it follows Pattern B which ~40% of playbooks use. Fixing only 1.4 creates a false sense of consistency.
- **Fix 6 (missing prerequisite) is correctly WARNING.** Low risk to add a note.
- **Fix 7 (footer metadata) is correctly a GAP, not WARNING.** Purely cosmetic consistency.
**FIX_ORDERING:**
1. Fix 1 (heading `##` → `###`) — independent, zero risk, immediate
2. Fix 2 (date "January" → "February") — independent, zero risk, immediate
3. Fix 6 (add ExchangeOnlineManagement prerequisite) — independent, low risk
4. Fix 7 (footer metadata on 9 files) — independent, low priority batch
5. Fixes 3 & 4 (title rename) — **defer** until repo-wide title normalization is scoped
6. Fix 5 (SEC 17a-4 retention) — **blocked on human review**
**OVER_ENGINEERED:**
- **Fixes 3 & 4 (1.4 title format):** Renaming two files to match "Pattern A" when ~40% of the repo uses "Pattern B" is arbitrary scope. This should be a repo-wide normalization pass (all 284 playbooks), not a 2-file patch in this batch. Fixing it here creates an inconsistency with other Pattern B files (2.4, 2.5, 2.6, 3.5, 4.7, etc.).
- **Fix 5 (SEC 17a-4 alignment):** Proposing to "align to a single consistent value" without specifying which value, or acknowledging the regulation has different retention periods for different record types, oversimplifies the problem.
**CRITIQUE_SUMMARY:** The two CRITICAL fixes (heading hierarchy and date conflict) are correctly identified, verified, and safe to ship immediately. However, the title-format fixes are over-scoped for this batch and the SEC 17a-4 retention fix requires regulatory domain expertise before any change — applying it mechanically risks introducing a compliance documentation error, which is worse than the current inconsistency.