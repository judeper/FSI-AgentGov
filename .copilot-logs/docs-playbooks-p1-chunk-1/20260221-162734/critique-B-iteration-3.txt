Now I have complete data to render the risk analysis.
---
**HIGH_RISK_FIXES:**
- **Fix 4 (SEC 17a-4 retention alignment):** MEDIUM-HIGH risk. Three concerns: (1) Changes a numeric value inside a PowerShell code sample (`retention_days: 2190` → `2555`) — admins may have already deployed with 2190; changing it silently alters operational behavior for anyone copy-pasting. (2) **Incomplete scope** — `4.3/powershell-setup.md` L85 also uses `2190` with an explicit citation to `SEC 17a-4(a)`, which was not flagged by the review. Fixing only 1.4 while leaving 4.3 untouched creates a *new* inconsistency. (3) SEC 17a-4 genuinely specifies different periods by subsection — `17a-4(a)` is 6 years, `17a-4(b)(4)` is 3 years; `2.13/portal-walkthrough.md` L123 explicitly documents the framework's rationale as "6-year requirement + 1-year buffer." The 1.4 code sample may be intentionally citing the statutory minimum for that subsection. **Recommendation:** NEEDS_HUMAN_REVIEW — a regulatory SME should confirm whether framework policy is "always 7 years" or "cite per-subsection minimums."
**SEVERITY_CORRECTIONS:**
- **Fix 4 should be elevated from WARNING → GAP/NEEDS_HUMAN_REVIEW.** Inconsistent retention periods in FSI regulatory documentation carry compliance risk — an auditor comparing 1.2's "7 years" with 1.4's "6 years" will flag the discrepancy. This is not a cosmetic inconsistency; it's a substantive regulatory claim that needs human adjudication.
- **Fix 5 severity is correctly classified as GAP.** The 10-year retention for Z3 compliance sign-off without citation is a legitimate gap — it could be defensible (SOX record retention or internal policy) but needs an explicit citation.
- **Fixes 1, 2, 3, 6 are correctly classified as WARNINGs** — all cosmetic/consistency issues with zero compliance impact.
**FIX_ORDERING:**
- **Fixes 1, 2, 3, 6:** Independent, zero dependencies, safe to execute in any order or in parallel. All are pure formatting changes with no cross-file interaction.
- **Fixes 4 and 5 must be resolved together** as a single retention-policy decision: the framework needs a unified position on SEC 17a-4 retention (statutory 6 years vs. conservative 7 years vs. extended 10 years for Z3) before any individual file is changed. Fix 4 without Fix 5 leaves Z3 retention undefined; Fix 5 without Fix 4 may create a third variant.
- **Recommended execution order:** {1, 2, 3, 6} → human review of {4, 5} → {4, 5} together.
**OVER_ENGINEERED:**
- **Fixes 2 and 3 (adding header metadata to 1.4 files)** are borderline. The 1.4 blockquote intro pattern is functionally valid and self-documenting. However, given the project's explicit template consistency mandate, these are justified — but should be lowest priority since they add no compliance or functional value.
**CRITIQUE_SUMMARY:** The review's false-positive analysis (URLs, nav orphans) is excellent and saved significant wasted effort. However, Fix 4's proposed "standardize on 7 years" recommendation is under-analyzed — it misses the `4.3/powershell-setup.md` instance (creating incomplete scope), overlooks that SEC 17a-4 subsections have genuinely different statutory periods, and proposes changing deployed-code values without a migration caveat. Fixes 4 and 5 should be bundled for human/regulatory review before execution; the remaining four fixes are safe to ship immediately.