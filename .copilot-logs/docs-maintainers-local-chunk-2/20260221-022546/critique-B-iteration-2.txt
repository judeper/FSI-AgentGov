Based on my analysis, all 12 issues and all 7 proposed fixes target `maintainers-local/` which is **gitignored** (line 31 of `.gitignore`) and does not exist in this worktree. Here is my assessment:
---
**HIGH_RISK_FIXES:**
- NONE. All 7 fixes target gitignored `maintainers-local/` files. Zero risk to published docs, mkdocs build, or git history. These files cannot break cross-references because they are not part of the site build or repository.
**SEVERITY_CORRECTIONS:**
- Fixes 1-4 (researcher-package controls 00-03): CRITICAL→DOWNGRADE TO WARNING. These are local-only files not tracked in git. They matter only if actively distributed to external reviewers, which is an out-of-band process. The data staleness is real but the blast radius is confined to a single maintainer's local copy.
- Warnings about `maintainers-local/analysis/`, `maintainers-local/reviews/`, `maintainers-local/notes/structure.md`, `maintainers-local/notes/implementation-plan.md`: WARNING→DOWNGRADE TO INFO/NO-FIX. These are **point-in-time historical documents** (dated Dec 2025 / Jan 2026, pre-v1.1). Updating them would destroy their historical accuracy. They should remain as-is or be clearly labeled as superseded, not rewritten.
- mkdocs nav orphans: Correctly identified as false positive — no action needed.
**FIX_ORDERING:**
- Fix 7 (`python scripts/compile_researcher_package.py`) MUST run BEFORE fixes 1-4. If the script regenerates the researcher package from source controls, it will auto-populate the missing 9 controls, making manual edits to files 00-03 redundant. Run the script first, verify output, then only apply manual fixes if gaps remain.
- Fixes 5-6 (version bumps in notes) are independent and can run in any order.
**OVER_ENGINEERED:**
- Fixes 1-4 are redundant with Fix 7. The `compile_researcher_package.py` script exists specifically to regenerate the researcher package from source control files. Running it should pick up controls 1.25-1.28, 2.22-2.24, 3.11-3.12 automatically. Proposing both manual edits AND script regeneration is duplicative — use the script alone.
- Fixes 5-6 (updating version numbers in maintainer scratch notes) are low-value busywork. These local guide files (`claude-code-gsd-guide.md`, `codex-cli-guide.md`) will go stale again at the next version bump. They are not published or consumed by any automated process.
- Updating historical analysis/review documents (flagged as WARNINGS) would be actively harmful — they are snapshots of a prior state and should not be revised to reflect current numbers.
**CRITIQUE_SUMMARY:** All 12 issues target gitignored local-only files with zero impact on the published site or repository; the review correctly identifies real data staleness but over-classifies severity and proposes redundant manual fixes (1-4) that the existing `compile_researcher_package.py` script (fix 7) should handle automatically.