Now I have all the facts needed. Here's the analysis:
---
**HIGH_RISK_FIXES:**
- Fix #4 (`reports/ZONE-AUDIT-REPORT.md`): Proposed command `python scripts/audit_controls_zone_hygiene.py` references a **non-existent script**. No file matching `audit_controls_zone_hygiene` exists in `scripts/`. Executing this fix as written will fail. Must identify or create the correct script first.
**SEVERITY_CORRECTIONS:**
- **CRITICAL downgrade — `notes/REPO-CONTEXT.md` `validate_docs_anchors.py` reference**: Review flagged "NEEDS_HUMAN_REVIEW whether this script still exists or was renamed." Confirmed `scripts/validate_docs_anchors.py` **does exist** in the repo. Downgrade from CRITICAL to informational — no action needed for that specific bullet.
- **All CRITICALs and WARNINGs scope reduction**: Every file in this review (`researcher-package/*`, `notes/*`, `reports/ZONE-*`, `analysis/*`) resides under `maintainers-local/` (gitignored per line 31 of `.gitignore`) or `docs/analysis/` (gitignored per line 26). **None of these files are committed or published.** The severity of stale data in local-only maintainer notes is inherently lower than stale data in published docs. CRITICALs for researcher-package remain valid (it's a deliverable for external reviewers) but all `notes/` and `analysis/` items should be WARNINGs at most — they're personal reference docs that never leave the maintainer's machine.
- **`reports/ZONE-AUDIT-REPORT.md` and `reports/ZONE-CLEANUP-PLAN.md`**: These files do **not** exist in the committed `reports/` directory (which contains only `audit/`, `documentation-review-2026-01-26.md`, `learn-changes/`, and a regulatory language report). They must be under `maintainers-local/reports/` — again gitignored. Downgrade from CRITICAL/WARNING to informational unless the maintainer actively uses them.
**FIX_ORDERING:**
1. Fix #1 first (`compile_researcher_package.py`) — this is the only fix touching files that serve as an external deliverable, and it's automated via an existing script. Zero manual editing risk.
2. Fix #2 (`notes/REPO-CONTEXT.md`) second — manual text edits to a local-only file; low risk, no dependencies.
3. Fix #3 (`reports/ZONE-CLEANUP-PLAN.md`) third — manual path replacements in a local-only file; no dependencies on other fixes.
4. Fix #4 (`reports/ZONE-AUDIT-REPORT.md`) **blocked** — the proposed script doesn't exist. Must resolve script identity before executing.
5. Fix #5 (nav orphans) — correctly identified as no-op. No action needed.
**OVER_ENGINEERED:**
- Fix #2 (`notes/REPO-CONTEXT.md`): This is a gitignored local scratchpad. Proposing a formal fix with exact string replacements for a file that is never committed or published is over-engineering. A one-line note at the top ("⚠️ Stale — see CONTROL-INDEX.md for current counts") would suffice.
- Fix #3 (`reports/ZONE-CLEANUP-PLAN.md`): Same reasoning — local-only file. Path updates are nice-to-have but not worth formal tracking as a fix item.
- Fix #4: Proposing automated regeneration via a script that doesn't exist adds unnecessary risk. A manual count update or marking the file as superseded is simpler.
- The review tracks 19 issues but 9 of the WARNINGs (`analysis/*`, `notes/structure.md`, `notes/implementation-plan.md`, all 8 jan-update-review files) are explicitly acknowledged as historical point-in-time documents. Tracking them as issues inflates the count — they should be excluded or tracked separately as "known stale, no action."
**CRITIQUE_SUMMARY:** The review correctly identifies the researcher-package as the one actionable fix (regenerable via existing script), but over-scopes by treating gitignored local maintainer notes as formal issues requiring tracked fixes, and proposes one fix (#4) that would fail because it references a non-existent script.