I now have sufficient context. All target files are under `maintainers-local/` which is gitignored (line 31 of `.gitignore`) — never committed or published.
---
**HIGH_RISK_FIXES:**
NONE
All 8 proposed fixes target files under `maintainers-local/` which is gitignored and excluded from the published site. No fix can break `mkdocs build`, cross-references in published docs, or the GitHub Pages site. These are internal-only maintainer reference documents with zero deployment surface area.
**SEVERITY_CORRECTIONS:**
- Fixes 1-4 (researcher-package pillar files): Severity is correctly CRITICAL. These are actively used reference compilations shared with external reviewers. Stale control counts (24→28, 21→24, 10→12) and missing controls in tables/TOCs create a misleading picture of framework coverage and could undermine reviewer confidence. The internal inconsistency (overview says "28 controls" but listing shows 24) is especially damaging for credibility.
- Fix 5 (claude-code-gsd-guide.md "62→71 controls"): Severity is correctly CRITICAL. An AI agent guide with wrong control counts will cause agents to produce incorrect outputs when referencing the framework scope.
- Fixes 6-7 (version bumps, path updates in notes/): Downgrade from WARNING to INFO. These are internal scratch notes (`codex-cli-guide.md`, `structure.md`) that serve as historical snapshots. Updating them risks losing the historical context of what the baseline looked like when they were written, with minimal benefit since no active workflow consumes these version strings.
- Section A nav orphans: Severity classification as FALSE POSITIVE is correct. No action needed.
**FIX_ORDERING:**
- Fixes 1-4 should be applied together as a single atomic batch — they all address the same root cause (controls added in v1.1/v1.2 not backfilled into researcher-package overview sections). Fix 1 (summary guide) depends on fixes 2-4 being consistent, so apply 2→3→4→1.
- Fix 5 is independent — apply anytime.
- Fixes 6-7 are independent and low priority — apply last or defer.
- Fix 8 (nav orphan tooling) is out of scope for this batch — it's a tooling investigation, not a doc fix.
**OVER_ENGINEERED:**
- Fix 7 (notes/structure.md): Proposed update is over-engineered. This file is a historical snapshot from the v1.0 Beta era. Updating paths, counts, version, and architecture description transforms it from a historical artifact into a maintenance burden that will go stale again. **Better approach:** Add a single-line caveat at the top: `> ⚠️ Historical snapshot from v1.0 Beta (Dec 2025). Current framework has 71 controls across a 3-layer architecture. See AGENTS.md for current structure.` This preserves history while preventing misuse.
- Fix 6 (notes/codex-cli-guide.md version bump): Same concern — minor version bumps in internal notes create recurring maintenance churn with no measurable benefit. A caveat line is more durable than version pinning.
- The WARNING-level items in `analysis/` and `reviews/` are correctly noted but no fix is proposed — good restraint. These are historical artifacts and should remain as-is.
**CRITIQUE_SUMMARY:** The critical fixes (1-5) are well-targeted, correctly classified, and carry zero deployment risk since all files are gitignored. Fixes 6-7 are over-engineered for internal scratch notes — a deprecation caveat would be more durable than updating values that will immediately drift again.