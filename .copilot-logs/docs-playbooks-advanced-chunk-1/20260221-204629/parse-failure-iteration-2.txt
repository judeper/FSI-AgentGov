Reason: STATUS field missing or unrecognized value

Now I have all the data. Here is the final review report.
---
## Review Report: `playbooks-advanced-chunk-1`
**ISSUE_COUNT:** 7
---
### CRITICAL (2)
**C1.** `docs/playbooks/advanced-implementations/configuration-hardening-baseline/index.md:3`
Version header states `FSI-AgentGov v1.3` — version v1.3 does not exist. The current framework version is v1.2.51. This will mislead readers into thinking they need a future/unreleased version of the framework.
```
Before: **Status:** February 2026 - FSI-AgentGov v1.3
After:  **Status:** February 2026 - FSI-AgentGov v1.2.51
```
**C2.** `docs/playbooks/advanced-implementations/environment-lifecycle-management/architecture.md:94-96`
An admonition block (`!!! note "Zone Classification Algorithm"`) is inserted between lines 93 and 96, splitting the "Choice Field Value Definitions" table. The table header/separator is at lines 80-81; the admonition at lines 94-95 breaks the table context. Rows for `er_environmenttype`, `er_region`, `er_datasensitivity`, `er_expectedusers`, `pl_action`, and `pl_actortype` (lines 96-129) will **not render as a table** — they'll appear as raw pipe-delimited text on the published site. The admonition must be moved outside the table (before line 80 or after line 129).
---
### WARNINGS (4)
**W1.** Batch-wide — **Inconsistent framework version footers** across 25 files. Seven distinct version strings found:
| Version | Files Using It |
|---------|---------------|
| v1.2 | confidence-and-routing.md, zone1-min-explainability.md, deny-event-correlation-report/* (5 files), environment-lifecycle-management/* (5 files) |
| v1.2.6 | conditional-access-automation/* (2 files), agent-blueprint-promotion-gates/* (3 files) |
| v1.2.10 | microsoft-audit-reporting-tools.md |
| v1.2.12 | human-in-the-loop-triggers.md, labs.md |
| v1.2.48 | evidence-and-audit.md |
| v1.2.51 | dspm-for-ai-policy-pack.md, sharepoint-copilot-preflight/index.md |
| v1.3 | configuration-hardening-baseline/index.md (CRITICAL — see C1) |
If versions represent "last updated at this framework version," this convention is undocumented and creates confusion. Consider standardizing.
**W2.** `docs/playbooks/advanced-implementations/deny-event-correlation-report/index.md:1,362`
Header (line 1) shows `v2.0.0` (solution version) while footer (line 362) shows `v1.2` (framework version). Mixed version semantics — the framework version should be `v1.2.51` per current release. The dual-versioning (solution vs. framework) is not explained to the reader.
**W3.** `docs/playbooks/advanced-implementations/human-in-the-loop-triggers.md:33`
References "FINRA 2026 Annual Regulatory Oversight Report" as an established document defining autonomy levels. **NEEDS_HUMAN_REVIEW** — confirm this document has been published and that the autonomy level definitions (Assisted/Augmented/Automated/Autonomous) accurately reflect its content.
**W4.** `docs/playbooks/advanced-implementations/agent-blueprint-promotion-gates/implementation-guide.md:521`
PowerShell pseudocode bug — `$scanResults.Summary.Passed` references `$scanResults.Summary.Critical` and `$scanResults.Summary.High` *within the same hashtable literal being assigned*. Since `$scanResults.Summary` doesn't exist yet when the hashtable is evaluated, both comparisons resolve to `$null -eq 0` which is `$true` in PowerShell, making `Passed` always `$true`. The script is marked as pseudocode so this is non-blocking, but it sets a bad pattern.
```powershell
# Current (line 521):
Passed = $scanResults.Summary.Critical -eq 0 -and $scanResults.Summary.High -eq 0
# Fix — move after hashtable assignment:
$scanResults.Summary.Passed = ($scanResults.Summary.Critical -eq 0) -and ($scanResults.Summary.High -eq 0)
```
---
### GAPS (1)
**G1.** Batch-wide — No standardized playbook footer convention exists. Control files have a documented footer format (`Updated: Month Year | Version: vX.X | UI Verification Status: Current`), but playbooks use a free-form `*FSI Agent Governance Framework vX.X - Month Year*` pattern with no consistency requirements. This contributes to the version sprawl in W1.
---
### FIXES (3)
**F1.** `docs/playbooks/advanced-implementations/configuration-hardening-baseline/index.md:3`
```
Old: **Status:** February 2026 - FSI-AgentGov v1.3
New: **Status:** February 2026 - FSI-AgentGov v1.2.51
```
**F2.** `docs/playbooks/advanced-implementations/environment-lifecycle-management/architecture.md:93-96`
Move the admonition block (lines 94-95) to after the table ends at line 129. The table rows must be contiguous for markdown rendering:
```markdown
# Current (broken):
| **er_zone** | Zone 3 | 3 |
!!! note "Zone Classification Algorithm"
    Zone is automatically determined by...
| **er_environmenttype** | Sandbox | 1 |
# Fixed:
| **er_zone** | Zone 3 | 3 |
| **er_environmenttype** | Sandbox | 1 |
...
| **pl_actortype** | System | 3 |
!!! note "Zone Classification Algorithm"
    Zone is automatically determined by...
```
**F3.** `docs/playbooks/advanced-implementations/agent-blueprint-promotion-gates/implementation-guide.md:516-522`
```powershell
# Current:
    $scanResults.Summary = @{
        Critical = ($scanResults.Findings | Where-Object { $_.Severity -eq "Critical" }).Count
        High = ($scanResults.Findings | Where-Object { $_.Severity -eq "High" }).Count
        Medium = ($scanResults.Findings | Where-Object { $_.Severity -eq "Medium" }).Count
        Low = ($scanResults.Findings | Where-Object { $_.Severity -eq "Low" }).Count
        Passed = $scanResults.Summary.Critical -eq 0 -and $scanResults.Summary.High -eq 0
    }
# Fixed:
    $scanResults.Summary = @{
        Critical = ($scanResults.Findings | Where-Object { $_.Severity -eq "Critical" }).Count
        High = ($scanResults.Findings | Where-Object { $_.Severity -eq "High" }).Count
        Medium = ($scanResults.Findings | Where-Object { $_.Severity -eq "Medium" }).Count
        Low = ($scanResults.Findings | Where-Object { $_.Severity -eq "Low" }).Count
    }
    $scanResults.Summary.Passed = ($scanResults.Summary.Critical -eq 0) -and ($scanResults.Summary.High -eq 0)
```
---
### Section A Pre-Pass Disposition
All **15 broken external URLs** from the automated pre-pass are **false positives** for this batch:
| Category | URLs | Disposition |
|----------|------|-------------|
| Placeholder/example domains | `contoso.sharepoint.com`, `contoso.crm.dynamics.com`, `your-org.crm.dynamics.com`, `yourorg.crm.dynamics.com` | Intentional example URLs — not bugs |
| API endpoints requiring auth | `api.applicationinsights.io` (×2) | Valid endpoints used in code samples; return 404 without bearer token |
| Microsoft portal URLs | `make.powerautomate.com` | Valid URL; HTTP 405 from automated HEAD request |
| Template variable URLs | `powershellgallery.com/api/v2/package/$module` | `$module` is a PowerShell variable placeholder |
| BAP API URLs with backtick | `api.bap.microsoft.com/...` (×5) | Trailing backtick is the closing markdown inline code delimiter, not part of the URL |
| Graph API template | `graph.microsoft.com/v1.0/groups/@{triggerBody(` | Power Automate expression syntax in a code block |
The **mkdocs nav orphans** (100+ entries) are a repo-wide issue unrelated to this batch — the files in this batch exist at their expected paths; the orphans are from other sections (controls, framework, other playbooks) whose files are absent from this worktree.
---
### STATUS: **CONDITIONAL_PASS**
Two critical issues (C1: version mismatch, C2: broken table rendering) require fixes before publishing. All other findings are warnings or minor gaps. Content quality, regulatory language compliance, PowerShell annotation discipline (pseudocode warnings), and cross-reference integrity within the batch are strong.