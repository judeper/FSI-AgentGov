Let me verify the control name claims for the `config/README.md` issue.
Now I have all the data. Here's the risk analysis:
---
**HIGH_RISK_FIXES:**
NONE — All 7 fixes target `scripts/README.md` and `scripts/config/README.md`, which are **not in `docs/`** and are **not referenced in `mkdocs.yml` nav**. These files are outside the MkDocs build scope, so no fix can break cross-references or the mkdocs build. Zero build risk.
**SEVERITY_CORRECTIONS:**
- **Fix 6 (config/README.md control names):** Correctly flagged as NEEDS_HUMAN_REVIEW. The review's own control name lookups are confirmed accurate — Control 1.3 is "SharePoint Content Governance and Permissions" (not "Data Loss Prevention Policies") and Control 2.6 is "Model Risk Management Alignment with OCC 2011-12/SR 11-7" (not "Risk Assessment Framework"). However, severity should be **WARNING not lower** — these are example snippets in a how-to guide, and incorrect control names could cause an admin to update the wrong control. The example claims `Control IDs must match those in docs/controls/CONTROL-INDEX.md`, making accuracy an internal consistency issue, not merely illustrative.
- **All other severities are accurate.** The two CRITICALs are correctly classified: the governance directory tree showing 1 script when 19 files exist, and the "Planned" heading for already-implemented scripts, are both factually misleading to readers.
**FIX_ORDERING:**
1. **Fix 2 first** (update governance tree) — it establishes the accurate directory picture
2. **Fix 3 second** (rename "Planned" heading) — depends on accurate tree from Fix 2
3. **Fix 1 third** (add `config/` to tree) — independent but logically groups with tree fixes
4. **Fixes 4, 5, 7 in any order** (inventory tables, footer version, add `verify_language_rules.py`) — independent leaf changes
5. **Fix 6 last** (config/README.md control names) — separate file, needs human decision on whether to correct names or add "illustrative" disclaimer
**OVER_ENGINEERED:**
- **Fix 2:** Listing all 19 governance files in the directory tree is over-engineered. The tree should show a summary line like `├── governance/ # 19 files — see governance/README.md` rather than expanding every file, matching the pattern already used for `hooks/`. The governance `README.md` already has the full inventory table.
- **Fix 4:** Adding full inventory categories for every root-level script risks creating a maintenance burden that caused the current staleness. A better approach: add a "Other Scripts" catch-all row or a note saying "See directory listing for additional scripts" rather than exhaustively cataloguing all ~35 root scripts in tables that will go stale again.
**CRITIQUE_SUMMARY:** The review is thorough and all 7 issues are real — confirmed by filesystem inspection. The main risk is not breakage (zero mkdocs impact) but over-engineering the fixes into a new maintenance burden; prefer summary references to `governance/README.md` over duplicating its inventory, and avoid exhaustive root-script cataloguing that will drift again.