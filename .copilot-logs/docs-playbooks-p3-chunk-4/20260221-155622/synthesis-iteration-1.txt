The issues are in `3.12/powershell-setup.md`. Let me verify both critical issues.
Both CRITICALs verified against file content. Here is the synthesized final review:
---
ISSUE_COUNT: 2
CRITICAL:
- `docs/playbooks/control-implementations/3.12/powershell-setup.md` (line 346): Script 2 FetchXML uses `operator="last-x-days" value="0"` to find "already expired" exceptions (per comment at line 332). This operator returns records from "the last 0 days" — effectively nothing or only today — not records whose expiration date has passed. The correct pattern is `operator="lt" value="$today"` (before today), consistent with the `on-or-before`/`on-or-after` pattern in Script 1 (lines 320-321). `$today` is already defined at line 298. This silently returns wrong data rather than failing, making it worse than a crash for audit workflows.
- `docs/playbooks/control-implementations/3.12/powershell-setup.md` (lines 666–706): Script 4 (`Export-ExceptionAuditEvidence.ps1`) is a non-functional stub. The FetchXML query is a placeholder comment (line 666), `Export-Csv` is commented out (line 671), and `[System.IO.File]::OpenRead($exportFile)` at line 679 will crash because no file was ever written. Additionally, metadata placeholders `[COUNT]`, `[FIRST]`, `[LAST]` (lines 705-706) are literal strings never populated with dynamic values.
WARNINGS:
- NONE
GAPS:
- `docs/playbooks/control-implementations/3.12/powershell-setup.md` Script 4: Requires a complete implementation — actual FetchXML query, `Get-CrmRecords` call, data transformation, uncommented `Export-Csv`, and dynamic population of `[COUNT]`/`[FIRST]`/`[LAST]` metadata. This is effectively new code authoring (~30+ lines), not a simple fix, and carries risk of introducing bugs into copy-paste audit scripts.
FIXES:
1. `docs/playbooks/control-implementations/3.12/powershell-setup.md` (line 346): Replace `<condition attribute="fsi_expirationdate" operator="last-x-days" value="0" />` with `<condition attribute="fsi_expirationdate" operator="lt" value="$today" />`. Surgical one-line change; `$today` already defined at line 298. Apply first.
2. `docs/playbooks/control-implementations/3.12/powershell-setup.md` (lines 666–706): Complete Script 4 by inserting a full FetchXML query (modeled on Script 1 lines 304-326 but without date windowing to capture all approved exceptions), adding `Get-CrmRecords` execution, uncommenting `Export-Csv`, and replacing `[COUNT]`/`[FIRST]`/`[LAST]` with dynamic values (e.g., `$exportData.Count`, min/max of date field). **HIGH RISK**: this is new functional code in an FSI audit-evidence script — should be reviewed carefully and ideally validated against a Dataverse environment before merging. Apply second.
STATUS: ISSUES_FOUND