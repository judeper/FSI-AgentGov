Let me count the nav orphan entries precisely and verify the batch files' cross-references.
Now let me verify cross-references within the batch and check for language violations.
No FSI language violations found. The `contoso.crm.dynamics.com` URL appears 9 times, all within PowerShell `.EXAMPLE` blocks and usage examples — this is standard Microsoft placeholder convention, not a broken link.
ISSUE_COUNT: 474
CRITICAL:
- mkdocs.yml: 473 nav entries reference files not found in this worktree (systemic repo-wide issue affecting `mkdocs build --strict`). Covers controls/, framework/, playbooks/, reference/, downloads/, getting-started/, and index.md.
WARNINGS:
- docs/playbooks/control-implementations/3.12/powershell-setup.md: `https://contoso.crm.dynamics.com` flagged as broken URL by automated tooling. This is Microsoft's standard placeholder domain used in `.EXAMPLE` blocks and usage examples — not a production URL. No content change needed; this is a false positive from URL validation.
GAPS:
- NONE
FIXES:
1. mkdocs.yml: 473 nav entries reference files not present in this worktree. Either create the missing doc files or ensure they exist in the main branch before running `mkdocs build --strict`. This is a systemic repo-wide issue outside the scope of this 3-file batch.
STATUS: ISSUES_FOUND
**Batch content quality note:** All three Control 3.12 playbooks (powershell-setup, troubleshooting, verification-testing) are well-structured, internally consistent, and FSI-language-compliant. Zone policies (Zone 1: 90d/1 approver, Zone 2: 60d/2 approvers, Zone 3: 30d/3 approvers) and renewal limits (max 2) are consistent across all files. Cross-references within the batch (`verification-testing.md`, `troubleshooting.md`, `portal-walkthrough.md`) are correct. PowerShell cmdlets (`Connect-CrmOnline`, `Get-CrmRecords` from `Microsoft.Xrm.Data.PowerShell`) are valid. Heading hierarchy, code block language tags, and table formatting are all correct.