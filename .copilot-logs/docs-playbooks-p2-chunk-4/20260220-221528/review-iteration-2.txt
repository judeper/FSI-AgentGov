Now let me read the 2.24 troubleshooting file that was too large, and check for the broken internal links in 2.24/powershell-setup.md:
I've now read all 21 files. Let me do targeted checks for language compliance and verify the 2.24/powershell-setup.md "broken links."
Now I have all findings. Here's my review:
ISSUE_COUNT: 5
CRITICAL:
NONE
WARNINGS:
- docs/playbooks/control-implementations/2.23/powershell-setup.md: Script 5 (New-DisclosureComplianceReport.ps1) lines 506 and 526 — `Write-Host "=" * 80` and `Write-Host "`n" + "=" * 80` will fail at runtime; PowerShell requires parentheses for expression evaluation: `Write-Host ("=" * 80)`
- docs/playbooks/control-implementations/2.23/powershell-setup.md: Script 3 (Get-ConsentRecords.ps1) — `Get-AccessToken` helper function is defined at line 380 (end of script) but called at line 339; PowerShell .ps1 files execute top-down, so this call will fail with "command not found". The function must be defined before the call, or the architecture note at line 334 should clarify the function must be moved or sourced separately.
- docs/playbooks/control-implementations/2.23/powershell-setup.md: Script 2 uses `Get-AdminPowerAppChatBot` and `Get-AdminPowerAppChatBotTopic` cmdlets which may not exist in the Microsoft.PowerApps.Administration.PowerShell module — NEEDS_HUMAN_REVIEW
- docs/playbooks/control-implementations/2.23/powershell-setup.md: Script 1 uses `Invoke-MgGraphRequest -Uri "https://graph.microsoft.com/v1.0/admin/copilot/settings"` — the `/admin/copilot/settings` Graph API endpoint may not exist in v1.0; script already has a caveat comment but users may attempt to run as-is — NEEDS_HUMAN_REVIEW
- docs/playbooks/control-implementations/2.23/troubleshooting.md: Line 230 code block uses ` ```sql ` tag but contains FetchXML (XML), not SQL — should be ` ```xml `
GAPS:
NONE
FIXES:
1. docs/playbooks/control-implementations/2.23/powershell-setup.md (line 506): Change `Write-Host "=" * 80 -ForegroundColor Cyan` to `Write-Host ("=" * 80) -ForegroundColor Cyan`
2. docs/playbooks/control-implementations/2.23/powershell-setup.md (line 526): Change `Write-Host "`n" + "=" * 80 -ForegroundColor Cyan` to `Write-Host ("`n" + "=" * 80) -ForegroundColor Cyan`
3. docs/playbooks/control-implementations/2.23/troubleshooting.md (line 230): Change ` ```sql ` to ` ```xml `
4. docs/playbooks/control-implementations/2.23/powershell-setup.md: Move the `Get-AccessToken` function definition (lines 379-387) above the `try` block (before line 330) so it is available when called at line 339, or add a prominent warning that the function is a placeholder requiring implementation before the script is usable
NOTE ON SECTION A PRE-PASS FINDINGS:
- **Broken External URLs**: All 17 are false positives — placeholder/example domains (contoso.com, tenant.sharepoint.com, org12345.crm.dynamics.com), portal URLs requiring authentication (admin.powerplatform.microsoft.com, copilotstudio.microsoft.com, make.powerautomate.com), API endpoints in code samples (api.bap.microsoft.com), or API scope strings (api.bap.microsoft.com/.default). No fixes needed.
- **Broken Internal Links in 2.24/powershell-setup.md**: All 5 are false positives — PowerShell array expressions like `("Allowed", "Restricted", "Prohibited")` inside code blocks were misinterpreted as markdown links by the scanner. No fixes needed.
- **MkDocs Nav Orphans**: Worktree artifact — files exist in the main repository but not in this partial worktree. Not within scope of this chunk review.
STATUS: ISSUES_FOUND