All findings verified against source files. Here is the synthesized review:
ISSUE_COUNT: 4
CRITICAL:
- docs/playbooks/control-implementations/2.14/powershell-setup.md: Prerequisites (line 14) requests `LearningContent.Read.All` Graph scope, but no script in the file uses Viva Learning APIs. The actual scripts use `RoleManagement.Read.All` (line 36). Admins following this guide will consent to an unnecessary API permission in their tenant — a least-privilege violation with real security impact in regulated environments. Copy-paste error confirmed by correct scope on line 36.
WARNINGS:
- docs/playbooks/control-implementations/2.13/powershell-setup.md: Validation script Check 3 (lines 166-168) references only "WORM Storage" but the companion portal-walkthrough documents both WORM and audit-trail alternatives per the October 2022 SEC 17a-4 amendments. The comment (line 166), heading (line 167), and info line (line 168) should all reflect both options.
- docs/playbooks/control-implementations/2.13/verification-testing.md: Same WORM-only terminology inconsistency as the powershell-setup — Test 3 title (line 19: "Verify WORM Compliance"), TC-2.13-04 (line 40: "WORM prevents deletion"), evidence checklist (line 50: "WORM storage settings"), and attestation template (line 74: "WORM storage is configured for Zone 3") all omit the audit-trail alternative documented in the portal-walkthrough.
- docs/playbooks/control-implementations/2.13/powershell-setup.md: Line 41 `Connect-PnPOnline -Url "$AdminUrl$SiteUrl"` concatenates the admin URL (e.g., `https://tenant-admin.sharepoint.com`) with `/sites/AI-Governance`, producing an invalid URL retaining `-admin`. The Complete Configuration Script (line 226) correctly handles this with `$AdminUrl -replace "-admin", ""`, but the standalone `New-AIGovernanceSite.ps1` block does not. NEEDS_HUMAN_REVIEW — may be intentional simplification given placeholder URLs in `.EXAMPLE`, but is a functional script bug.
GAPS:
- NONE
FIXES:
1. docs/playbooks/control-implementations/2.14/powershell-setup.md (line 14): Change `Connect-MgGraph -Scopes "User.Read.All", "LearningContent.Read.All"` to `Connect-MgGraph -Scopes "User.Read.All", "RoleManagement.Read.All"` to match the scopes actually used and follow least-privilege.
2. docs/playbooks/control-implementations/2.13/powershell-setup.md (lines 166-168): Change comment `# Check 3: WORM storage (manual)` to `# Check 3: WORM / audit-trail storage (manual)`, heading `Write-Host "[Check 3] WORM Storage"` to `Write-Host "[Check 3] WORM / Audit-Trail Storage"`, and info line to `Write-Host "[INFO] Verify WORM or audit-trail alternative for Zone 3 (SEC 17a-4)"`.
3. docs/playbooks/control-implementations/2.13/verification-testing.md: Update four WORM-only references to include the audit-trail alternative — line 19 to "Verify WORM / Audit-Trail Compliance (Zone 3)", line 40 to "WORM or audit-trail prevents deletion", line 50 to "WORM or audit-trail storage settings", line 74 to "WORM or audit-trail storage is configured for Zone 3 (if applicable)".
4. docs/playbooks/control-implementations/2.13/powershell-setup.md (line 41): Change `Connect-PnPOnline -Url "$AdminUrl$SiteUrl"` to use the same `-replace "-admin", ""` pattern as line 226: `$siteUrl = $AdminUrl -replace "-admin", "" -replace "sharepoint.com", "sharepoint.com$SiteUrl"` then `Connect-PnPOnline -Url $siteUrl -Interactive`. NEEDS_HUMAN_REVIEW.
STATUS: ISSUES_FOUND